# 逆境之路 AdversityRoad

**V2.0**：由玩家真实目标驱动的个性化开放世界逆境动作 RPG。
玩家不是为了"通关心理章节"而行动，而是为了一个自己选择的目标穿越世界。

> 输入一个你真正想实现的目标，游戏就把通往它的道路变成一座
> 会观察你、针对你、阻挡你，也允许你学习、逆袭并最终穿越的开放世界。

> 目标是方向，开放世界是道路，逆境是阻力，战斗是回应，失败是证据，
> 复盘是学习，宿敌是成长对照，逆袭是变化的证明，最终胜利是抵达自己选择的目标。

- 引擎：Unity 6000.5（URP，安卓/PC 双端）
- 打包：GitHub Actions + game-ci/unity-builder（推送到 `main` 或 `claude/**` 分支自动构建安卓 APK，Library 已缓存）

## V2.0 上层操作系统（本次迭代）

V2.0 采用"保留资产与机制、重构上层调度"的策略：**V1.0 的七大章节、24 个子章、
全部敌人 / Boss / 动作 / 姿态 / 技能 / 装备 / 言语攻防 / 心理数值 / 复盘 / 安全屋
一个都没有删除**——它们被升级为 Goal OS 可以动态调用的「通用逆境模块库」。

| V2.0 系统 | 它解决的问题 | 落在哪里 |
| --- | --- | --- |
| **Goal OS** | V1 把心理章节当主线，玩家理解成"我要完成一套心理训练"；V2 把结构反转，目标才是主线 | `Scripts/Goals/GoalOS.cs`、`GoalParser.cs` |
| **Goal Journey Graph** | 目标旅程是有向图不是一条线：直接打 / 先训练 / 找资源 / 绕开 / 失败后恢复都是路 | `GoalJourneyGraph.cs` |
| **四维进度** | 拒绝"完成了 63%"的百分比幻觉：里程碑 · 关键行动 · 已消除障碍 · 能力准备 | `GoalJourneyGraph.Progress` |
| **AI 目标专属章节（强制）** | 只匹配固定模块 = 心理标签推荐；每条旅程必须至少有一个只属于这个目标的章节 | `GoalChapterGenerator.cs`、`ChapterBlueprintValidator.cs` |
| **Fallback Blueprint** | 没网络 / 没 API Key / 生成被拒时，本地照样产出合法的专属章节 | `FallbackBlueprintLibrary.cs` |
| **开放世界** | 从关卡选择器升级为一片连续可走的持久化城市：家门口一路走到任务区不过菜单 | `Scripts/OpenWorld/OpenWorldBuilder.cs` |
| **三层世界** | 现实层可信 → 逆境层扭曲但可识别 → 心理裂隙通往 V1 经典关卡 → 回到原地点 | `WorldLayerController.cs` |
| **逆境导演** | 不是作弊 AI，而是找"足够困难但可学习"的区间；五维预算，公平边界写死在代码里 | `Scripts/Adversity/AdversityDirector.cs` |
| **自适应敌人 / 宿敌** | 敌人学的是**游戏行为**不是隐私；被识破就换打法；宿敌记住每一场 | `AdaptiveEnemyController.cs`、`NemesisSystem.cs` |
| **Stress / Resolve** | 把"快撑不住"做成可玩体验；逆转只奖励压力下的高质量行动，绝不随机送胜利 | `StressStateMachine.cs`、`ResolveSystem.cs` |
| **逆境画像** | 只用可观察行为；每条边带样本数与置信度；可查看、可删除、可一键关闭 | `AdversityProfile.cs` |
| **AI 现场生成场景** | 目标不同 → 走进去的那个地方就不同：房间/道具/NPC/灯光/规则/台词全部现造，不受 24 个固定场景限制 | `Scripts/Goals/SiteBlueprint.cs`、`Scripts/OpenWorld/SiteBuilder.cs` |
| **动作语法** | 十项帧数据全部公开，并自检"重招必须可读"——Boss 靠要求更高而不是看不清变难 | `Scripts/Combat/MoveGrammar.cs` |
| **敌方武术十类** | 敌人不是"血量不同的同一个人"；玩家学的是应对一类打法，而不是背招表 | `Scripts/AI/MartialArchetype.cs` |

### 一条旅程是怎么跑起来的

1. 在「目标」面板写下你想去的方向（或选模板）。系统当场做**有效性检查**：
   行动性 / 完成定义 / 现实可控性 / 冲突提示；以伤害、犯罪、报复为方向的目标**直接拒绝**并给出安全替代。
2. Goal Parser 生成初始 Goal Model：里程碑、关键行动、障碍、需要成长的能力、风险。
3. 章节装配：V1 七大线按**目标相关性**插入 → 你自己预设的障碍可以加进来 →
   AI 生成**至少一个**目标专属章节（过 CQS≥0.72 与四重校验才准进世界，不合格就用本地兜底）。
4. 走进开放城区。当前里程碑的章节在对应区域展开；Legacy 章节则在现实地点长出**心理裂隙**。
5. 打不过就撤：与目标无关的挑衅，解除锁定离开判为**边界胜利**。
6. 输了不是重生了事：世界状态持久变化（路线关闭但出现替代路线、宿敌升级换巡逻区、
   时间推进），失败的那个敌人成为**宿敌候选**并记住你的打法。
7. 濒临崩溃时打开**逆转窗口**——一次精准格挡或一次说清事实的回击就能翻过来。
8. 全部里程碑完成 → **终局守门人**出现，它守的是你自己写下的完成条件，
   不是统一的旧我。击败它，这条旅程进入 Hall of Goals。

### AI 现场生成场景：从"输入目标"到"走进一个新地方"

这是 V2.0 与"只有 24 个固定关卡"的分界线。玩家写下目标后，链路是：

```
目标文本
  → Goal Parser：里程碑 / 关键行动 / 障碍 / 能力
  → AI 章节蓝图（三层 Prompt + 严格 JSON 契约）
      · 关卡层：章节名、绑定的里程碑与障碍、敌人组合、Boss、机制、成败条件
      · 场景层 site：场景名、类型、布局、氛围、房间(名/用途/道具)、
                     NPC(角色类型/数量/行为/环境台词)、规则、
                     externalLines(敌人喊的话) / internalLines(脑内回声)、可交互物
  → Validator：清洗到已批准 Kit/道具/角色库内 + CQS≥0.72 + 四重校验 + 台词逐句安全过滤
  → SiteBuilder：**运行时把它建出来**（几何/道具/灯光/NPC）→ 单独烘焙 NavMesh
                 → 注册为动态区域（区域表可运行时追加，不再固定 25 个）
  → 城区里长出 SiteGate（牌子是 AI 起的名字）→ 按 E 走进去
  → 进场接管台词池：敌人与脑内回声都换成这一关专属的句子；规则逐条念出来
  → 通关/阵亡 → 回到城里原地 → 场景整体卸载
```

**生成什么**

| 层面 | 由 AI 决定 | 由引擎决定（AI 永远拿不到） |
| --- | --- | --- |
| 场景类型 | 26 种已批准 Kit（办公层/机房/公寓/地铁/商场/病房/仓库/天台…） | 具体几何与尺寸 |
| 布局 | 房间群 / 长廊 / 迷宫 / 大厅 / 开放街区 / 围合院落 | 每面墙、每条通路的坐标 |
| 房间 | 名字、用途、尺寸、道具清单 | 房间怎么摆、门开在哪 |
| 道具 | 从 32 种已批准道具里挑 | 构件形状、碰撞、朝向 |
| NPC | 角色类型、数量、行为、环境台词 | 出生点、导航、日程 |
| 氛围 | 8 种（白天/黄昏/夜/雨/冷光/暖光/闪烁/雾） | 灯光参数 |
| 规则与台词 | 关卡规则、外部言语攻击、内心言语攻击 | 何时触发、如何过滤 |

**三条硬约束**：AI 只给语义不给坐标；相同 `assemblySeed` 必定生成完全相同的场景；
建完立刻在本场景范围内单独烘焙导航，绝不放出未经验证的几何。

**门怎么处理**：经典 24 关的门不写死目标，而是按章节顺序在运行时解析——
每关只留「上一关 / 下一关」两扇（否则训练武馆会同时开着三扇通往不相邻关卡的门）。
但开放城区常开常在、AI 现场生成的场景更是压根没有章节号，两者都算不出相邻关，
所以 `Portal` 另留一条**显式目标**通道专供它们使用，经典关卡一律照旧走章节解析，
不退回"每扇门硬编码目标"的老问题。

**没有 API Key 也能跑**：本地 Fallback 为十条弱点轴各写了一套完整场景骨架
（房间/道具/NPC/内外台词齐全），离线生成的仍然是"这个目标专属的地方"——
只是措辞不如云端贴合。

### 开放世界垂直切片（方案 21.2 验收项）

一片连续可自由移动的城市区域：**玩家住宅 + 街道 + 公交站 + 商店 + 办公区 + 小巷**。

| 区域 | 环境 | 玩法 |
| --- | --- | --- |
| 住宅区 | Player Home（玄关/客厅/卧室/厨房/卫生间可走动）、小区楼栋、便利店、绿地长椅 | 生活、准备、恢复、社会事件 |
| 商业区 | 商场、店铺、广场 | 人群压力、机会、交易 |
| 工作区 | 写字楼、可进入的一层大堂、会议桌、招聘区 | 目标专属任务、评价压力 |
| 交通区 | 主干道、十字路口、公交站、地铁入口、停车场 | 路线选择、时间压力、追逐 |
| 生活服务区 | 医院、餐馆、银行、公园 | 资源、支持、恢复 |
| 边缘区 | 小巷、车库入口、废弃建筑 | 高风险遭遇、宿敌 |

**Player Home 的物理 UI**（方案 17.3：菜单功能由房间物件承载，而不是一屏按钮）：
墙上目标板 → 目标与旅程；书桌 → 复盘与行动计划；衣柜 → 装备；镜子 → 角色与旧我；
电脑 → 弱点/优势图谱；手机 → 现实行动打卡；冰箱 → 补给。走近按 **E** 使用，门通向开放世界。

## 当前可玩内容

游戏启动后由 `GameBootstrap` 在运行时自动搭建整个世界（不依赖编辑器手工建场，保证 CI 无头打包可用）：

### 章节主线（大章-子章结构 · 对齐方案七大章节）

主线按方案组织为「**大章（成长线）→ 子章（场景关卡）**」，共 8 条线 25 个子章（24 个可玩区域）：

| 大章（成长线） | 子章 · 场景 | 子章心魔 | 状态 |
| --- | --- | --- | --- |
| 序章 · 觉醒 | 其一 独居小屋 | 自我怀疑低语 | ✅ |
| | 其二 训练武馆 | 明日幻影 | ✅ |
| 第一章 · 公平与承诺线 | 其一 两元赌桌 | 两元赖账王 | ✅ |
| | 其二 债务车影 | 新车债王 | ✅ |
| | 其三 小题大做审判庭 | 自我否定法槌 | ✅ |
| 第二章 · 外界刺激线 | 其一 一声咳嗽的街道 | 咳声刺客 | ✅ |
| | 其二 眼神审判走廊 | 万眼审判者 | ✅ |
| | 其三 陌生挑衅路口 | 挑衅镜像 | ✅ |
| | 其四 噪声放大（终战·回访街区） | 刺激放大器 | ✅ |
| 第三章 · 拖延与目标线 | 其一 目标遗忘房 | 目标遗忘者 | ✅ |
| | 其二 拖延沼泽 | 明天之王 | ✅ |
| | 其三 求职沉默荒原 | 无回应之王 | ✅ |
| | 其四 城市广场（终战） | 拖延影魔 | ✅ |
| 第四章 · 边界与责任线 | 其一 老实人消耗局 | 好人牢笼 | ✅ |
| | 其二 责任转嫁法院 | 全责法官 | ✅ |
| | 其三 无限代付走廊（终战） | 无限代付者 | ✅ |
| 第五章 · 低谷与生存线 | 其一 饥饿荒巷 | 饥饿犬影 | ✅ |
| | 其二 车库寒夜 | 寒风刃 | ✅ |
| | 其三 病房回廊（终战） | 低谷巨像 | ✅ |
| 第六章 · 哲学与行动线 | 其一 哲学虚无图书馆 | 概念迷宫师 | ✅ |
| | 其二 无限追问大厅 | 无限问题兽 | ✅ |
| | 其三 意志断桥（终战） | 无限追问者 | ✅ |
| 终章 · 旧我与新我线 | 其一 失败展览馆 | 过去审判官 | ✅ |
| | 其二 意志塔 | 旧声音复读机 | ✅ |
| | 终局 旧事回声馆（含整合结局） | 旧我 | ✅ |

- 章节开场面板显示「大章标题 + 线主题 + 子章名」；击败子章心魔推进主线并解锁下一区域
- 区域解锁按"主线到过的区域集合"判定（支持回访式子章）；锁定的门会明确提示由哪个子章开启
- 24 个区域由传送门按剧情链路相连；「传送」面板可直达任意已解锁区域
- **主线结构重排说明**：本次升级重开主线进度（技能树/装备/图鉴/档案/影子护卫全部保留），
  老玩家可用设置面板「跳过当前子章」快进；死亡后复盘再战，子章进度不丢；通关后进入自由修炼模式

### 方案对齐 · 四大新关卡（本次迭代）

按最终商业化方案把 Demo 四关一 Boss 补齐成型：

- **小题大做审判庭（方案关卡一）**：倾斜的法官席 + 两侧旁观者席阴影；6 枚**浮动标签**
  （"太敏感/小题大做/你也有问题"……）悬浮在通道上空，用兵器击碎回补自尊、降低反刍；
  **证据桌**走近即"看清事实"——此后标签一击即碎（事实先于评价）；**破碎镜子**回补自尊。
  Boss【自我否定法槌】三阶段：**标签弹幕 → 审判冲击波（大红圈预警）→ 自我否定重锤（追身落锤）**；
  二阶段起触发**围攻审判**（旁观席召唤旁观嘲笑者），重锤落地后**法槌裂缝显现**=短暂破绽窗口
- **噪声放大（方案关卡二 Boss）**：回到噪声街区，街心广场上的【刺激放大器】——
  街道新增**噪声区**（广告牌下/公交站旁：站入专注缓慢流失，定心姿态减免、不读心盾免疫，影响很轻防晕）；
  周期性**噪声放大**（全场专注冲击，定心格挡/不读心盾可整个化解）；制造**幻影假目标**
  （攻击幻影=把力气花在猜测上：掉专注、涨反刍）——「不读心盾」使幻影显形消散、
  「注意力回收」全场清除后猛攻真身
- **拖延沼泽（方案关卡三）**：主入口在**噪声街区尽头东南侧**（第九章开启的一瞬门就解锁，
  全链路不再有"提前出现的锁死门"）；浅泥轻减速/深泥重减速+行动力流失、**干地平台**落脚、
  **手机光点区**（站入即流失专注与行动力）、**目标石板**（站上恢复行动力）、床铺藤蔓与漂浮计划纸。
  Boss【明天之王】**泥壳护体**（伤害大减打不动）——点燃场边**三座五分钟火种台**（走近即点燃）
  泥壳崩裂进入长破绽；战斗中召唤明日泥怪、往玩家脚下浇灌临时深泥
- **旧事回声馆（方案 Demo Boss·旧我回声）**：博物馆展柜长廊——靠近展柜触发**旧事回声**（反刍上升），
  站定完成**归档**（看见事实→命名感受→提取边界→转化行动），归档 3 座开启终局大门；
  失败展廊有旧话复读者/过去判官/反刍虫群把守。终局 Boss【旧我】四阶段：
  **旧话复读 → 身份冻结（「五分钟火种」解冻）→ 失败召回（召唤前章旧敌幻影）→ 整合选择**——
  残血后旧我停手且无法再被攻击（血线保护），走进**整合圆环**完成「旧我整合式」：
  **旧我化为影子护卫**随行（永久保存，靠近时反刍消退加速）。
  核心台词：「你曾经保护过我，但现在我要继续往前。」

### 世界模拟

- **昼夜循环**：太阳旋转、光照色温、环境光渐变，路灯夜晚自动点亮
- **街区生态**：行人漫步（NavMesh）、车辆往返行驶、建筑窗户、路面标线
- **室内场景**：独居小屋完整家具陈设与暖光照明

### 战斗系统（借鉴黑神话悟空框架）

- **双系连段（招式全部绑定 Mixamo 动作库真实动作）**：
  拳=巨剑连斩「巨剑横斩→巨剑撩斩→突刺→巨剑旋风斩」，腿=腿法连段「正踢→侧踹→旋身空翻踢→飞踢」，
  两系可自由混接；动画从发力相位起播+提速播放，命中后即开取消窗口——连点无缝衔接不糊招
- **组合招式（连点拳/腿按序自动成招）**：拳拳拳=三连斩·大回旋、腿腿腿=三连腿·裂空飞踢、
  拳拳腿=双斩·空翻旋踢、腿腿拳=双腿·贯心突刺；2 势绝招：拳拳腿腿=龙卷·旋风绝斩、
  腿腿拳拳=踏空·裂地跳劈、拳腿拳腿=剑影·惊鸿飞踢（终结动作均取动作库大招）
- **摇杆磁吸锁敌**：攻击范围内有敌人即自动咬住目标出招（摇杆用于多敌间选目标、或明确指空处脱离），
  磁吸突进差多远冲多远，连点每一击都实打实命中
- **意势系统**（棍势式资源，0-3 点）：命中轻击/完美闪避/蓄力积攒，HUD 圆点显示，满势脚下亮起光环
- **蓄力重击**：按住「重」沉腰举械蓄力（耗体力、积意势），松开释放**蓄力·巨剑跳劈**；
  **势≥2 触发旋风终结**（消耗全部意势大幅增伤）；连段中轻点「重」派生**切手·撩斩**快速反击；
  指令技（轻点重+方向）：前=疾影突刺、后=旋身空翻踢、左右=旋风斩；跳中按重=空袭·裂地跳劈
- **完美闪避**：在敌人攻击命中瞬间翻滚 → 短暂时缓 + 意势+1 + 下一击必暴击（敌人攻击带 0.28s 前摇可读招）
- **空中攻击**：跳+拳=空袭跳劈、跳+腿=飞踢、跳+重=空袭·裂地跳劈；
  **蹲伏攻击**：蹲+腿=扫堂腿（贴地环扫、高削韧）、蹲+拳=低位突刺（下段直线戳击）
- **空中连段**（一次滞空两段，跳跃不再只是位移手段）：第二段剑向=**空中回旋绝斩**
  （环身横扫 + 滞空续航，浮空斩打得完），拳向=**空中连环踢**（压地收招，干净落回）；
  跳+重同样占用滞空额度，落地清零——空中不能无限刷伤害
- **招式规格表（`MoveSpec.cs`：轨迹 / 范围 / 判定点线面 / 伤害，敌我同构）**：
  每一招都有明确规格——轨迹分七类（**点**短促直击 / **线**直线穿透 / **横弧**横扫一片 /
  **纵弧**纵向挑击 / **环**环身 360° / **面**罩住前方 / **空**凌空罩落点），
  各带横宽 × 纵高 × 纵深（米）、判定中心、伤害系数、削韧系数、击退值。
  战斗判定框与「招式表」面板**由同一份规格派生**，不会出现"说明书写 2 米、实际打 1.5 米"
  （「招式表」面板四页：基础规则 / 自由融合 / 玩家数据 / 敌人数据，后两页由规格表实时生成）；
  敌人另有 `EnemyMoveTable`——**同一套结构、各自调值**（玩家大招要付蓄力与意势的代价，敌人不付），
  敌人判定框现按招式轨迹取形：突刺细长（侧移可躲）、回旋斩环身（只能拉开或跳）、
  重砸罩一片（须提前翻滚穿到侧后方），读招之后终于"知道该往哪躲"
- **自由融合连招（`ComboFusion.cs`：放开限制，一切动作都是连招元素）**：
  元素表 = 拳P · 剑K · 重H · **跃J** · **闪D** · **术S**（技能/绝招）· **架G**（招架成功）——
  跳跃、闪避、技能不再是自成一体的孤岛，而是可被前后动作接住的一环。
  - **融合增伤按「用到几种不同元素」阶梯上升**：2 种 ×1.15 / 3 种 ×1.35 / 4 种 ×1.60 /
    5 种 ×1.90 / 6 种以上 ×2.25。单一元素连打（只会拳拳拳拳）没有加成——
    奖励的不是"背下正确按键顺序"，而是**临场把各种手段串起来的能力**；
    招名由实际打出的元素动态生成（拳→跃→剑→术 打出「拳·跃·剑·术 — 四元融合」）
  - **跨元素融招**（在融合链上匹配，与拳剑序列配方并行）：跃→剑=踏空斩·凌云一式、
    闪→剑=闪身突刺·后发制人、术→剑=术后追斩·势不可挡、架→剑=架后反斩、重→剑=重斩接锋，
    跃→重→剑=踏空三叠·裂地崩斩（1 势）、架→拳→剑=架打连环·后发先至（1 势），
    跃→术→剑=踏云术斩·天倾一击（2 势）、闪→术→拳=影遁术拳·无相连环（2 势）
  - **连段收招不清空融合链**（链在 2.2 秒内有效，超时才断）——跳跃/闪避/技能正是用来
    在两段连招之间搭桥的，一收招就清零等于把"跨系统串招"从根上取消掉；
    融合倍率同时作用于普攻、空中段、技能连段的每一段
  - 屏幕左侧连段条改显示**当前融合链与倍率**（「拳·跃·剑 ×1.35」），
    "再换一种手段"的收益当场可见
- **双角色 + 武器库 + 面具库（角色/武器/面具三者资产分离，内置 glTFast 支持 glb/gltf）**：
  「角色」面板先选角色（角色·贰=`PlayerModel2`，glb/gltf/fbx 均可、动作沿用角色壹动作库、
  缺失自动回退；**异源骨架自动对齐**——glb 角色骨名无 mixamorig 前缀/层级不同时自动改名
  与链路对齐，默认动作库直接可用），再从武器库（`Characters/Weapons/`，glb/fbx/gltf/zip）
  选武器——默认持剑（模型无自带兵器时程序化长剑兜底），重选即替换：**握持对齐+手指握拳**
  （复刻角色壹自带巨剑的世界空间握姿、柄端入掌心、五指绕柄轴卷曲攥住剑柄）；
  再从面具库（`Characters/Masks/`，fbx/glb/gltf/zip）选面具戴脸——自动定尺贴脸、随头骨转动，
  支持多面具切换；三者选择均本地持久化。动作库补位：放入 Blocking/Stand To Roll/Stunned/
  Great Sword Casting/Leg Sweep 等专用片段即自动替换现有替代动作（见 MIXAMO_SETUP.md）
- 格挡（边界盾）/ 精准格挡（定心格挡化解心理攻击）；锁定与互动全自动化（无需按键）
- **战斗可读性（对标商业动作游戏的产品化打磨）**：
  - **近镜角色淡出**：镜头贴近任何角色（近身缠斗/贴墙）时该角色整体淡为半透明，
    根治"整屏被模型糊脸/镜头穿模"；镜头碰撞回缩下限抬高，不再缩进身体
  - **角色保持原色**：玩家与敌人均不做整体染色，保留模型原本材质与肤色；
    敌我识别交给头顶状态条、前摇红圈与恶意台词
  - **屏幕固定连击计数器**（格斗游戏惯例）：连击数固定在屏幕右侧弹缩显示、越高越红、断连淡出，
    不再跟着命中点满场乱飞；招式名浮字节流（同名招 0.9s 内不重复弹），战斗信息不刷屏
  - **夜战保底亮度**：夜晚/暗场景环境光与主光下限上调，看清双方拳脚与前摇优先于氛围
- **部位受击反应**：按命中点定位身体部位（头/躯干/腰腹/左右腿），被打部位向打击方向甩动回弹
  （打头甩头、扫腿屈膝），接触点弹出部位标签；冲量可叠加——连打两下头就有两次可见甩头；
  重击倒地为「当场栽倒」：受击位移只在身体后仰头几帧内完成，全程零漂移滑行
- **战斗反馈**：伤害数字、受击闪红、碎块飞溅、顿帧、震屏、程序化动作姿态（前倾挥击/翻滚/倒地/僵直/施法/死亡倒伏消散）；
  **程序化音效**（零外部资产，运行时 PCM 合成）——挥击风声/命中闷响/重击/格挡/定心弹反/前摇警示/受击/死亡各有其声；
  刀光改为细窄半透明加色的一道光（非大色块），绝招/组合技为「能量光爆」（强闪光+放射光条+上升火星+细冲击环）而非平铺色板
- **招式分工**：拳=快攻（出手快、低击退、高频衔接），腿=击退（大幅推开、打断突进），重击=破防（高削韧击破霸体），
  防御=减伤（体力格挡免伤 80%），闪避=反击（完美闪避时缓+返势+必暴击），意志技/定心格挡=反制心理攻击
- **重击倒地**：高伤害攻击将玩家击倒，倒地演出 + 起身无敌帧
- **敌人头顶状态条**：名字+等级、血条、韧性条、实时情绪（窥伺/紧逼/狰狞/讥讽/慌乱）

### 心理攻击（语言+情绪层面）

- 敌人实时**恶意台词**：头顶气泡 + 底部字幕，按弱点轴与所在场景组合（追击中持续低语，心理攻击时高声喊话）
- 受击**暗角脉冲**：物理伤害红色、心理伤害紫色
- **AI 提示词配置**（游戏内「AI台词」面板）：全局提示词 + 各场景提示词均有**默认模板**，可随时修改
- **云端 LLM 台词生成**：支持 **OpenRouter / DeepSeek / EdenAI**（OpenAI 兼容 Chat 接口），
  游戏内配置 API Key 与模型；**延迟方案**——进入区域即后台预取一批台词入池（区域×弱点轴），
  敌人喊话即取即用零等待，池空/断网/未配置时无缝回退本地模板

### 防晕设计（镜头/帧率/后处理/移动）

- **帧率**：安卓默认 30 帧已解锁——目标帧率取屏幕刷新率（60–120），并启用 Frame Pacing（Swappy）稳定帧间隔
- **镜头**：跟随点为平滑 CameraTarget（水平/纵向均临界阻尼软跟随，极短时间几乎无滞后又滤掉逐帧
  移动微抖动，消除晃屏与橡皮筋游动感），转角临界阻尼平滑无过冲，FOV 固定 62° 永不变化，
  碰撞回缩快伸出慢不弹跳，震屏改为极小纵向脉冲（无随机抖动），专注低不再摇镜头（改由暗角表达）
- **三模式运镜（角色转向快 / 位置中速跟随 / 镜头旋转慢）**：
  - **探索镜头**（无敌）：位置用中速 `SmoothDamp` 跟住玩家、角度基本固定；**玩家一转向，
    镜头即刻开始平稳缓慢地转到其背后（面朝方向）**——用大平滑时间的临界阻尼弹簧，转身即跟、
    小抖动只带来极轻微慢移不晃屏；树木/建筑遮挡自动透明
  - **战斗镜头**（锁定敌人）：取景点偏向「玩家↔敌人中点」让**两人同框居中**、压低俯仰、
    适度拉近看清拳脚与前摇。**与探索共用同一套稳定设施**（此前战斗分支是只有 4° 死区的
    裸伺服，探索独有的滤波/迟滞/意图识别/墙壁减速/手动让位一条都不走，
    而近身缠斗恰恰方位角变化最剧烈——2m 处敌人以 2.5m/s 横移就是 71°/s）：
    - **渐进软区**（驱动量＝偏差×(|偏差|/软区)²）：小偏差增益≈0 不追抖动，
      但始终保有微弱回正力。走过两次弯路——先是二值迟滞开关（关断时硬置零角速度＝
      无限 jerk，8 秒内通断 19 次、峰值角加速度 1509°/s²，而峰值角速度只有 38°/s，
      读作"抽动发晕"）；改成硬死区后又出现死区伺服的经典缺陷：**稳态残差恰好等于
      死区大小**，镜头永久停在偏离目标 13~20° 处不再修正，读作"转动很松"，
      且按幅度筛选会把真实运动一并杀掉（0.25Hz 绕行跟随只剩 0.057，镜头基本不跟了）
    - **抗抖靠按频率筛选的低通**（`AimLowPass` 3.2），不靠加大死区——
      低通能压掉 1Hz 以上的缠斗抖动而几乎不影响 0.25Hz 的真实绕行
    - **角加速度限幅 1100°/s²，只限提速不限减速**（阻止镜头停下来只会制造拖尾）；
      只约束镜头自身的运动，玩家手动转镜不受限（自发运动不引起晕动）
  - **持续绕行限速（修复"摇杆推到底角色一直转圈"）**：
    摇杆是镜头相对的（`moveDir = 镜头偏航 + 摇杆角`），而镜头又要绕到角色背后，
    这构成一个代数环——解 `H = C + θ` 且 `C = H`，**只有 θ=0（正前）存在不动点**；
    摇杆持续推向左/右/后时方程无解，角色必然沿弧线行进，**弧的角速度＝镜头绕行速率**。

    这三条性质数学上最多同时满足两条：

    | | (a) 摇杆↔画面一致 | (b) 镜头绕到背后 | (c) 角色不画弧 |
    | --- | --- | --- | --- |
    | 主流第三人称动作游戏 | ✓ | ✓ | ✗（走缓弧） |
    | 本作曾试过的锁存参考系 | ✗（手指按左、画面向前） | ✓ | ✓ |
    | **现方案** | **✓** | **✓** | ✗（缓弧） |

    先前试过锁存移动参考系来断环，环确实断了，但镜头绕到背后后摇杆与画面就对不上——
    实测下来这个代价不可接受，已撤回。缺陷从来不是"会画弧"，而是**弧太紧**：
    本作曾以 207~322°/s 绕行，半径仅 0.9~1.4m，读作原地转圈。
    现由 `SustainedOrbitCap` 把持续绕行压到 30°/s，实测弧线半径
    **正前=直线 / 30°=5.6m / 45°=10.1m / 90°=10.2m / 180°=10.5m**，全部落入大作区间。
    限速只在**推着非正前方向**时生效（`OrbitGateAngle` 12° 起算、30° 过渡）：
    正前小幅修正不限速，摇杆回正或松开后环即消失，镜头恢复全速追平
  - **离轴奔跑的视野靠取景而非转速（引导留白 lead room）**：
    同一条恒等式还有第二个推论——镜头要在角色行进方向背后需 `H-C=0`，
    故**摇杆持续推在离轴 θ 时，镜头在几何上不可能转到背后**。
    实测持续推左 90°，把绕行上限由 30°/s 提到 340°/s，偏离角只从 **87.9° 缩到 75.2°**，
    而角色自转从 29°/s 暴涨到 **207°/s**——提速换不来视野，只换来转圈。
    所以横向/后向奔跑的"看不见前方"只能靠取景解决：焦点朝移动方向前移，
    把角色让到画面后侧、前方空间纳入画面。留白量**随离轴角增大**
    （正前奔跑视锥纵深本就足够，横向/后向最需要），上限 2.2m（角色偏离画面中心 43%，
    仍属三分位构图；2.6m/51% 就贴边了），并叠加离轴拉远 12%。
    留白量本身走 0.5s 平滑——若直接跟随摇杆瞬变，焦点会在 0.09s 内平移 1.75m
    （≈19m/s 的镜头平移），那是比原盲区更糟的晕动源
  - **转向开阔**：转向拉远由 +6% 提到 **+15%**（`_yawErr` 驱动，转完即回）。
    用**拉远吊杆**而不是**放大 FOV**——变焦会显著加剧晕动，纯位移只是把画面推开。
    角色占屏仅在转向瞬间由 40%→35%。
    三项叠加后横跑的**可见前方 5.5m→8.1m、看前时间 1.06s→1.55s（1.5 倍）**
  - **一键镜头回正（双击右半屏 / 桌面 `V`）**：同一条恒等式的最后一个推论——
    "镜头对着角色正前方"要求 `C = H`，与 `H = C + θ` 联立得 **θ = 0**。
    即只要摇杆推在非正前方向，镜头在几何上就不可能对着角色正前方，
    **与转速、与聚焦在哪里都无关**。大作对此的通行解法不是让镜头去追一个无解的目标，
    而是给玩家一个显式动作。0.35 秒 SmoothStep 到位（90° 峰值 386°/s，仍在舒适区），
    比常规自动收敛（0.67s）快 1.9 倍；**回正期间冻结移动参考系**——
    否则快速绕行会把角色一起拖转（0.35s 可拖出 90°），回正反而把人转晕。
    锁定敌人时回正目标改取敌我方位（仍是"看向要打的人"）
  - **回正现在也是自动运镜的一部分**（见下方专节）：交战中未锁定时，
    **取景窗**负责"看得见"（敌人在窗内镜头完全不动，出窗才推回窗沿；窗宽由相机
    FOV 与宽高比精确解出，手机上约 ±30°），
    **自动回正**负责"看得正"（松杆时做一次 0.4~0.8s 的动作把敌人摆回画面中央）。
    玩家主动的一键回正照常保留
  - **各角度的可见前方（实测，取景三项叠加后）**：

    | 摇杆偏角 | 改前 | 改后 |
    | --- | --- | --- |
    | 0°（正前） | 不受限 | 不受限 |
    | 30° | 11.2m / 2.16s | 15.1m / 2.91s |
    | 45° | 8.1m / 1.55s | 11.3m / 2.18s |
    | 90° | 5.8m / 1.12s | 8.7m / 1.67s |
    | 180° | 5.5m / 1.06s | 8.2m / 1.59s |

  - **转向的完整时间线（实测）**：正前奔跑中推左 90°、按住 h 秒后松杆——
    松杆后镜头对准新行进方向恒定只需 **0.68~0.70s**（此时无代数环，全速收敛），
    视野打开（偏差<20°）只需 0.67s。这一段已接近舒适上限：
    峰值角加速度 904°/s²，再压缩到 0.52s 需 1050°/s²，收益 0.15s 而超出舒适区，不值。

    对照旧的不限速版本：对准略快（0.87s vs 1.03s），但**转向会转过头**——
    按住 0.8s 时角色从意图的 -90° 冲到 -227°（多转 137°），并伴随 207°/s 的自转。
    即"提速"不只带来转圈，还让转向不准

    三版实测对照：

    | 版本 | 0.25Hz 真实跟随 | 1Hz 抖动传递 | 稳态残差 | 峰值角加速度 |
    | --- | --- | --- | --- | --- |
    | 原始裸伺服 | 0.731 | 0.326 | 4.0° | 4800°/s² |
    | 硬死区 13° 版 | 0.057 | 0.000 | 13.0° | 290°/s² |
    | **现版（软区 8°）** | **0.659** | **0.097** | **1.0°** | **234°/s²** |

    - 偏差→速度连续映射、墙壁减速、手动转镜让位
    - 实测传递率：1 / 1.5 / 2.5 Hz 的方位抖动由 **0.32 / 0.22 / 0.13 降到 0**，
      而 0.25 Hz 的真实绕行仍保持 **0.63** 跟随——该动时动、该稳时稳
  - **3/4 侧位机位**：战斗时镜头不站在玩家正后方的延长线上（那是最平庸的机位——
    玩家挡住敌人、双方只剩背影与正面两个平面）。偏开一个肩位（22°）后是对峙的经典构图：
    玩家侧背、敌人侧脸，**拳脚的运动轨迹横穿画面**而不是朝镜头戳过来（纵向运动在屏幕上
    没有位移感）。偏哪一侧由两侧可用空间决定，需领先 0.6m 才换边，切换走 1.4 秒慢插值
  - **景别词汇表**（`CameraDirector`：行进中景 / 对峙同框 / 群战拉远 / 狭窄收紧 / 决胜特写）：
    参数级插值＝推轨过去，不是切镜。狭窄判定改为只看**吊杆真正要退去的方向**——
    旧实现取四周六向最短值，只要靠近任何一面墙就推近镜头，哪怕镜头正对开阔地
  - **特写的克制**（"知道何时特写"的另一半是知道何时不特写）：节流 2.6 秒 +
    **群战抑制**（身边 ≥3 个敌人时强度砍半、时长压短——看清包围态势比欣赏这一击重要）+
    强度分级（超必杀满推、处决 0.75、高阶融招 0.65、击杀/完美闪避轻推）。
    此前每个敌人死亡都触发 1.1 秒满推特写，群战连杀会把镜头长期焊在贴脸位
  - **视线高度随景别**：特写抬高到面部、群战压低把包围圈纳入画面；
    此前恒定不变，于是"推近了却仍对着胸口"——推近只是变大而没有变成特写
  - **掉头不再有盲区**：全速前进后立刻反向奔跑，实测盲区（新方向进入画面）
    **开阔 1.58s→0.75s、贴墙 3.21s→0.80s**。四处叠加的延迟逐一拆掉：
    - **`backingIntent` 判据修正（主因，0.6 秒冻结）**：此前用"朝镜头行进了多久"
      区分「转身看正脸」与「掉头往回跑」——可掉头时偏差同样是 180°，被同一条规则
      冻住 0.6 秒。两者输入相同、意图相反，可靠的区分是**速度**：前者原地转身或碎步、
      后者全速持续移动。现按 `moveSpeed > 0.55×runSpeed` 判定，低速看正脸的行为不变
    - **稳定确认时长随偏差缩短**（0.15s→0.02s）：小幅修正需要确认防摇杆抖动，
      180° 掉头本身就是无歧义的意图，再等 0.15 秒纯粹是加盲区
    - **墙壁减速下限 0.25→0.6 且掉头幅度越大越不减速**：旧值把贴墙掉头从 1.15s
      拖到 2.60s，换来的只是吊杆好看一点——而吊杆顶墙本就有碰撞回缩兜底，盲区更要命
    - **二级平滑随偏差缩短**（0.11s→0.05s）：串联的固定平滑在掉头时又多花 0.33 秒
    - **掉头甩镜（whip pan）**：全速反向且偏差 >120° 时转速上限提到 5×（425°/s），
      实测峰值 359°/s，仍在人眼舒适上限内；普通转向不走这条路
- **取景开阔度（实测重调）**：原 vFOV 56° + 吊杆 4.76m 下，角色占屏高 **45%**、
  水平视野仅 **86.8°**——主流第三人称动作游戏是占屏 28~36%、水平 95~105°。
  "人物有分量"被做过头成了"人物填满画面"，这正是「压抑、不开阔」的直接成因。
  更反向的是「对峙」景别还把 FOV **再收窄 1°**，战斗时比探索更闷。
  调参走了两步：65°/吊杆 5.25m 实测「偏晕且角色变小」，回到 **62°/吊杆 4.76m**——
  只保留 FOV 加宽、撤回吊杆加长。两者都会缩小角色，但只有 FOV 能真正扩大周边视野，
  吊杆加长只是等比缩小画面里的一切。角色占屏与视野开阔是直接对立的两个量，
  40% 是这条曲线上的折中点，不是"两者都要"的解。各景别实测：

  | 景别 | 水平 FOV | 角色占屏 | 原始 |
  | --- | --- | --- | --- |
  | 行进·中景 | 93.8° | 40.2% | 86.8° / 45.4% |
  | 对峙·同框 | 94.9° | 38.6% | 85.6° / 45.5% |
  | 群战·拉远 | 98.2° | 33.2% | 91.5° / 33.7% |
  | 狭窄·收紧 | 100.3° | 43.7% | 92.6° / 50.0% |
  | 决胜·特写 | 91.5° | 53.6% | 80.6° / 64.9% |

  贴墙极限回缩下限 1.6→1.9m：1.6m 处角色占屏 **135%**（整个画面被身体填满），
  是贴墙时"突然极度压抑"的来源
  - **第一人称**：镜头在眼睛高度、脸前方，俯仰自由——低头看见自己的手/脚/剑，抬头看见天空，
    挥剑/踢腿低头即见肢体与兵器在空中运动；只隐藏头部，身体其余可见
  - 底层：转角/位置均临界阻尼平滑无过冲
- **遮挡自动透明**：树木（含无碰撞体的树冠）挡在镜头与玩家之间时自动淡出为半透明，移开即淡回
- **移动**：加减速平滑（速度无突变），摇杆全向 360° 模拟量走跑（极小死区仅滤静止噪声）；
  人形步态叠加脊柱反向扭转/躯干侧倾/骨盆侧摆/落步点头等次要动作，消除机械感
- **后处理**：运行时强制关闭运动模糊/色差/镜头畸变/噪点，暗角强度限幅

### 言语攻防 · 反刍 · 战后复盘（本次迭代）

把方案里最核心的循环——「语言打语言、事实打赖账、边界打索取、不读心打刺激、复盘代替反刍」——落成可玩机制：

- **言语攻防（快速选择式）**：敌人发动心理攻击（凝视/低语）时，屏幕下方弹出**三选一回应**面板
  （一句「最优回击」+ 两句「无效回应」，随机打乱，5 秒倒计时，战斗不暂停用非缩放时间计时）。
  - **选对**：敌人**语塞进入破绽**（削韧，可击破触发大破绽）、对应弱点属性回补、反刍下降，**且不吃这次心理伤害**
  - **选错/沉默**：照常吃心理伤害，并**额外累积反刍**（互骂/退缩/过度辩护只是把自己拖进反刍）
  - 回应库按弱点轴给出最优句（如噪声轴「我听见了，但我不跟随」、边界轴「帮助有边界，这次你自己承担」、
    求职轴「没有回应不代表没有价值，我再来一次」），全部抽象化、服务战斗机制，绝不输出现实操控话术
  - 冷却/恢复模式/已在进行时自动回退为原来的即时落伤害
- **内部言语攻击（脑内回声·本次迭代）**：方案的「内心敌人字幕攻击」落地——
  言语攻防不只来自外部敌人，也来自玩家**自己的脑内回声**。反刍累积到 ≥55%
  （或场上有 `Internal` 类内心敌人逼近且反刍 ≥35%）时，第一人称的负面念头浮出
  （「我是不是根本不行？」「万一又失败了呢？」「明天再开始也一样」），用同一套
  三选一言语攻防回应：**答对**→脑内回声散去、反刍下降、对应属性回补；
  **忽略/答错**→照常吃心理伤害并额外累积反刍（越回避越回放）。反刍越高、内心
  敌人在场，触发越频繁；与外部言语攻防、休养生息答题互斥（同一时刻只弹一个模态）
- **反刍值（方向相反的消耗条）**：越满越糟。被心理攻击命中即累积，选错回应额外增加；
  战斗外缓慢消退避免软锁；上升时紫色暗角脉冲提示
- **行动力 ActionPower（本次迭代·替代"决断"）**：拖延轴攻击/深泥/手机光点消耗它，
  **过低时移速明显下降**（拖延的具象体感）；五分钟火种/火种台/目标石板恢复
- **关系消耗值 RelationshipDrain（本次迭代·第二条反向消耗条）**：被索取/责任转嫁类攻击、
  接下虚假责任时上升；**≥70 时技能冷却 ×1.5**（被掏空的注意力与精力）；
  守住边界的正确回应与边界圈使其回落，战斗外缓慢消退
- **战后复盘（安全屋核心·「复盘」面板）**：把这一战整理成**四栏——事实 / 感受 / 边界 / 行动**
  （按当前章节主题生成，抽象化不复刻真实人物/事件）；**「归档此战」清空反刍值并回补心理属性**——
  用复盘把刺痛转成经验，旧事进入档案不再无限回放
- **失败诊断与失败档案（方案"失败即复盘的起点"落地）**：倒下时不再只有一句通用话——
  按死亡时的**心理数值轴**（意志/专注/自尊/边界/反刍/体力）给出**个性化诊断 + 针对性策略**，
  并归因到**致死心魔**；重复败给同一心魔会**逐步升级引导**（第 3 次起指向「图鉴」破解）。
  诊断作为**复盘种子**在再战后预填「复盘」面板的感受/行动两栏；「档案」面板顶部新增
  **失败洞察**（累计倒下 / 最常败给谁 / 明细）——失败是事实，不是身份，你每次都站了起来
- **失败展览馆·个人化联动**：旧我线「失败展览馆」的展品**动态取自你真实的失败记录**——
  每件展品裱着你败给过的心魔与次数、配可读铭牌；走近展品触发**重构台词**
  （"你败给它 N 次，但一次次站了起来，它没有定义你"）；中央展台**故意空着**
  （失败没资格坐上"定义你是谁"的位置）。无失败记录时退回通用主题展品

### 暂停休养生息答题系统（本次迭代 · 方案 V3.0 第四十五—四十七节）

把方案 V3.0 新增的「三部文档全课程映射答题系统」落成可玩机制——**420 固定题**
（7 章 × 12 概念 × 5 训练形态：原则理解/情境应用/日损辨识/迁移结果/恢复提示），
每题带来源标签（哈佛积极心理学 23 讲 HPP-Lxx / 道德经 81 章 DDJ-xx / 传习录核心工夫 CXL-xxx），
题库内置于 `Resources/Quiz/quiz_bank.json`：

- **触发与暂停**：战斗中**任一正向心理能量低于 40%**（意志/专注/自尊/边界/行动力），
  或**任一负向能量高于 70%**（反刍/关系消耗）→ 立即暂停战斗（timeScale=0，
  位置/镜头/敌人状态原样冻结）进入休养生息答题；面板顶部注明触发原因与当前章节主题
- **抽题（加权不放回）**：从当前主线章节对应的 60 题中抽 5 题——**优先未答题、曾答错题、
  当前失衡能量对应题**（题目 energyTags 与失衡属性匹配时加权）；答题记录本地持久化，
  错题在后续关卡中被优先抽取
- **结算**：每答对 1 题，**所有未满正能量 +20、所有高于 0 的负能量 −20**（各自 Clamp 0—100）；
  **答错不扣能量、不播放羞辱性音效**——只显示依据（正确项为何成立 + 两个干扰项的可命名偏差：
  读心/灾难化/无限准备/过度负责/报复/消极躺平……）与来源标签
- **收尾**：五题结束返回原战斗，并给 **2 秒免受心理伤害保护**；期间不弹言语攻防、
  言语攻防进行中也不抢屏（两个模态系统互斥）
- **安全出口**：「跳过休整」任何时候可离开答题（安全优先，不把玩家锁在面板里）
- **练习模式**：HUD「答题」按钮或安全屋「答题 · 常识提醒与恢复训练」入口随时进入，规则相同；
  面板显示本章掌握进度（已答对 X/60）
- **章节映射**：主线大章一—七对应题库 CH1—CH7；序章（自我怀疑/明日幻影）映射到拖延与目标线 CH3
- **AI 自动命题（方案第四十六节·来源约束升级版）**：HUD「命题审核」面板可开启——
  复用云端 LLM 配置（「AI台词」面板），按**动态题源上下文**（当前章节/代表场景/玩家错题概念/
  当前失衡能量）生成候选题；每题依次经过**结构校验**（恰好 3 个不同选项、单一 correctIndex、
  依据齐全）、**来源校验**（sourceTags 必须落在来源矩阵 HPP-L01—23 / DDJ-01—81 / CXL·SEQ 内，
  凭空来源按 SOURCE_GAP 拒绝）、**语义去重**（对固定题库与已有 AI 题）、**风险分级**；
  **低风险题通过自动校验后立即临时参与抽题**（答题面板标注「AI 临时题·待人工审核」），
  **人工审核只决定能否进入长期正式题库**——通过后永久保留（不受开关影响），否决即删除；
  涉及现实威胁/贫困/病房/创伤/人身安全的**高风险题在人工通过前一律不可用**；
  本章可用 AI 题不足时后台自动补充，全过程写入「日志」面板

### 公平刺痛值 FairnessPain（本次迭代 · 补齐方案「十七、核心数值系统」）

方案核心数值里的**公平刺痛值**此前缺失（公平轴攻击错误地扣到边界值上），现补齐为独立属性：

- **上升**：遇到赖账、毁约、小题大做等不公平攻击（公平轴心念弹/近战）时累积——越高越糟；
  战斗外缓慢平复（比反刍更慢）
- **三档规则**（严格对齐方案）：
  - **0-30 清明区**：记住事实、不被困住——事实系物理输出 ×1.12；
  - **31-70 激愤区**：攻击上升（×1.08），但代价是**反刍累积风险 +50%**；
  - **71-100 失控区**：愤怒让判断下降——物理输出 ×0.9、**一切心理伤害吃得更重（×1.25）**，失控瞬间字幕警示
- **回落**：用事实回应不公（言语攻防答对该轴）主降刺痛并回补边界；**战后复盘归档**一并清零；
  休养生息**答对题目 −20**
- **接入答题系统**：公平刺痛 >70% 计入休养生息触发条件与失衡标签，优先抽「公平与承诺线」
  的 FairnessPain 标签题（题库 60 题）；HUD 属性区新增「刺痛」条（负向，从空开始）

### 能量动态与分级警告（本次迭代）

心理能量不再"只有 HP 在动"——战斗行为本身驱动全部能量条起伏（`MentalDynamics`）：

- **流失（压力面）**：被物理命中→意志/专注下降、反刍上升（偷袭额外打专注）；被击倒→自尊大降；
  **被 ≥2 名敌人围攻**→边界持续流失（人数越多越快）；**战斗中站桩犹豫 3 秒**→行动力流失（拖延具象）；
  **低生命（<30%）**→意志持续流失（生存恐慌）；**反刍过高（>70%）**→专注被持续侵蚀
- **回复（行动面）**：命中敌人→行动力+/自尊微+；击杀→意志/自尊/行动力显著回复；完美闪避→专注回复；
  战斗中的被动回复同步调低——这些起伏在属性条上真实可见
- **分级警告**（`VitalAlertController`）：每项能量两级警告（偏低 <40% 字幕提醒 / 告急 <20% 字幕+警示音，
  带迟滞回差防刷屏）；负向能量反向判定（>70% / >85%）
- **生命垂危决策**：生命 <25% 红色罩屏脉冲随伤势加深；**<15% 弹出「生命垂危」模态弹窗**——
  由玩家决定「休整答题·恢复能量」（进入休养生息答题，答对回能量）或「继续战斗」（30 秒内不再打扰）
- **垂危警告保证送达（事件驱动）**：掉血**穿越垂危线的瞬间**立即触发弹窗（不依赖每帧轮询，
  单次高伤害快速穿越不再漏检）；**濒死守护**——从高于 1 血被一击打穿至 0 时拦下这次死亡、
  留 1 点生命并强制弹出垂危决策（90 秒冷却一次；不是无敌，是最后一次选择的机会）；
  强制弹窗无视「继续战斗」的 30 秒静默期

### 五大主题技能连招化（本次迭代）

「定/收/还/火/盾」从单发效果升级为各自一整套连招演出（多段动作+位移+多段判定+特效），心理机制效果全部保留：

| 键 | 连招 | 演出编排（v2：更长动作链、伤害递增、终结段时缓） |
| --- | --- | --- |
| 定 | 定心·四象归一 | 收势凝神 → 三重内收气环（伤害递增+推离）→ 旋身归一爆发+心神恢复 → 终结「镇岳跳劈」大范围震波 |
| 收 | 收心·万流归元 | 三连旋踢清场（伤害递增+旋进位移）→ 幻影全灭+归元冲击波+专注回收 → 终结「回身斩」+时缓 |
| 还 | 还域·界返五连 | 撩斩挑飞 → 横斩接力 → 旋身反震（责任球全数打回/好人墙震破）→ 弓步突刺 → 界域震地波终结 |
| 火 | 燃火·五段燎原 | 点火解冻 → 三连突进斩（伤害递增、每段突进 2 米）→ 上撩火浪+意势+1 → 终结「烈焰跳劈」落地火环 |
| 盾 | 镜界·退身反击 | 镜环护心 → 后空翻拉开 → 双镜界气刃连发 → 终结「镜返突刺」闪身欺近反击+时缓 |

- **起势保护 + 连招霸体**（大作惯例：技能起手不被抢断）：技能起手 0.65 秒无敌帧，
  保证连招能顺利开出来；其后连招全程霸体——轻击不打断动作且受伤 ×0.6，
  只有击倒级重击才能打断（有代价的豪赌）。此前技能常在起手式上被敌人先手打断、无法发挥
- 连招中被击倒会打断（与蓄力二连击同规则）；判定形状与普攻共用同一形状表

### 锁定与吸附（对齐大型动作游戏惯例·本次迭代）

- **默认手动锁定**（魂系惯例）：按 `Q` / 触屏「锁」按钮锁定最近敌人，再按解除；
  目标死亡或脱离距离自动解除，系统不会擅自换目标；设置面板可切回「自动锁定」
- **攻击吸附（软磁吸）独立开关**：默认开（手动锁定的目标绝对优先——锁谁打谁）；
  设置面板关闭后出招完全按摇杆方向，不吸附任何敌人（完全手操）
- **普攻/技能同一套吸附规则**：技能连招（定/收/还/火/盾）与远程气刃的自动转向
  与普攻共用同一判定——吸附关闭时技能也不自动定位；吸附开启且未锁定时
  只在**正面 90° 锥内**软吸附（任何动作都不会转身去咬背后的敌人）
- 专注值过低时锁定随机丢失的噪声干扰机制，两种模式都保留
- **战斗回正镜头（未锁定）**：手动锁定模式下原地换向——比如转身打背后的敌人——
  镜头也平稳转到玩家身后把新目标框进画面；**三重防抖**（魂系/战神跟随镜头惯例）：
  镜头追【低通滤波后的朝向】而非瞬时朝向；起步需偏差 >55° 且新朝向已**稳定 0.35 秒**
  （摇杆快速搓动、连段磁吸的瞬间换向都不触发）；追到 <12° 停的迟滞开关；
  探索回正同样需朝向稳定 0.22 秒、死区放大到 10°——细碎修正完全不牵动镜头
- **敌人攻击距离上限**：普通敌人远程心念弹 ≤8m、心理攻击 ≤7m——超距只能追近再打，
  禁止隔半张地图狙击/隔空施压
- **敌人远程「凝念能量」预算**（`EnemyRangedEnergy`，对齐大作的敌人资源约束）：
  敌人不能不间断、无限制、零成本地泼洒弹幕——每发心念弹消耗凝念能量（100 能量/每发 18~20，
  一轮 4~5 发即见底），打空后必须停火回气（约数秒），期间头顶弹「凝念枯竭」提示——
  这正是玩家反打的窗口；所有 Boss 弹幕（法槌标签/万眼凝视/引文/追问/追问者）与普通敌人
  远程弹统一走此门禁，并叠加 14m 距离硬上限
- **部位精准伤害**：按判定框真实接触点分部位——头部会心 ×1.35（伤害数字更大更红）、
  腿部伤害 ×0.9 但削韧 ×1.4（扫腿更易打失衡）、躯干标准；配合部位受击反应动画
- **输入仲裁（连按不同键时到底出哪一招）**：
  - **两个都出，先按先出，后按排队**——不会互相覆盖、不会凭空消失
  - **缓冲寿命按意图分两种**：动作锁定期间按下＝明确的"接下一招"意图 → **不过期**，
    活到该动作可被接续为止（上限 2.5 秒）；自由站立时按下＝"现在就做" → 0.6 秒作废
    （此前只有一个 0.6 秒固定窗，而技能锁 1.15–1.45 秒、蓄力二连 0.88–1.0 秒、
    超必杀 1.75 秒、蓄力气场最长 2.05 秒**全都比它长**——那一下必被静默吞掉。
    闪避窗 0.30 秒更糟，连翻滚自身 0.42–0.7 秒都撑不过，连续翻滚必丢键）
  - **收招取消窗对攻击/技能/闪避一视同仁**：长动作打完主要判定即可被打断接续，
    不必干等整个锁走完——定省 0.36s、收 0.51s、还 0.28s、火 0.57s、盾 0.32s、乱舞 0.48s
  - **终结一击的判定一定会打完**：取消窗刻意开在最后一段判定窗之后，
    连打不会把自己的终结段取消掉（此前设在判定还没走完时）
  - 实例：点「重」→0.1 秒后点「拳」，两招都完整打出，衔接点在 0.78 秒
- **操作手感**：闪避随时取消轻连段收招（清序列/收判定框，落地全新起手）；
  转身速度与大角度掉头加速上调——摇杆换向、攻防切换更灵敏跟手

### 威胁感知 + 打击精度校准（本次迭代）

**① 镜头威胁感知：区分「看正脸」与「迎击追兵」**

上一版为了让"面壁转身看正脸"不被绕镜，抑制了朝镜头方向的回正 0.9 秒。
但**「向前突然改为向后」（逃跑/迎击后方追兵）是完全相同的输入模式**——
于是后方跟随的敌人陷入盲区。两者输入相同、意图相反，唯一可靠的区分是
**那个方向上有没有威胁**：

| 情形 | 判定 | 镜头 |
| --- | --- | --- |
| 面壁转身看正脸（附近无敌） | 无威胁 | 抑制 0.6s，看得到正脸 |
| 突然向后（后方有追兵） | **威胁命中** | 门槛降到 0.06s、阈值降到 35°，**约 0.11s 即看清后方** |

缓存附近敌人方位角（0.3s 刷新），每帧判断"我正在转向的方向上有没有敌人"。

**② 打击精度：所见即所中**

「踢中一根很细的柱子很难、方向容易偏、移动太多或太少」——查出两个成因：

- **无目标时的盲目前冲**：打敌人有磁吸（精确停在贴身距离），但打柱子这类
  **非敌人目标没有磁吸**，只剩固定 0.35m 前冲——你明明对准了，一出招人却滑过去。
  普通招式的空挥前冲压到 **0.12m** 以内：**站位由玩家说了算**（突进类指令技保留大位移）
- **瞄准基准漂移**：摇杆方向是**相机相对**的，镜头一边回正、你的"前"就一边漂移，
  连打中每段朝向都偏一点。新增**瞄准锁存**：连段进行中沿用起手那一刻锁存的世界方向，
  除非明显改推别的方向（>35°）才更新——一套连招内瞄准基准保持稳定

### 镜头导演 · 景别语汇（本次迭代 · 从"调参数"到"会选镜"）

此前镜头只有**一种机位在微调**——这正是"学徒"与"摄影指导"的分野：
专家的本事不在调参数，而在**有一套景别词汇表，并知道此刻该用哪一种、用多久、怎么过渡过去**。
新增 `CameraDirector`（景别选择层）：

| 景别 | 触发情境 | 机位语言 |
| --- | --- | --- |
| **行进·标准中景** | 探索无敌 | 基准机位，留出前方引导空间 |
| **对峙·双人同框** | 1–2 名敌人 / 已锁定 | 近水平低机位，取景点偏向双方中点，长焦微压缩 |
| **群战·拉远看局势** | **≥3 名**敌人近身 | 拉远 ×1.24、抬高、略俯 +6°、**广角 +4°**——看清包围态势与空位 |
| **狭窄·收紧贴身** | 场地空间 **<2.6m**（走廊/贴墙） | 收紧 ×0.82、广角 +5°——避免顶墙塌视野 |
| **决胜·推近特写** | 大招 / 处决演出 | 推近 ×0.78、压低、**长焦 −5°**——看清这一击的分量 |

- **优先级**：决胜 > 狭窄 > 群战 > 对峙 > 行进。**狭窄优先于群战**——地方都不够站，
  拉远只会把镜头顶进墙里，这是学徒最容易犯的错
- **过渡即一切**：所有景别参数（距离/高度/俯仰/焦段/同框比例/阻尼）都做**指数插值**，
  镜头是**推轨**过去的而不是切过去的；且切换速率因情境而异——
  进特写果断（4.5/s，要有戏剧性）、出特写从容（2.2/s，不惊扰）、进狭窄要快（3.5/s，
  否则已经顶墙）、其余缓慢（1.6/s，几乎察觉不到）
- **场地感知**：向镜头四周 6 向投射取最短可用距离，识别走廊/贴墙——
  镜头开始"知道自己站在什么地方"
- 敌情与场地扫描 0.3s 节流一次，不做每帧全场遍历

### 与官方/开源参考的参数核对（本次迭代）

对照 [Unity 官方 ThirdPersonController](https://github.com/nrsharip/unity-third-person/blob/main/Assets/StarterAssets/ThirdPersonController/Scripts/ThirdPersonController.cs)
与 [UE SpringArm 官方示例](https://dev.epicgames.com/documentation/unreal-engine/using-spring-arm-components?application_version=4.27)
逐项归一化核对，结论是**本项目数值本就在标准区间内**：

| 项目 | 本项目 | 官方参考 | 判定 |
| --- | --- | --- | --- |
| 跑速 | 5.2 m/s | Unity 5.335 m/s | 几乎一致 |
| 走速 | 2.6 m/s | Unity 2.0 m/s | 同量级 |
| 重力 | −20 | Unity −15 | 略重（动作向） |
| 角色转向时间常数 | 0.071s | Unity RotationSmoothTime 0.12s | 更跟手（战斗向） |
| 土狼时间 | 0.12s | Unity FallTimeout 0.15s | 同量级 |
| 镜头吊杆长度 | 4.76 m | UE SpringArm 3–4 m | 更长＝视野更开阔 |
| 镜头位置跟随 | 0.09s | UE CameraLagSpeed 3.0 → 0.33s | 远比 UE 跟手 |
| 视野 | 垂直 56°（@16:9 ≈ 水平 87°） | UE 第三人称模板 水平 90° | 不算窄 |

**这一核对本身就是结论**：此前所有卡顿/盲区/不连贯问题，根因都是**结构性缺陷**
（体力静默中断连段、互斥分支漏掉中幅转向、消费式输入互抢、镜头意图模型错误），
而不是数值不对——所以"照抄别家参数"解决不了，必须逐个定位逻辑。

**唯一有实据可改进的一处已采纳**：Unity 官方用**指数逼近**做加减速
（其注释原文：*curved result rather than a linear one giving a more organic speed change*），
本项目原为线性匀加速（`MoveTowards`）。已改为 `1−e^(−k·dt)` 指数逼近（帧率无关）：
起步 k=20（0.05s 到 63%、0.15s 到 95%）、刹车 k=26——起步瞬间给足冲量更跟手，
末段自然收敛不突兀。

### 镜头意图识别（本次迭代 · 「面壁转身看正脸却被甩向墙面」）

实测反馈：**面前是墙、用摇杆转身想看清角色的脸，镜头却立刻绕开去看墙面。**
这不是参数问题——是镜头的**意图模型**错了：它假设「玩家永远想看角色的正前方」，
于是把一切转身都读作"要换行进方向"。补上两层意图识别：

**① 区分「转身看一眼」与「真要往那个方向去」**

角色朝镜头走来（偏差≈180°）时，此前一律绕到背后。改为按**是否持续行进**判定：

| 玩家行为 | 镜头 |
| --- | --- |
| 短暂转身看正脸 / 贴墙后退调整站位 | **岿然不动**——看得到正脸，视野不塌 |
| 持续朝镜头方向跑 **>0.9s** | 缓缓绕过去——角色不会跑出画面 |
| 战斗中转身打背后的敌人 | **豁免**，仍快速追击（看见敌人优先） |

**② 不把镜头甩进墙里**

大幅回正前先**探测目标方位的空间**（与主碰撞同一套过滤规则）：越憋屈转得越慢
（最低降到 **25%**）。面壁转身时"绕到角色背后"恰好是墙的方向，硬绕过去只会让镜头
顶在墙上被迫贴脸、视野瞬间塌掉——现在会把镜头留在看得见的地方。

**③ 手动取景保护 0.3s → 1.2s**：玩家刚手动摆好角度，镜头 0.3 秒后就自己转回去，
读作"和我抢镜头"；1.2s 让手动取景有足够停留时间。

### 镜头同步跟随统一化（转向尺度 → 镜头幅度）

> ⚠️ 本节的两条判据已被下方「战斗跟随信号」一节修正：
> 「正在推杆也计入主动意图」与「威胁方向挂在朝向上」在**战斗中**是抖动的直接来源。
> 保留此节记录当时的取舍。

目标：**转向/移动幅度越大，镜头同步调整越快；任何情形下都不留盲区。**
核查发现此前的实现还有两个覆盖缺口：

- **战斗中 12°–55° 的中幅转向完全没有跟随**——战斗与探索是互斥的 `if / else if`
  两条分支，`InCombat` 为真时只走战斗分支（需 >55° 才启动），中等幅度转向被两边都漏掉，
  这是战斗中长期存在的**部分盲区**
- **技能/出招期间推杆转向不跟随**——出招时角色被"定步"锁住只转不移动，
  `moveSpeed≈0` 被判定为静止，回正条件不成立

改为**一条统一的渐进跟随曲线**（无分档断层）：

| 情形 | 行为 |
| --- | --- |
| 中小幅转向（>10°）+ 有移动**或推杆** | 温和跟随，偏差越大越快 |
| 大幅换向（战斗 >55° / 探索 >45°） | 迟滞开关锁定追击，直到 <12° 才松开 |
| 站立不动且未推杆 | 完全不动（无漂移） |
| 手动转镜期间 | 一律让位给玩家 |

- 角速度按偏差**连续映射**：平滑时间 0.5s→0.15s、转速上限 **70→340°/s**——
  小幅修正慢而稳（防抖），大角度掉头迅速跟上（防盲区），两者不再互相牺牲
- **「正在推杆」也计入主动意图**：覆盖出招/技能中的转向，角色转到哪镜头跟到哪
- **视野开阔化**：疾跑与大幅转向各给一点点拉远（合计 ≤ +12%，变焦极慢）——
  转身掉头的瞬间正是最需要看清周围的时刻，给"要去的方向"留出可见余量

### 战斗跟随信号：朝向 → 行进方向（本次迭代 · 「摇杆走位出招时镜头频繁抖动」）

实测反馈：**摇杆一边走位一边和敌人出招时，方向稍有变动镜头就频繁抖动、非常敏感**；
而黑神话悟空这类游戏没有这个现象——它们的方向操作与镜头操作是**分开**的。

前几轮已经把稳定设施做得相当完整（低通、渐进软区、角加速度限幅、绕行限速、一键回正），
但这一条症状它们一条都治不了，因为**问题不在滤波器，在喂给滤波器的信号**。

**根因：未锁定的战斗，镜头追的是「角色朝向」。**

锁定是**手动**的（`LockOnSystem.AutoAcquire` 默认关，对齐魂系惯例），
所以绝大多数近身互殴 `lockTarget == null`，走的是**探索分支**——而探索分支追
`_headingAvg`（角色朝向）。战斗中角色朝向恰恰是全场最跳的量：

| 来源 | 朝向变化 | 是否触发 220°/s 的稳定计时清零 |
| --- | --- | --- |
| 出招磁吸 `ApplyAttackFacing` | 每一击**瞬间**拧向敌人 | 单帧瞬转，之后立刻恢复累计 |
| 轻击连段推杆转向 | 150°/s | **否**（低于阈值，一路畅通） |
| 技能/绝招 `SteerDuringAction` | 85~110°/s | **否** |
| 翻滚起手 | 直接硬赋值 | 单帧 |

再加上 `threatAhead = ThreatNear(_headingAvg, 70°)` 在交战中**几乎恒真**
（只要你面朝正在打的那个敌人就成立），它把启动门槛压到 `holdNeed 0.06s`、
`bigAngle 35°`，迟滞开关全程锁死。于是摇杆的每一次细碎方向调整都经由
朝向被忠实地送进镜头——这就是"很敏感、频繁抖动"。

**改法：交战中改追「真实行进方向」。**

行进方向（低通后的水平速度方向）对上述四种朝向跳变**天然免疫**——
站着挥拳时它压根不存在。而探索时朝向 ≡ 行进方向，既有的全部调参与实测结论不受影响。

**再进一步：追错了东西——战斗中该框住的是【敌人】。**

换成行进方向后抖动大幅收敛，但**残余是几何上必然的**：移动是镜头相对的 ⇒
`行进方向 = 镜头偏航 + 摇杆角` ⇒ **偏差 err ≡ 摇杆离轴角 θ**。
追一个恒等于 θ 的目标**永远不会收敛**——只要摇杆偏着推，镜头就一直以绕行限速
（30°/s）缓缓爬。宏观上不是"抖"，但读作**"镜头永远不安定、很敏感"**。

于是先试了"干脆全关"——**结果把镜头的首要职责也扔了：打着打着敌人跑出画面、
不知道在哪**。这一步是过度修正，随即撤回。

复盘下来，两次弯路的共同点是**追的都是 `C+θ`**：无论"角色朝向"还是"行进方向"，
它们都是镜头偏航的函数，构成代数环，既抖又不收敛。而战斗中该框住的从来是**敌人**——
**敌人方位不是镜头偏航的函数，没有代数环，追它天然收敛。**

**追法：取景窗（camera window），而不是伺服到画面中央。**

| 敌人屏幕角 | 镜头 |
| --- | --- |
| 落在窗内（水平半视野的 58%，手机上 ±30°） | **完全不动** |
| 出窗 | 只把它**推回窗沿**，到位即停——不追到中央 |

- **为什么不追到中央**：那意味着镜头必须跟着敌人的每一步走，正是要根治的
  "永远在轻微动"。推回窗沿则"一到位就停"，静止是常态，动一定是因为真的要出画

**这里第一版把窗宽写死成 34°，结果整套逻辑是个空转**——照 FOV 半宽 47° 估的，
却漏掉了**镜头在玩家身后 4.6m 造成的向心压缩**：玩家身边的东西在画面上天然靠中间。

| 敌人距玩家 | 屏幕水平角**上限** |
| --- | --- |
| 1.5m | 19.0° |
| 2.0m | 25.8° |
| 2.5m | 32.9° |
| 3.0m | 40.7° |

近身缠斗（1.5~2.5m）**永远达不到 34°**，那段代码每帧都判"在窗内"而什么也不做。
现在窗宽由 `Camera.fieldOfView + aspect` 精确解出水平半视野再取 58%（16:9 → ±27°、
19.5:9 → ±30°），换 FOV／换屏幕比例都自动跟着走。

其余实现要点：

- **屏幕角用镜头【真实的 forward】量**（`Vector3.SignedAngle`），不从 `_curYaw` 反推：
  肩后构图的横向偏移让实际视线偏离 `_curYaw` 约 5.6°，反推会让取景窗左右不对称
  （24° vs 36°）；`SignedAngle` 还天然表示"敌人跑到镜头背后"（|角|>90°）
- **低通滤【敌人位置】而不是屏幕角**：屏幕角里同时含有镜头自身的转动，滤它等于把
  本回路的反馈也延迟 τ≈0.31s，与伺服时间常数同量级，必然过冲振荡
- 目标切换有迟滞：当前敌人还活着且在 16m 内就继续咬住，绝不因为"另一个刚好近了
  0.2m"就换人——换目标＝一次大幅摆镜
- **撤离豁免**：持续朝背离敌人的方向全速跑时框敌让位给探索跟随——
  否则镜头会扭回去盯着你正在逃离的敌人，你就变成对着屏幕底部往里跑

**在这之上，一键回正正式接进自动运镜（原一键回正保留）。**

### 什么时候回正、什么时候绝不回正

判据只有一条，战斗与探索**共用同一条**：

> **你接下来要面对的东西，现在看得见吗？**
> 战斗看正在打的**敌人**；探索看角色**前方 6m 的「视察点」**。都用同一个取景窗量。
> 看得见 → 镜头一动不动。看不见 → 需要回正。

用"视察点在不在窗内"而不是"朝向转了多少度"，是因为前者自动把**镜头在身后 4.6m
造成的向心压缩**算进去了。代入几何解出的实际阈值：

| 角色转向 | 视察点屏幕角 | 判定 |
| --- | --- | --- |
| 15° | 8.5° | 窗内 → **不动镜** |
| 30° | 17.0° | 窗内 → **不动镜** |
| 45° | 25.6° | 窗内 → **不动镜** |
| 55° | 31.4° | 出窗 → 需要回正 |
| 90° | 52.5° | 出窗 → 需要回正 |
| 180°（从前转朝后） | 180° | 出窗 → 需要回正 |

也就是说：**±50° 以内的转向、走位、闪避、出招转向一律不动镜**——那些方向你本来
就看得见，动了就是白抖；**超过约 55° 才回正**——那时你确实什么都看不到。
阈值是从几何解出来的，不是拍的。

**「想回正」与「能回正」是分开的两件事**：

- **想**：视察点／敌人出窗持续 0.2s（滤掉一闪而过）
- **能**：**摇杆松开**（≥0.07s），或摇杆已推回接近画面正前（离轴 <35°）

"能"这一条是**几何硬要求**，不是保守：移动是镜头相对的 ⇒ `行进方向 = 镜头偏航 + 摇杆角 θ`。
推着离轴 θ 时，把镜头转到行进方向背后**无解**——转多快都一样，只会让角色画更紧的圈
（实测把上限提到 340°/s，偏离角只从 87.9° 缩到 75.2°，自转却涨到 207°/s）。
更糟的是回正一结束、参考系解冻的那一刻，摇杆的世界方向会整体旋转 θ 度，
玩家会被"掉头回去"。松杆时环消失，回正才既收敛又无副作用。

所以：**只"想"不"能"时**，由探索伺服以 30°/s 走缓弧、配合引导留白与转向拉远撑住视野，
**玩家松杆的那一瞬间立刻兑现**（0.07s 阈值——掉头时拇指扫过摇杆中心很快，抓得紧一点）。

完整的决策表：

### 镜头返工定稿：撤回一整轮加法，只留能算得清的那几条（本次迭代）

上一版把跟随重写成「先判情景、再配行为」的分派器（`CamIntent`：静止/巡航/盲区/
近身/取景/锁定/演出），只有落进「盲区」这一个情景才跟随。反馈是
**「没有效果，而且好像别的地方也被改坏了」**——两条都成立，原因也都能算出来：

**① 跟随再也停不下来（这是"被改坏"的那一半）。**
跟随的偏差恒等于摇杆离轴角 θ（`H = C + θ ⇒ H − C ≡ θ`），而 θ **不随镜头转动而改变**：
手指按着不动，镜头转多少度，θ 还是那么大。于是"盲区"这个判据一旦成立就再也不解除，
镜头以 75°/s **一直转下去**——按 5.2m/s 的跑速换算，角色在半径 4.0m 的圈里绕，
画面每 4.8 秒转满一圈，直到玩家松手为止。这正是"摇杆四个方向不停转动、
整个屏幕也跟着不停转"。
而被删掉的 `SustainedOrbitCap`（30°/s，半径 9.9m）本来正是压这个的——**删掉它是纯粹的倒退**。

**② 起手要等 0.5 秒（这是"没有效果"的那一半）。**
落进「盲区」要同时满足：持续位移 0.35s + 拇指角稳定 0.22s + 前视点出取景窗
（按镜头在身后 4.6m、前视点 4.68m 的几何反算，需要转过 **73°** 才够），
再加速率爬升 0.2~0.32s。跑动中随手一个 60° 转向，判据根本不成立；
够成立的大转向，也要半秒后镜头才开始动。

**结论：那一整套是错的方向，已全部撤回**，回到 `9d0e603` 的连续跟随（它是收敛的、
有绕行限速的、参数经过实测的），只在其上叠加**能单独算清收益**的几条：

| 保留 | 为什么它是对的（可验算） |
| --- | --- |
| **时钟统一 `Time.deltaTime`** | 每次命中都顿帧（`HitStop`：timeScale=0.08，0.035~0.12s）。原先镜头跑无缩放时间 ⇒ 世界冻住而镜头照常收敛，且 `moveSpeed = 位移(8%)/dt(100%)` 每拳塌一次，引导留白随之单帧跳最多 0.45m——与出招同频的一路抖动 |
| **`_combatBlend` 交战程度** | 此前稳镜只认「是否锁定」，而锁定默认手动开 ⇒ 绝大多数近身互殴一直吃"探索档"的跟手参数。改为锁定/出招中/身边有敌人在打三者取或 |
| **战斗中死区抬高 10°→30°、门槛 45°→62°、转速 ×0.55** | 转速 = 增益 × 偏差，而偏差 ≡ θ ⇒ **拇指的抖动被一比一翻译成镜头角速度**。虚拟摇杆拇指本就在 ±10~20° 内游走，抬高死区让这段噪声整个落进去。加平滑没用——平滑只改相位，不改幅频比 |
| **位置侧同档加重** | 焦点死区 0.03→0.15m、跟随时间常数 0.09→0.24s、引导留白上限 2.2→0.5m（近身走位是短促往复，留白只会把焦点前后甩 2.2m）；另加 6cm 纵向死区（`CharacterController` 贴地/台阶的高频跳动） |
| **出画兜底** | 稳定压倒一切，但"看不见敌人"不是稳定，是失职。8m 内正在打你的敌人掉出取景窗并持续 0.28s ⇒ 恢复全速把它推回**窗沿**（不是画面中央——推到中央意味着镜头得跟着敌人每一步走） |
| **取景窗按真实相机几何算** | 曾写死 34°。而镜头在身后 4.6m 时，敌人在 1.5/2.0/2.5/3.0m 处的屏幕角**上限**只有 19.0°/25.8°/32.9°/40.7° ⇒ 那条逻辑在近身时**几何上永远不触发**。改用 `atan(tan(vFOV/2)·aspect)`：62°@19.5:9 ⇒ 半宽 52.5° |
| **自动回正并入自动运镜**（一键回正保持不变） | 见下 |
| **回正时长按角加速度预算反解** | 见下 |

#### 自动回正：为什么时机是「松杆的那一刻」

`H = C + θ` 的最后一个推论：镜头对着角色正前方要求 `C = H`，联立得 **θ = 0**。
即**只要摇杆推在非正前方向，镜头在几何上就不可能对正**，与转速、与聚焦在哪都无关。
所以连续伺服永远追不平；而**松开摇杆的那一刻环就消失了**，那才是把镜头一次性摆正的
正确时机——也正好是玩家跑完一段、在换气的节拍，运镜发生在这里最不打扰。

四重闸门（少一条就会变成"每隔一两秒摆一次"）：

| 闸门 | 值 | 挡掉的是 |
| --- | --- | --- |
| 松杆时长 | > 0.12s | 搓杆过中心的瞬间 |
| 需求持续 | > 0.2s | 一帧的抖动 |
| 幅度 | > 25°（交战 > 55°） | 不值得动镜的小偏差 |
| 「移动刚结束」窗口 | 站定 < 1.2s | **玩家特意让镜头对着角色的脸站着**——那是他摆的机位，镜头一动都不该动 |
| 间隔 | > 2.2s（交战 > 5s） | 交战中松杆极频繁（出招/格挡/闪避之间都会松） |

#### 回正为什么"转得太快、让人头晕"

晕动的驱动量是角**加速度**，不是角速度；公认舒适上限约 **600°/s²**。
`SmoothStep` 走完 Δ 度用时 T ⇒ 峰值角加速度 = `6Δ/T²`。原先固定 0.35s：

| 回正幅度 | 固定 0.35s 的峰值角加速度 | 按预算反解后 |
| --- | --- | --- |
| 90° | 4408°/s² | 一键 0.62s / 自动 1.04s |
| 180° | **8816°/s²**（舒适上限的 14 倍） | 一键 0.88s / 自动 1.47s |

改为 `T = sqrt(6Δ / 预算)`：自动回正 500°/s²（镜头自己决定要动，最保守），
一键回正 1400°/s²（玩家自己按的，自发运动耐受度高，但仍只有原先的六分之一）。

#### 这一轮真正学到的

- **一个不收敛的回路，调多少参数都没用。** 前面几轮反复在"速率、平滑、门槛"上加东西，
  而 `H − C ≡ θ` 决定了误差压根不随镜头转动而减小。看清这一点之前，所有调参都是白做。
- **判据越"聪明"，越容易一条都不成立。** 三个串联条件各自都合理，连乘之后成了
  "跑动中随手转向永远不触发"。加判据必须验算它的**联合触发率**，不能只看单条是否合理。
- **删掉一条压制性的限速时，必须先问它在压什么。** `SustainedOrbitCap` 看起来只是
  "限制绕行速度"，实际是这个不收敛回路唯一的兜底。


### 视野开阔度裁决运镜快慢（本次迭代 · 「看得见就慢慢跟，看不见才赶紧转」）

反馈：跑动中转向**不必立刻决定切换或回正**——要看当前视野可见范围。
**开阔、能看见远方一定距离 ⇒ 后续平稳跟随即可；视野受限、看不清远方陷入盲区 ⇒
才需要快速调整到目标方向**。并且应当**尽量不让盲区发生**，始终保持前方视野开阔，
这样也就避免了镜头突然快速转动导致的头晕。另外只要视野开阔，
就不该这么频繁地自动回正。

这条反馈把"该不该动镜"的判据从**几何**（镜头在不在背后、转了多少度）
换成了**功能**（看不看得见要去的地方）——后者才是玩家真正在意的东西。
转向本身从来不是动镜的理由。

#### 通视距离：一条平射探测，两个方位

新增 `SightDistance(yaw)`：从取景点沿某方位平射（球扫，半径 0.35m，上限 26m），
返回多远才被挡住。与既有的 `FreeBoomDistance` 分工明确——
那条往**吊杆要退去的方向**探（会不会顶墙），这条往**要看/要去的方向**平探。
忽略触发器、动态碎屑、玩家与敌人本体（敌人挡一下反而是你正想看的东西）。
两次探测都做 4/s 低通，且改用 `SphereCastNonAlloc`（每帧两次，不能有 GC）。

#### 紧迫度 `_urgency`：三条来源取最大

| 来源 | 度量 | 挡的是 |
| --- | --- | --- |
| ① 当前画面闭塞 | 沿**画面正前**的通视距离，18m→0、6m→1 | 走廊、贴墙、拐角里看不到东西 |
| ② 要去的方向快撞上 | **剩余时间** `通视/速度`，3.0s→0、0.9s→1 | 跑向墙面/尽头 |
| ③ 要去的方向不在画面里 | 前视点屏幕偏角，0.65→0.95 倍半视野 | 开阔地**朝镜头跑**：通视很长却完全看不见路 |

第 ③ 条不能省——只看开阔度会漏掉最典型的那种盲区。
站定时（`moveSpeed < 0.6`）②③ 一律不成立：**站着不存在"前方"**，
否则"转身看正脸"会被读成"前方 180° 全是盲区"，镜头随即绕走。

`_urgency` 本身走 0.5s 临界阻尼——它调制着转速，若阶跃，转速就跟着阶跃，
又变回"猛地开始转、猛地停住"。

#### 关键：用【剩余时间】而不是【剩余距离】，盲区就不会发生

5.2 m/s 的跑速下 `TtiRelaxed = 3s` 对应 **15.6m**——
也就是墙还在十几米外，镜头就已经不紧不慢地开始转了，等你真跑到墙边早已对好。

**这才是"避免镜头突然快速转动导致头晕"的正解：不是把快的那一下调慢，
而是让它根本不需要发生。**

#### `_urgency` 接到哪几处

| 参数 | 开阔（→0） | 盲区（→1） |
| --- | --- | --- |
| 跟随死区 | **28°**（斜着跑也不动镜） | 10°（细碎修正也跟） |
| 转速系数 | ×0.45 | ×1.0 |
| 大幅换向追击门槛 | 70° | 45° |
| 稳定确认时长 | ×2.2（0.33s 才起手） | ×1.0（立刻起手） |

交战中另有一套并联（稳定第一）：死区固定 30°、转速 ×0.55——
战斗里"看得见敌人"由出画兜底负责，不靠通视距离。

#### 「停下来之后自动回正」已整条撤销

反馈：跑动/移动停止后镜头还会自动回正，有必要吗？——**没有，而且成熟大作里
一款都没有这个设计**：

| 作品 | 未锁定时的回正 |
| --- | --- |
| 魂系 / 只狼 / 艾尔登法环 / 黑神话悟空 | 只在**移动时**缓缓跟到身后；一停就停在原处。回正是**显式按键**（右摇杆按下 / 锁定键） |
| 战神 (2018 / 诸神黄昏) | 从不自动回正，越肩镜头只跟随移动方向 |
| 地平线 / 刺客信条 / 对马岛之魂 | 移动时温和跟随，停步不触发任何运镜；有独立的"居中镜头"键 |
| 塞尔达 旷野 / 王泪 | ZL / X 是玩家自己按的复位，停步不触发 |
| 鬼泣 5 | 混合机位 + R3 居中键 |

道理也站得住：**停下来恰恰是玩家想看看四周、看看自己角色的时刻**，
这时候把镜头摆走是最不合时宜的一次运镜；而且它无法预测——玩家没做任何动作
画面却动了，那正是"镜头有自己的想法"这种糟糕观感的来源。

功能上它也已经没有必要：连续跟随本来就在你【移动时】把镜头带到身后，
盲区时还会自动提速；停下之后即使构图偏一点，按开阔度原则——
只要看得见远方，那就不是问题。下一次起跑时跟随立刻接管，自己就正回来了。

**保留一键回正**（双击右半屏 / 桌面 `V`）：那是这个类型的通行解，
玩家自己按的运动不引起晕动。它的时长仍按角加速度预算反解（1400°/s²，
180° 用 0.88s、90° 用 0.62s），不再是会把人转晕的固定 0.35s。

#### 停下来还是会转（第三处，也是幅度最大的一处）

前两处（自动回正动作、追击锁存）都修掉之后，实测仍会转 —— 因为**"停了没有"
这个判断本身晚了 0.7 秒**：

| 环节 | 延迟 |
| --- | --- |
| 角色实际停下（`decelRate 26` ⇒ τ=0.038s） | 0.12 s |
| `moveSpeed` 是 8/s 低通均速，从 5.2 掉到 0.6 的门槛 | **0.27 s** |
| 跟随驱动 `_followDrive` 再滑停（0.3s 临界阻尼） | +0.45 s |
| **合计** | **0.72 s** |

开阔地 90° 偏差下峰值 89°/s，这 0.72 秒扫过约 **40°**——玩家早就松手站住了，
画面还在慢慢转。三条一起改：

1. **判据换成"手指还在不在摇杆上"**。移动完全由摇杆驱动，手指一离开就是
   "我不去了"，**这个信号零延迟**，不需要等任何滤波追上来。位移与速度都要过滤波，
   只有这件事是当帧就知道的。（门槛取 0.1 而非移动逻辑的 0.2：轻推慢走经死区
   重映射与 1.3 次曲线后只剩 0.12，用 0.2 会把"走"整个判成没推杆。）
2. **起手柔、收手利落**：驱动的时间常数拆开，起 0.3s（防抖）、收 0.14s（防拖尾）。
   起步慢是防抖、停得慢是拖尾，两者诉求相反，本就不该共用一个常数。
   下游的角加速度限幅本来也是【只限提速不限减速】，同一个道理。
3. **升速曲线也吃视野系数**。"偏差越大转越快"（最高 4×）是为了压**盲区时长**而设的，
   而开阔地根本没有盲区可压——那里 90° 偏差也照样一览无余。
   开阔时升速倍率压到 1.4×，盲区仍是 4×。

| | 改前 | 改后 |
| --- | --- | --- |
| 开阔地 90° 偏差的跟随速度 | 89 °/s | **42 °/s** |
| 盲区 90° 偏差的跟随速度 | 199 °/s | 236 °/s（不打折） |
| 松杆后继续转动 | 0.72 s / **40°** | 0.34 s / **9°** |

9° 是伺服自然的减速尾巴，读作"镜头稳稳停住"；40° 才会读作"镜头自己决定要转"。

#### 修掉「停下来镜头还在自己回正」的真正来源

上一版删掉的是**自动回正动作**，但停下来仍会转——来源是另一处：
大幅换向的追击锁存 `_combatReorient`，它的解除条件只有"偏差追到 <0.6×死区"。
跑动中转向会把这个锁存打开，而代数环让偏差恒等于摇杆离轴角、**根本追不下去**；
于是你一停，锁存还在，镜头就自顾自地继续绕到你背后——
**玩家没做任何动作，画面却在动**，这正是最坏的那种观感。

改法两条：
- 解除条件加 `!active`：**跟随的前提永远是"你正在往某处去"，停了就该停。**
- 跟随的驱动强度 `_followDrive` 改走 0.3s 临界阻尼，而不是布尔开关——
  直接关伺服会让 `_yaw` 在有速度的那一帧硬停（速度台阶＝无限 jerk，读作"卡一下"）。
  现在镜头是**自己滑停**的。

#### 对镜头无感知的停止：判据换成【未经平滑的手指状态】

上一版已经把判据从"低通均速"换成"摇杆有没有推"，但仍留了两段延迟：
`StickWorldDir` 来自**平滑后**的摇杆量（力度 SmoothDamp、松手 τ≈0.03s），
落到阈值以下要 ~0.07s；再加 80ms 宽限。合计 0.12s 全速转动。

这次直接引一条**零平滑的原始通道**：`MobileInput.MoveHeld`
（虚拟摇杆在 `OnPointerDown` / `OnPointerUp` 里直接置位）＋桌面端的
`Input.GetAxisRaw`，汇总成 `PlayerController.StickHeld`。
手指离开的**那一帧**镜头就不再往前转了。

收手的时间常数再压到 **0.06s（约 4 帧）**。这不会造成"卡一下"——
`_yaw` 停住之后，渲染用的 `_curYaw` 还要经 `rotationSmoothTime`(0.11s) 的临界阻尼
追上来，那几度收尾是**纯粹的减速曲线**，不是一次运镜决定。

| 版本 | 松杆后继续转动 |
| --- | --- |
| 最初（低通均速 0.27s + 滑停 0.3s，89°/s） | **58°** |
| 上一版（0.12s + 0.14s，42°/s） | 15° |
| 本版（0 延迟 + 0.06s，42°/s） | **6.8°**（其中 4.6° 是渲染平滑的减速尾巴） |

唯一还会明显收尾的情形是**转向到一半就松手**（那时镜头正以 170°/s 运动，
物理上必须减速，约 27°）——但那是玩家自己刚要求的运动被中途取消，
而且硬停 170°/s 才是真正的晕动源。

### 提速倍率随转向幅度给：掉头只走一小段弧就摆正（本次迭代）

诉求：大幅转向（比如从朝前直接拉到朝后）应该**只走一小段弧线就立刻把镜头调正**。

之前瞬态提速是**固定 3.5 倍**，也就是不论转 45° 还是 180° 都用同一个峰值 75°/s。
而这笔账**随幅度急剧恶化**——180° 掉头走的是半圆，横向绕出是 **2R**：

| 绕行速率 | 半径 | 180° 耗时 | 横向绕出 (2R) |
| --- | --- | --- | --- |
| 75 °/s（改前） | 4.0 m | **2.40 s** | **7.9 m** |
| 120 °/s | 2.5 m | 1.50 s | 5.0 m |
| 203 °/s | 1.5 m | 0.89 s | 3.0 m |

7.9 米、2.4 秒——正是"走了很大一段弧才摆正"。而小转向本来就没多少盲区，
慢一点毫无代价。**同一个倍率服务两端，必然一头过快一头过慢。**

改为按幅度给（在瞬态起手那一刻**锁存** `|θ|`）：

| 转向幅度 | 倍率 | 峰值速率 | 提速峰值角加速 | 对齐耗时 | 横向绕出 |
| --- | --- | --- | --- | --- | --- |
| 45° | 2.5× | 33 °/s | 106 °/s² | 0.77 s | 2.6 m |
| 90° | 5.4× | 94 °/s | 165 °/s² | 1.15 s | 3.2 m |
| **180°** | **10×** | **203 °/s** | **401 °/s²** | **1.27 s** | **2.9 m** |

180° 掉头由 2.98 s / 7.9 m 变成 1.27 s / 2.9 m，而提速侧峰值角加速度 401°/s²
仍在舒适上限 600 之内（减速侧不限——下游限幅本就只限提速，拦着镜头停下只会拖尾）。

**幅度必须锁存，不能用瞬时偏差**：偏差一降倍率就塌，大幅掉头会在半途失速——
那正是上一版 180° 要花 3 秒的原因。锁存量用拇指角 `|θ|`（与镜头无关），
瞬态包络 1.2 s 内回落，长按不放依旧回到 26°/s 的稳态缓行，不会变成转圈。

### 连续换向也要逐步摆正：把"稳定计时器"换成"方向置信度"（本次迭代）

诉求：转向时**不松开摇杆连续切换**方向的情形下，允许角色跑一段弧线，
但镜头要**持续在逐步摆正**。

先看现状为什么它反而完全不动：跟随要求 `_headingHoldT > steadyNeed`，
而 `_headingHoldT` 在朝向角速度 >220°/s 时被清零。
连续换向时角色不停急转 ⇒ 计时反复清零 ⇒ **跟随一次都不成立、镜头一动不动**——
恰恰在最该慢慢摆正的时候。

#### 一个量同时回答两个问题

把朝向当**单位向量**做低通（τ≈0.45s），均值的**模长天然就是置信度**：

| 输入模式 | 均值模长 | 可用速率 |
| --- | --- | --- |
| 按住一个方向不动 | 1.00 | 100% |
| 两个方向来回切（每 0.5s） | 0.79 | 80% |
| 三方向连续切（每 0.4s） | 0.64 | 30% |
| 搓杆 0.5 圈/秒 | 0.57 | 7% |
| 搓杆 1 圈/秒 | 0.33 | **0%** |

它一个量同时给出了「平均朝哪」（均值的辐角）与「该不该跟」（模长），
于是**"连续换向要逐步摆正"与"搓杆不许动镜"由同一个量分开**，
不再需要一个会被急转清零的计时器。

跟随目标也从瞬时低通朝向 `_headingAvg` 换成这个**均值方向** `meanYaw`——
连续换向时它就是那一串方向的中分线，镜头朝它缓缓摆正，角色因此走出一段弧线，
正是想要的行为。

四秒内实测的镜头摆正量（不含起始瞬态提速）：

| 输入模式 | 4 秒摆正 |
| --- | --- |
| 按住一个方向不放 | 100° |
| 两方向来回切 | 67° |
| 三方向连续切 | 57° |
| 搓杆 0.5 圈/秒 | 20° |
| 搓杆 1 圈/秒 | **5°** |

### 一个算错了的直觉：慢不等于弧小，只等于绕得远（本次迭代 · 再提速）

反馈：镜头切换还是太晚，几乎到终点才回到前方。

重算之后发现前面那个"慢 = 弧缓"的直觉是错的。对齐一次 90° 转向，
角色走的是半径 `R = v/ω` 的圆弧、**总共转过 90°**，
所以绝对偏离 `= R(1−cos90°) = R`：

| 绕行速率 | 半径 | 对齐耗时 | 弧长 | **横向绕出** |
| --- | --- | --- | --- | --- |
| 26 °/s | 11.5 m | 3.46 s | 18.0 m | **11.5 m** |
| 45 °/s | 6.6 m | 2.00 s | 10.4 m | 6.6 m |
| 60 °/s | 5.0 m | 1.50 s | 7.8 m | 5.0 m |
| 90 °/s | 3.3 m | 1.00 s | 5.2 m | **3.3 m** |

**慢并不等于"弧小"，只等于"绕得久、绕得远"。**26°/s 那一档要绕出 11.5 米、
跑 3.5 秒——那才是"大弧线"的真身。而真正的约束只有角加速度（晕动），
90°/s 配 0.45s 的平滑升起也只有约 120°/s²，舒适上限是 600，离得很远。

于是起始瞬态的提速倍率 2 → **3.5**：

| | 值 |
| --- | --- |
| 绕行峰值速率 | **75 °/s** |
| 峰值角加速度 | **120 °/s²**（舒适上限 600） |
| 90° 对齐耗时 | **1.62 s**（上一版 2.60s、再上一版 3.92s） |
| 整段横向绕出 | **约 4 m**（26°/s 恒定那一档是 11.5 m） |

**更快、更直、更早看见前方——三项同时改善**，因为之前是在错误的方向上做取舍。
转完 1.2 秒内回落到 26°/s 的稳态缓行，长按不放仍然不会变成转圈。

#### 顺带修掉一个同款的正反馈

瞬态包络原先由**朝向角速度** `_headingRateAvg` 驱动，而它含镜头自己转动的份额：

    镜头转 ⇒ 角色被带转 ⇒ 朝向角速度升高 ⇒ 提速 ⇒ 转更快 ⇒ …

在 2 倍时还不明显，放到 3.5 倍就会变成**又一次"绕圈回到原方向"**。
改由**拇指角速率的峰值**驱动（衰减 400°/s² 的峰值保持）——
手指不动它就是 0，镜头转多快都不变，正反馈从源头断掉。
再乘"方向已定下来"，于是搓杆永不触发。

### 让「看见前方」来得更早：先省延迟，再只在起始瞬态提速（本次迭代）

诉求：保持平稳的前提下，转向时让镜头**尽早**转到目标方向。

稳态速率决定弧的松紧，不能动。但有两处可以：

#### ① 纯延迟——省掉它不会让弧紧一分

| 来源 | 原值 | 改后 |
| --- | --- | --- |
| 稳定确认 `steadyNeed` 的"开阔加权" | ×2.2 ⇒ 0.165 s | **大幅转向退到 ×1.0**（90° 以上立即起手） |
| 跟随驱动 `_followDrive` 起手 | 0.3 s | **0.18 s** |
| 朝向低通 `_headingAvg` 的下限 | lpRate 5 ⇒ τ=0.20 s | **8 ⇒ τ=0.125 s** |

其中"开阔加权"的本意是"开阔时不必立刻决定"，防的是转一半又改主意；
但**大幅转向本身就是无歧义的意图**，再压 0.33 秒纯粹是延迟，
所以让它随转向幅度退掉：45° 以下照旧稳一稳，90° 以上立即起手。

起手时间常数从 0.3 收到 0.18 也不影响平稳——
**"平稳"由角加速度决定，不由时间常数决定**：26°/s ÷ 0.18s ≈ 145°/s²，
远在舒适上限 600 之内。

#### ② 起始瞬态提速——唯一还能加速而不显得"镜头跟你较劲"的窗口

刚转向的那一两秒，玩家本来就在主动改方向，镜头多转的那一点被他自己的操作盖住了。
用 `_turnBurst`（低通后的转向角速度，且要求方向已定下来——搓杆不算）做包络，
起 0.45 s、收 1.2 s，峰值约 0.7：

| | 值 |
| --- | --- |
| 峰值速率 | 26 → **44 °/s** |
| 峰值角加速度 | **147 °/s²**（舒适上限 600）——快而不颠，因为颠的是加速度不是速度 |
| 额外转过的总量 | 约 22° |
| 峰值处弧线半径 | 6.8 m，持续不到 0.5 s ⇒ 横向偏离仅 **0.49 m** |

#### 合计效果

| | 90° 对齐耗时 |
| --- | --- |
| 改前（26°/s 恒定 + 0.3s 起手 + 0.165s 确认） | 3.92 s |
| 改后（瞬态提速 + 0.18s 起手 + 免确认） | **2.60 s** |

**快了 1.3 秒，而稳态速率、弧线松紧、补杆节奏一概没变**——
提速全部发生在玩家自己正在转向的那个瞬态里，转完立刻回落到 26°/s。

### 定案：实时映射 + 缓慢绕行（本次迭代 · 取舍已由玩家拍板）

前面几轮把这套输入方案的取舍空间穷举完了，结论是只有两派，没有第三种。
本轮由使用者拍板选定**魂系/悟空那一派**：
**摇杆映射永远实时（一帧都不错开），靠一条很慢的绕行逐步对齐。**
同时明确否决"松手那一瞬调整镜头"。

于是撤掉三样东西：
- 移动参考系偏置（`MoveBasisOffset` / 对齐欠账）——摇杆与画面不再有任何错位；
- 盲跑归位（松手时的自动摆镜）；
- 转向额度那套快速摆镜（`_turnBudget` / `TurnFollowRate` / 拇指记账）。

`PlayerController` 里只剩一句 `frameYaw = cameraTransform.eulerAngles.y`，
唯一例外仍是玩家自己按的一键回正。

#### 那个速率是唯一的旋钮，它同时决定三件事

`H = C + θ` ⇒ 镜头每转 1°，角色行进方向就转 1°。所以绕行速率 ω 同时是：

    角色弧线半径 = v/ω     玩家需要的补杆速率 = ω     对齐耗时 = θ/ω

**转得快 = 弧紧 + 补杆急；转得慢 = 弧缓 + 补杆轻。**
补杆的【总量】是固定的（θ 必须降到 0），速率只决定它摊在多长时间里。

而最关键的一点：**只要玩家在补杆，角色就是直线**——弧只在完全不补杆时才出现。
所以速率要慢到"补杆是无意识的"，人手自然就跟上了。取 **26°/s**：

| 摇杆离轴 | 绕行速率 | 弧线半径 | 补杆速率 | 对齐耗时 |
| --- | --- | --- | --- | --- |
| 30° | 0.9 °/s | 344 m | 1 °/s | — |
| 45° | 7.4 °/s | 40 m | 7 °/s | 6.1 s |
| 60° | 13.9 °/s | 22 m | 14 °/s | 4.3 s |
| 90° | **26 °/s** | **11.5 m** | 26 °/s | 3.5 s |
| 180° | 26 °/s | 11.5 m | 26 °/s | 6.9 s |

速率随离轴角**比例**给出而不是一到门槛就满速：死区（开阔 28°）内一动不动，
死区往上 60° 内线性升到满速，再往上封顶。于是日常的小幅走位完全不动镜，
只有真的换方向才缓缓跟过去。

前方真的闭塞时 ×1.8（那一刻"看得见路"值得多付一点弧）；
交战中另有死区 30°、转速 ×0.55 的稳定档并联，绕行再降一级。

`SustainedOrbitCap` 就是这个旋钮：调大 ⇒ 对齐更快但弧更紧、补杆更急；
调小 ⇒ 路径更直但对齐更慢。

### 盲跑归位：只在【零代价的那一刻】动镜（本次迭代）

反馈：截图里摇杆与角色移动方向已经一致了，但角色朝镜头方向跑，
**镜头能不能也实时转到前方**？否则一直看不见前方视野。大作怎么解决？

先把这一格算死。θ = 摇杆相对画面正前的角度，截图这一格 θ≈180：

| 映射方式 | 结果 |
| --- | --- |
| **实时** `H = C + θ` | 镜头转 ΔC ⇒ 角色也转 ΔC ⇒ θ 恒为 180 ⇒ **永远追不到背后**。不是慢，是**不收敛**——转十秒也还是 180° |
| **欠账** `H = C − A + θ` | 角色直线，行进方向的画面夹角降到 180 − A。代价：摇杆与画面偏差 **恰好等于 A**，一分不差 |

**这一格只有这两种可能，没有第三种。**大作的分野正在这里：

| | 选择 | 玩家实际体验 |
| --- | --- | --- |
| 魂系 / 法环 / 只狼 / 悟空 / 地平线 | 实时映射 + 缓慢绕行 | 朝镜头跑会跑出大弧，玩家不停补杆（"镜头跟你较劲"是这类游戏的常见吐槽） |
| 原神 / 崩坏 / 幻塔 / 塞尔达 / 多数手游 | **镜头压根不自动转** | 偏航完全交给右侧拖拽 + 一键居中；朝镜头跑就是看不见前方 |

两边都没有"既对准前方又完全不偏"的方案——**因为它不存在。**

#### 但有一个时刻两种代价都为零：松开摇杆的那一瞬

角色本来就在停下 ⇒ 转镜**不画弧**（没有移动可画）、**不欠账**（没有映射要错）。
于是：

- 朝镜头方向真的跑了一段（θ > 110° 持续 0.8s）⇒ 记为「盲跑」；
- **松手那一帧**把镜头摆到刚才的行进方向上，时长 1.24s、峰值 217°/s、
  角加速度预算 700°/s²；
- 下一次推杆时，"画面向上"就正好是刚才要去的方向。

只在**真的盲跑之后**才做，而不是每次停下都做——后者正是此前被否决的
"停下来镜头还自己转"。归位途中玩家又推杆则立刻让位，
于是参考系冻结**永远不会在"有推杆"的时刻生效**，摇杆与画面不可能对不上。

同时把日常转向的对齐额度 30° → **20°**：真正的盲区已经由这条零代价的路解决，
日常转向就不必再背高额度，方向一致性再紧一档。

### 转向对齐：一笔有封顶、会自动还清的「取景欠账」（本次迭代 · 定稿）

反馈两条：① 摇杆与跑动方向基本一致了，但**还会出现偏差**；
② 持续移动中突然转向，**镜头还是应该对准目标方向**，否则对前方一无所知。

#### ① 偏差的来源：`closedUrge` 的量程定错了

`closedUrge = 1 − InverseLerp(6m, 18m, 画面正前通视距离)`，它一旦 >0 就给镜头
旋转授权，而旋转 ⇒ 角色被带转 ⇒ 偏差。可 18m 的量程下，**任何围合场地都算闭塞**：

| 正前能看到 | 改前 `closedUrge` | 改后（3m/9m） |
| --- | --- | --- |
| 8 m | 0.83 | 0.17 |
| 10 m | 0.67 | **0** |
| 12 m | 0.50 | **0** |
| 15 m | 0.25 | **0** |

这条判据的本意是"镜头顶着墙、什么都看不见"，不是"我在一个房间里"。
吊杆才 4.6m，能看到 9m 就完全够用。改成 3m/9m 后，房间里授权归零、角色严格直线；
走廊与贴墙才起来——那才是真需要转镜头的时候。

#### ② 守恒律，以及那个免费窗口

前面撞到过三次同一堵墙。把它写清楚：

> 只要摇杆映射最终会恢复实时，**∫角色被多转的角度 ≡ 镜头自动转过的总角度**。

能选的只有【什么时候付】：(a) 转镜当下付 → 急弧（已否决）；(b) 之后慢慢付 → 缓漂；
(c) 不付、一直欠着 → 摇杆与画面对不上（截图否决，180°）。

但存在一个**免费窗口**：玩家松手的那一刻角色本来就停着，欠账可以一笔勾销、
代价为零——而松手是极高频的动作（转向 → 看清 → 松手重新推杆）。

于是取一个各方都小的点：

| | 值 | 含义 |
| --- | --- | --- |
| 封顶 | **30°** | 摇杆与画面的最大偏差（截图那次是 180°） |
| 松手 | **立即归零** | 最常见的路径，完全免费 |
| 一直按着 | 保持 0.6s，再以 **18°/s** 还清 | 角色转弯半径 **17 m**，比"几乎察觉不到"的 9.9m 还平缓；1.7s 还清 |

关键在于：**这笔欠账同时就是取景收益**。角色朝向 `H = C − A + θ`，
于是行进方向在画面里的夹角由 `θ` 减到 `θ − A`。
上一轮说"旋转对取景毫无贡献"，那个结论的前提正是没有这个 A；
有了它，镜头转 30° 就真的把前方拉近 30°，1:1。
所以额度与偏置**共用同一个封顶**——同一枚硬币的两面一起被钉在 30° 内。

实测（跑动中一次 90° 转向）：

| t (s) | 镜头 C | 欠账 A | 角色朝向 H | 摇杆偏差 |
| --- | --- | --- | --- | --- |
| 0.15 | −6° | −6° | **−90°** | 6° |
| 0.30 | −19° | −19° | **−90°** | 19° |
| 0.50 | −26° | −26° | **−90°** | 26° |

**转镜全程角色朝向恒为 −90°，正是玩家指的方向——一条直线。**
之后保持 0.6s，再以 18°/s 缓漂还清；若玩家在此期间松手（最常见），欠账直接归零。

与上游撤回的那次（`cd4bc4a`）的区别不在手段，在**有没有封顶**：
那次是无限期、无封顶，偏差能涨到 180°；本轮封顶 30°、松手即零、按着不放也会
自动还清。持续绕行（稳态那一路）则仍然完全关闭——**它没有封顶，
正是它把偏差累积到了不可接受的量级。**

顺带把额度泄漏 25→10°/s：封顶已从 65° 收到 30°，泄漏按原值会吃掉近两成。

### 撤回掉头辅助：「摇杆方向 ↔ 画面方向」不可交易（本次迭代）

实机截图给出了否决性证据：**摇杆推在左下，画面里角色却在往画面深处跑**，
偏差最大 180°。上游此前也因同一现象撤回过（`cd4bc4a`）。两次独立验证，结论一致：

> 「摇杆方向 ↔ 画面方向」的一致性**比"轨迹是直线"更基本**，不可交易。
> 玩家是照着画面推杆的，一旦对不上，每一次输入都在赌。

移动参考系偏置整条撤销，`H = C + θ` 全时成立。而它只有一个推论：
**玩家按着摇杆时，镜头每转 1°，角色就被带转 1°。**
所以镜头侧唯一正确的答案是——**按着摇杆时压根不自动转**（即 `_viewBlocked`
开阔时授权为 0，上一轮已经就位）。

#### 这不是妥协，而是这一类游戏的通行做法

| 作品 | 未锁定时的镜头偏航 |
| --- | --- |
| 原神 / 崩坏 / 幻塔（虚拟摇杆） | **完全归玩家**（右侧拖拽），移动不产生任何自动偏航 |
| 塞尔达 旷野 / 王泪 | 归玩家（右摇杆），另有一个显式复位键 |
| 魂系 / 只狼 / 艾尔登法环 | 归玩家；跑动时仅有极缓慢的对齐，且随时被玩家覆盖 |
| 战神 (2018/诸神黄昏) | 固定越肩，完全手动 |

四条结论一致：**镜头偏航是玩家的输入通道，不是系统的自动行为。**
自动运镜只保留四处，且每一处都不与摇杆抢方向：
锁定伺服、显式一键回正、战斗出画兜底、遮挡换角。

一键回正仍保留参考系冻结——那是玩家**自己按的、有始有终的 0.6~0.9s 动作**，
期间冻结可避免镜头把角色一起拖转，动作一结束立即恢复。

### 搓杆时画面晃：留白只平滑了长度，没平滑方向（本次迭代）

反馈：摇杆快速转圈时角色在原地快速转圈，同时出现镜头晃动。

角色跟着搓杆转是输入本身的含义（大作同样如此）。**但画面晃是个真 bug，
而且与偏航跟随毫无关系**——所以之前把旋转授权压到 0 也没治到它。

引导留白把取景点沿【行进方向】前移最多 2.2 m，而镜头是刚性挂在取景点上的。
搓圈时行进方向在转 ⇒ 那个 2.2 m 的偏移跟着绕圈 ⇒ **镜头也在画一个半径 2.2 m 的圆**：

| 搓杆速度 | 方向角速度 | 镜头横移速度 |
| --- | --- | --- |
| 1.0 秒一圈 | 360 °/s | **13.8 m/s** |
| 0.7 秒一圈 | 514 °/s | 19.7 m/s |
| 0.5 秒一圈 | 720 °/s | 27.6 m/s |

根因是**只平滑了留白的「长度」（`_offAxisRun`），方向却是瞬时的**。

改成对**留白向量本身**做临界阻尼：搓圈时各方向互相抵消、均值趋零 ⇒ 取景点几乎不动；
按住一个方向时它照常收敛到满值。时间常数看【方向定没定下来】——
拇指已稳 0.09s ⇒ 0.22s（转向要跟得上），还在扫 ⇒ 0.7s（把搓杆彻底压平）。
判据用拇指角而非世界方向，与镜头无关，不会被镜头自己的运动带跑。

同一条判据也补给了转向拉远吊杆：搓杆时朝向角速度一直很高，
不加这一条吊杆会被长期顶在拉远位（+22%），读作画面忽远忽近。

### 掉头辅助：那条恒等式允许的唯一解（本次迭代）

反馈把问题说到了根上：**转向时镜头仍需要转到对准目标方向，但要避免"摇杆与运动
方向相反"**。举的例子是——一直朝前跑，突然把摇杆拉到相反方向持续移动，
此时镜头应平稳掉头，可玩家的摇杆恰好一直拉在反方向。

先把这个例子算完，它证明了前面所有做法都不可能满足要求：

    恒等式：H = C + θ（移动是镜头相对的，每帧现算）
    ① 摇杆拉到 180 ⇒ H = 0 + 180 = 180（向南跑）。正确。
    ② 镜头要绕到背后 ⇒ C 从 0 转到 180。
    ③ 但 θ 仍是 180（手指没动）⇒ H = C + 180 一起转 ⇒
       C 到 90 时 H = 270；C 到 180 时 H = 360 = 0 —— **转回原方向了。**

要同时成立需要 `C_final = H`（镜头在背后）且 `H = C_final + θ`，
两式相减 ⇒ **θ = 0**。也就是说：

> **只要手指还按在非正前方向，「镜头绕到背后」+「角色走直线」+「每帧现算」
> 三者在代数上不可能同时成立。**

这不是参数没调好。之前几轮在"转多快、转多少"之间反复，本质上是在一个无解的
方程里找解——镜头不转就看不到前方（你的第 2 条反馈），镜头转就画弧或绕圈
（你的第 1、3 条反馈）。

#### 唯一出路：掉头的那段时间里不要每帧现算

新增一个【移动参考系偏置】，由镜头持有、`PlayerController` 解算摇杆时减掉。
它等于本次自主转镜**已经转过的角度**，于是角色朝向在整段转镜里保持不变：

| t (s) | 镜头 C | 参考系偏置 | 角色朝向 H | 轨迹 |
| --- | --- | --- | --- | --- |
| 0.00 | 0° | 0° | 180° | 直线 |
| 0.31 | 28° | 28° | 180° | 直线 |
| 0.62 | 90° | 90° | 180° | 直线 |
| 0.93 | 152° | 152° | 180° | 直线 |
| 1.24 | **180°** | 180° | **180°** | 直线 |

镜头最终对准 180°（角色背后）✔　角色全程朝向 180°，一条直线 ✔

时长由角加速度预算反解：`T = sqrt(6×180/700) = 1.24s`，峰值角速度 217°/s、
峰值角加速度 700°/s²。

#### 与上游撤回过的那次锁存的区别：范围极窄

上游曾试过"锁存移动参考系"并撤回（`cd4bc4a`：**"手指还按着左，画面里角色却在
往前跑，代价不可接受"**）。这个代价是真实的，本版的区别在于把它压到最小：

| | 上游那次 | 本版 |
| --- | --- | --- |
| 生效范围 | 常态（任何跟随绕行） | **只对一次明确的掉头**（拇指角一次转过 >100°） |
| 时长 | 只要镜头在绕就一直有 | 有始有终，约 1.2s |
| 解除 | 镜头停了才解 | **松手立即归零**（那一刻角色本来就停着，无跳变） |
| | | 又做一次大幅换向（>60°）也立即解除——新命令该按当前镜头解读 |
| 增量操作 | — | **全程正确**：手指挪多少度，角色就转多少度；只有"摇杆的画面含义"是旧的 |

其余情形一概不涉及：中小转向仍然是"镜头一度都不转、角色严格直线"（上一轮的结论），
交战中走稳定优先那一套，锁定/手动取景/演出期间由玩家或导演支配镜头。

顺带修掉两处同类隐患：

- 拇指角改为相对 **`_curYaw − 偏置`** 计量。不扣偏置的话，掉头期间偏置一路涨到
  180°，thumb 就跟着甩 180°——又是一次自激（会误触发第二次掉头，或把刚建立的
  偏置立刻解除）。扣掉之后 thumb 恒等于真实的 θ。
- 偏置的锚点取 `_curYaw − 已积累偏置`，于是重叠的转镜（掉头途中又按一键回正）
  是在旧偏置之上**累积**而不是清零，不会跳变。

### 每一度旋转都要问「它换回了什么」（本次迭代 · 定论）

反馈：转向后角色会偏离目标方向一定角度、走弧线前进，最后还得用摇杆补方向。

上一轮把持续绕行按 `_urgency` 压到 0，但**转向额度仍然按 `_urgency` 授权**，
于是开阔地一次 90° 转向还是发 27° 额度 ⇒ 角色被多转 27° ⇒ 弧线 + 补杆。
问题不在数值大小，在**授权的依据错了**。

`_urgency` 是三项取最大。逐项问一句"转镜头能不能改善它"：

| 项 | 度量 | 转镜头能改善吗 |
| --- | --- | --- |
| ① 画面闭塞 `closedUrge` | 沿**画面正前 C** 的通视距离 | **能**——转镜头就是换看的方向 |
| ② 要去的方向快撞上 `ttiUrge` | 沿**行进方向 H** 的剩余时间 | 不能——H 与 C 绑在一起转 |
| ③ 要去的方向不在画面里 `offUrge` | 前视点的屏幕夹角 | 不能——已算出恒为 45.5°，与 C 无关 |

**而"跑动中转向"触发的恰恰是 ②③。**用三项的最大值去授权旋转，等于
*为两个转多少度都白转的理由，付出角色画弧的代价*——那就是弧线与补杆的全部来源。

改法：**旋转（转向额度 + 持续绕行）只由 ① 授权**。
②③ 仍然照常驱动死区、转速、确认时长与取景，那些要么不影响角色轨迹，
要么本来就是"更不动"的方向。

#### 开阔探索时，镜头旋转的每一个来源都是 0

| 来源 | 开阔地形取值 |
| --- | --- |
| 转向额度 `turnAsk × _viewBlocked` | `90° × 0 = 0°` |
| 持续绕行 `30°/s × _viewBlocked` | `0 °/s` |
| 遮挡换角 `_occYawBias` | 仅附近有敌人时 |
| 敌人出画兜底 | 仅有交战敌人时 |
| 锁定伺服 | 仅手动锁定时 |

⇒ **角色朝向恒等于摇杆方向，轨迹是直线，永远不需要补杆。**

#### 取景全部交给不影响轨迹的两个手段

| 配置 | 前视点屏幕角 |
| --- | --- |
| 无留白、吊杆 4.6 m | 45.5° |
| 引导留白 2.2 m | 28.3° |
| 留白 2.2 m + 转向拉远吊杆 5.6 m | **23.9°**（取景窗 36.8°，已在窗内） |

等价于 **21.6° 的"虚拟转镜"，而角色的轨迹一点没被动**。
并且把留白的响应时间常数在转向当下从 0.45s 缩到 **0.22s**——
既然镜头不再靠旋转来响应转向，这条通路就必须来得及，否则读作"镜头没反应"。

只有当镜头**自己**对着墙（走廊、贴墙、退进死角）时 `_viewBlocked` 才起来，
那时旋转确实能换回视野，才允许它动——**并且那一刻"看得见"压倒"走得直"。**

### 转向不走直线的根：持续绕行从来没有【停】过（本次迭代 · 定论）

反馈：转向还是不走直线。

上一轮修好了自激振荡，但那只治了"绕圈回到原方向"的极端情形。真正的根在别处——
**持续绕行只是被限速到 30°/s，从来没有被停掉**：

按住偏轴摇杆时 `err ≡ θ` 恒大于死区 ⇒ 跟随无限期成立 ⇒ 镜头以 30°/s 一直绕，
而 `H = C + θ` 让角色 1:1 跟着转。半径 9.9 m、**12 秒绕满一圈**。
基线（`9d0e603`）在三难取舍里主动放弃了"角色不画弧"这一条，实测证明这个代价不能接受。

#### 一个可以算出来的事实：按住摇杆时，镜头旋转对取景【毫无贡献】

θ 恒定时，把镜头转任意角度，"要去的方向"在画面里的夹角**完全不变**：

| 镜头偏航 C | 前视点屏幕角 | 含 2.2 m 引导留白 |
| --- | --- | --- |
| 0° | 45.5° | 28.3° |
| −30° | 45.5° | 28.3° |
| −60° | 45.5° | 28.3° |
| −90° | 45.5° | 28.3° |

道理就是那条恒等式：画面正前是 `C`，角色朝向是 `C + θ`，两者的差恒为 `θ`——
**镜头和角色是绑在一起转的，相对关系一步都没变。**

所以按住摇杆时，镜头每转 1° 只有一个后果：**角色多转 1°**。
真正改善取景的只有【引导留白】：45.5° → 28.3°（再叠转向拉远吊杆 → 23.9°），
而它**不动角色一根毫毛**。

#### 于是分工彻底清楚了

| 要解决的事 | 由谁负责 | 为什么 |
| --- | --- | --- |
| 看得见要去的方向 | **引导留白 + 转向拉远吊杆** | 唯一真正改善取景、且完全不影响角色轨迹的手段 |
| 转向时镜头有可见的响应 | **有界的转向额度**（一次性，转完就停） | 让画面重新定向，玩家自然会把摇杆推回"画面正前"，环就此收敛——人在回路里 |
| 不动就不动 + 走直线 | **持续绕行速率 = `30°/s × _urgency`** | 开阔时直接是 **0**：镜头一动不动，角色严格直线 |

按住偏轴摇杆时的持续绕行速率：

| 摇杆离轴角 | 视野开阔 | 前方盲区 |
| --- | --- | --- |
| 30° | **0 °/s（直线）** | 30 °/s（半径 9.9 m） |
| 45° | **0 °/s（直线）** | 30 °/s |
| 90° | **0 °/s（直线）** | 30 °/s |

盲区时仍留 30°/s——那时"看得见路"压倒"走得直"，9.9 m 的缓弧是可接受的代价。

顺带三处收紧：

- 转向额度的开阔系数 45% → **30%**（90° 转向只发 27° 额度，一次轻微倾身后彻底停住）。
- `OrbitGateWidth` 30 → **18**：死区抬到 28° 后，28~42° 之间还留着一段部分限速窗口
  （θ=35° 时仍以 ~10°/s 绕行，五秒偏 48°）。收到 18° 后 30° 起就是满限速。
- 绕行判据的参照偏航由 `_yaw` 改为 `_curYaw`，与摇杆的解算基准一致
  （同上一轮那个自激 bug 的同源问题）。
- `enemyNet`（敌人出画兜底）豁免绕行限速：那一路追的是**敌人方位**，
  不是镜头偏航的函数，压根没有代数环，限它只会让敌人留在画外。

### 转向额度的两个致命错误（本次迭代 · 「又开始晃」+「按住摇杆会绕圈」）

反馈两条，其实是同一个 bug 的两面：镜头晃动复现了；一直按着摇杆转向时角色
不走直线，而是**绕圈又回到原来的方向**。

#### 错误一：拇指角减错了偏航，构成自激振荡

摇杆的世界方向由 `PlayerController` 用**渲染**偏航解算
（`cameraTransform.eulerAngles.y`，即 `_curYaw`），而我拿它去减**目标**偏航 `_yaw`：

    thumb = (_curYaw + θ) − _yaw = θ + (_curYaw − _yaw)

`_curYaw` 比 `_yaw` 滞后约 `转速 × rotationSmoothTime(0.11s)`：

| 镜头转速 | `_curYaw` 滞后 |
| --- | --- |
| 42 °/s | 4.6° |
| 100 °/s | 11.0° |
| 170 °/s | **18.7°** |

这个滞后项在镜头起停时来回摆 19°，正好顶在 25° 的记账门槛上：

    发额度 → 镜头快转 → 滞后变大 → thumb 又跳 19° → 再发额度 → …

**自激循环。**170°/s 下转满 360° 只需 2.1 秒——`H = C + θ` 于是把角色也一起
转了一圈，正是"绕圈回到原方向"。改成减 `_curYaw` 才得到干净的 θ：
**镜头怎么转它都不变。**

#### 错误二：把"镜头转得多"和"角色走直线"当成了两件事

它们是同一个量的两面，而且是 1:1 的：

    H = C + θ ⇒ 镜头每转 1°，角色的行进方向就跟着转 1°

这没有任何办法绕开（唯一的例外是锁存移动参考系，而那会让"手指按着左、
画面里角色往前跑"，上游已经实测撤回过）。所以**转速就是角色的转弯角速度**：

| 转速 | 5.2 m/s 下的转弯半径 | 观感 |
| --- | --- | --- |
| 170 °/s（上一版） | **1.8 m** | 原地打转 |
| 100 °/s（本版） | 3.0 m | 倾身入弯 |
| 30 °/s（持续绕行限速） | 9.9 m | 几乎察觉不到 |

#### 两个诉求怎么同时成立：不是折中，是按需分配

「不动就不动」与「转向要跟」不能靠取一个中间值来满足——那样两边都不达标。
正解是**按当下到底需不需要来分配额度**：

| | 视野开阔 | 前方盲区 |
| --- | --- | --- |
| 额度系数 | **45%** | 100% |
| 90° 转向实发额度 | 40° | 65°（封顶） |
| 角色轨迹 | 几乎直行 | 缓弧（看得见路优先） |

再加三道收紧：

- **记账门槛 25° → 35°**：小幅修正一律不动镜。
- **额度上限 100° → 65°**：角色单次最多被多转 65°，超过就读作在原地绕。
- **记账间隔 0.9 秒**（新增硬闸）：无论上游信号出什么问题，
  快速摆镜都不可能连续发生。这既是"不动就不动"的兜底，
  也是对上面那种自激振荡的第二道防线——**一个能自激的机制，
  光修好信号是不够的，还要有一道与信号无关的闸。**

### 转向跟随：镜头该响应【动作】，不是【状态】（本次迭代）

反馈：转向时镜头几乎没有实时跟随切换到目标方向，这不合理。

**确实不合理，而且我之前的每一版都治不了它**，因为病根在代数环上：

    移动是镜头相对的 ⇒ 角色朝向 H = 镜头偏航 C + 摇杆角 θ ⇒ H − C ≡ θ

跟随的偏差**恒等于摇杆离轴角**，镜头转多少度它都不减小。于是：

- 任何"按偏差决定转速"的伺服，在这里都是一个**永不收敛**的回路；
- 为了不让屏幕一直转，必须有持续绕行限速（`SustainedOrbitCap` 30°/s）；
- **而那道闸同时把"玩家刚刚明确转向"这一下也一起压住了**——
  实测 90° 转向，一整秒里镜头只转过 30°，几乎看不出在跟。

一边是"不能一直转"，一边是"转向时必须跟"。之前几版一直在这两个要求之间调参数，
而它们在"按偏差伺服"这个框架里是**不可兼得**的。

#### 大作的做法：一次明确的转向 → 迅速摆过去一个有限的量 → 停

魂系 / 艾尔登法环 / 只狼 / 黑神话悟空 / 地平线的观感都是这套：
**快，但有始有终**；不是持续伺服，也不是无限绕行。
玩家转向后画面很快重新定向，于是他自然会把摇杆推回"画面正前"，环就此收敛——
**人在回路里**，镜头只负责把那一下做得干脆。

按这个思路实现成一份【转向额度】：

| 环节 | 做法 | 为什么 |
| --- | --- | --- |
| 计量什么 | **拇指角 θ**（相对画面）的变化量 | 它与镜头无关：镜头怎么转，按住不动的手指其 θ 恒定。用世界方向计量会被环带着走，永不停止 |
| 何时记账 | 拇指角**稳住 0.09s** 且这次转过 >25° | 搓杆/绕圈时 θ 一直在扫 ⇒ 压根不给额度 ⇒ 镜头维持慢速绕行（"摇杆转圈屏幕不该跟着转"）；一旦定在某个方向上，立刻换成额度 |
| 额度上限 | 100° | **这是"永远不会变成转圈"的保证**：不管怎么搓，一次转向最多换来 100° 的快速摆镜 |
| 怎么花 | 按镜头**实际转过的度数**扣减 | "有始有终"的实处——不是靠计时器，而是靠真的转到位 |
| 花的时候 | 转速上限 170°/s，且**越过绕行限速** | 绕行限速管的是【稳态】，一次转向是【瞬态】，不该由同一个闸管。少这一层就是"几乎没在跟" |
| 顺带 | 跳过确认时长与死区，起手时间常数 0.3→0.12s | 额度本身就是"玩家已明确转向并定下方向"的凭证，再等一遍确认就是那 0.33 秒的迟钝感 |

实测（跑动中一次 90° 转向，开阔地形）：

| 时刻 | 转速 | 已转过 | 剩余额度 |
| --- | --- | --- | --- |
| 0.10 s | 84 °/s | 4° | 83° |
| 0.30 s | 163 °/s | 32° | 50° |
| 0.50 s | 170 °/s | **66°** | 11° |
| 0.70 s | 42 °/s（回落） | 81° | 0° |
| 1.00 s | 42 °/s | 93° | 0° |

改前同样一秒只转过 **30°**。而额度用尽后立刻回落到常规限速，
搓杆时压根不发额度——两个要求同时拿到了，因为它们本来就该由不同的机制负责：
**瞬态归额度，稳态归限速。**

#### 转向缓冲拉远：用空间换时间

反馈：跑动中突然转向时，拉远距离作为缓冲、增加视野看清远方很有必要。

确实——这和紧迫度是同一思路的两只手：一个把盲区**提前**化解，
一个在转向**当下**直接给出更多可见范围，于是镜头有余裕慢慢转过去，
**那记"猛转一下"就免掉了**。

- **用拉远吊杆而不是放大 FOV**：变焦显著加剧晕动，纯位移只是把画面推开，
  是这几个杠杆里唯一还有余量且无副作用的。
- **判据用转向角速度，不用镜头偏差**：偏差恒等于摇杆离轴角 θ，稳态推着不放
  也一直大，吊杆会长期停在拉远位并随拇指抖动前后泵动；角速度只在真的在转的
  那零点几秒里大。45°/s 起算、180°/s 满级，再乘速度（站着原地转身不给缓冲）。
- **不走 `_lenFactor`**：那条是 0.75s 的分镜级慢插值，一次 0.4 秒的转向在它那里
  几乎看不出来。单独走一条起快收慢的临界阻尼——起 0.45s、收 1.2s。
- 幅度 +22%：吊杆 4.6 → 5.6 m，可见前方约多出 1.7 m，角色占屏 40% → 33%，
  仍在主流第三人称的构图区间内。峰值推拉约 1.2 m/s，属舒适区。

#### 顺带修掉的两处「站着不动镜头却在转」

- `steering`（推杆算行进意图）改为**只在交战中**成立。它本是为"出招时移动被定步
  锁住、角色只转不移动"而加的；非战斗时推杆必然产生位移，`moving` 本就覆盖得到，
  而它反倒把"站着用摇杆改朝向"读成了"我要往那边去"。
  短促的改朝向撑不到 0.6 m/s 的低通均速，正好被滤掉。
- 大幅换向的追击锁存 `_combatReorient` 此前**不看是否在移动**，
  于是站着原地转 90° 就会锁存住把镜头绕过去。现要求 `active`，
  背后有威胁 / 敌人已出画时豁免。


### 自动拉远 / 自动推近：景别词汇表补两条（本次迭代）

反馈：某些情景镜头能否自动拉远看全局、某些情景又自动靠近？——**大作里这是标配**，
叫动态景别（dynamic framing）。查证下来最一致的几条：

| 情景 | 大作做法 | 例子 |
| --- | --- | --- |
| 多敌包围 | **明显后拉 + 抬高 + 俯角**，看清包围圈与空位 | 战神、鬼泣、猎天使魔女、蝙蝠侠阿卡姆 |
| 腾空 / 坠落 / 滑翔 | **拉远 + 压俯角**，把落点纳入画面 | 塞尔达（滑翔伞）、原神、神秘海域、刺客信条（信仰之跃）、马力欧奥德赛 |
| 疾跑 / 高速 | 略后拉（速度感 + 更多前方） | 地平线、对马、几乎所有竞速游戏 |
| Boss / 巨型敌人 | 锁定取景距离随目标体型与距离放大 | 怪物猎人、法环、旺达与巨像 |
| 狭窄 / 室内 / 走廊 | **收紧**（不收就穿模） | 神秘海域、最后生还者、生化危机 4 重制 |
| 蹲伏潜行 | **推近压低**，画面收紧、临场感强 | 最后生还者、对马岛之魂、刺客信条、合金装备 |
| 处决 / 决胜一击 | 果断推近特写 | 只狼忍杀、鬼泣、战神怒气 |
| 瞄准 / 蓄力 | 越肩推近 | 几乎所有 TPS |

本作的 `CameraDirector` 已有词汇表（行进 / 对峙 / 群战 / 狭窄 / 决胜），
疾跑后拉与 Boss 距离取景也已在（`runOut`、锁定 `distanceMult` 随敌我距离）。
这次补齐缺的两条，并修掉群战那一条的两个问题：

- **新增「腾空·拉远看落点」**：`distanceMult 1.20`、抬高 +0.22、俯角 +8°。
  判据只认**真的在往下掉**（离地 + 纵向速度 < −1.5 + 持续 0.25s）——
  起跳的上升段落点还不是问题，短跳拉一下又收回来纯属呼吸感。
  过渡速率 4/s（快进快出：跳跃常常不到一秒，1.6/s 等于整段跳跃都没生效；
  落地要立刻收回，否则"人已落地镜头还在远处飘"）。
  俯仰另开一条通路：坠落时不等 2.5s 闲置延迟、也不看水平速度——
  **落点在角色下方**，不压俯角它就在画面外。
- **新增「潜行·收紧压低」**：`distanceMult 0.86`、压低 −0.04、仰角 −2°。
  一半是戏剧（紧张感来自看不到全貌），一半是实用（角色蹲下后，
  长吊杆会把大量地面塞进画面）。
- **群战拉远 1.12 → 1.22**：12% 在 4.6m 吊杆上只有 0.55m，实测读不出"局势展开"。
  22%（+1.0m）配合 +0.34 抬高与 7° 俯角才真的把包围圈纳入画面。
- **群战判据改用「真的在打你的敌人」**：路过一个不搭理你的待机/巡逻敌人
  不该把镜头切成群战。另加 **3 进 / 1 出的迟滞**——阈值上一个人来回进出，
  就是一次 1.0m 的推拉。
- **最短驻留 2.5s**：换景别是分镜级决定，不该每秒发生一次。
  演出 / 腾空 / 狭窄豁免——它们是短节拍，晚一步就没意义了。

选镜顺序即优先级，从最不容妥协的往下排：

    演出 > 没地方（拉远只会顶墙）> 腾空（落点必须看见）> 群战 > 对峙 > 潜行 > 行进

### 连打不再卡顿（本次迭代 · 体力静默中断连段）

实测反馈「连续快速按拳/剑/重/绝招/技能仍会明显停顿；同时推着摇杆更明显」。
算一笔账就找到了元凶——**不是延迟问题，是招根本没打出来**：

| | 数值 |
| --- | --- |
| 轻击连打频率 | ≈ 5.8 段/秒 |
| 体力消耗（旧：每段 8） | **47/秒** |
| 体力回复（旧） | 15/秒 |
| 结果 | 满体力 100 点 **连打 3.2 秒即见底**，之后每次出招 `SpendStamina` 失败 → 静默 `EndCombo` → **人站在原地不动**，每 0.53 秒才憋出一招 |

- **连段永不被体力静默中断**（核心修正）：体力不足时**照常出招**，改为进入「疲惫」——
  伤害 ×0.6、不积意势，并给节流提示。大作的原则是"打得动但没力气"，
  体力只影响**强度**，不影响**能否出招**——连打的手感必须永远连续
- **体力经济重平衡**：轻击 8→**3**、回复 15→**22/秒** → 连打净值 **−4/秒，可无限连打不见底**；
  体力的真实取舍回到闪避/蓄力这些「大动作」上
- **推杆时更明显的原因**：出招瞬间移动速度被**硬性砍到 10%**、收招瞬间弹回全速，
  配合已提速的加减速（0.09s 刹停）形成**速度震荡**，读作一顿一顿的抽搐。
  改为对倍率本身做 ≈0.07s 平滑过渡、下限抬到 0.3——既保留定步分量感，
  又让「边推杆边连打」是一条连续的速度曲线
- **技能侧**：仅在「出招锁定」时才排队重试（冷却/资源不足不入缓冲，避免每帧重试刷屏）；
  冷却与资源不足提示加节流；体力/意志不足从**静默失败**改为明确反馈

### 意图匹配：输入保真度（本次迭代 · 「人机合一」的底层）

实测反馈「对移动、动作、转向的控制不够精细灵敏，希望角色与玩家意图完全匹配」。
这类"隔了一层"的体感，根源不是速度不够快，而是**玩家按下的键没有被完整兑现**。
按大作的输入保真度标准补齐三层：

**① 通用输入缓冲 `InputBuffer`（核心）**
玩家在硬直、翻滚、出招锁定等**暂时无法响应**的窗口里按下的键，此前被整帧丢弃——
真实意图是「我现在就要闪」，却被系统读成「你按早了，重按」。现在一律记录时间戳暂存，
**恢复可控的第一帧立刻兑现**；超窗作废（防陈旧输入迟到触发）：

| 动作 | 缓冲窗 | 解决的具体问题 |
| --- | --- | --- |
| 闪避 | 0.30s | 硬直中/翻滚中按闪没反应；**连续翻滚失效** |
| 跳跃 | 0.20s | 落地前一瞬按跳被吞 |
| 技能 | 0.35s | 出招锁定中按技能石沉大海 |

- **土狼时间 0.12s**：离开平台边缘后仍可起跳——意图是"我在边缘按了跳"，不是"晚了两帧活该掉下去"
- **修复消费式输入互抢**：`MobileInput.GetDown` 每帧只能被读一次，此前 `PlayerController`
  与 `PlayerCombatController`（受身）各读一遍 `Dodge`，谁先执行谁拿到——
  **倒地时按闪常常没反应正是这个原因**。现在全局唯一消费点 + 共享缓冲（已加不变量校验）
- **修复缓冲采集时机**：输入采集原在翻滚 early-return **之后**，翻滚中的按键整帧跳过；
  已移到所有 early-return 之前

**② 移动响应提速（起步不再拖沓）**

| 项目 | 调优前 | 调优后 | 大作标准 |
| --- | --- | --- | --- |
| 0 → 全速 | 0.29s | **0.12s** | 0.08–0.15s |
| 全速 → 停止 | 0.20s | **0.09s** | ~0.10s |

防晕改由**镜头侧**负责（软跟随 + 焦点死区 + 渐进回正），而不是靠拖慢角色本体——
这是主流第三人称动作游戏的正确分工：**角色跟手，镜头稳定**。

**③ 已有的保真基础**（本次核对确认到位）：虚拟摇杆 0.06 径向死区 + 死区外重映射 0–1 +
响应曲线（小幅推杆＝精细慢走）；攻击输入缓冲 0.6s；全部旋转/位移基于 `dt` 帧率无关。

### 出招转向影响（本次迭代 · 「出招 + 推杆转向」不连贯）

实测反馈「做动作/技能/绝招同时推摇杆改变方向时不连贯、不连续」。查出三处叠加成因：

- **技能/绝招/重击期间摇杆完全无响应（主因）**：`IsHardLocked` 分支直接 `return`，
  整个 1.15–1.45 秒的技能连招里推杆毫无反馈，动作结束才**突然弹到**新朝向——
  这正是"断裂感"的来源。改为大作通用的 **attack steering（出招转向影响）**：
  硬锁动作期间仍可用摇杆调整朝向，按状态给不同角速度上限——
  技能/绝招 **110°/s**、重击/蓄力 **85°/s**（最重、转向最慢）；
  **受击/倒地/心理硬直/死亡不给转向**（挨打就该失控，这是打击反馈的一部分）
- **轻击连段转向被过度阻尼**：旧值 `Slerp(rate 2.2)` 每帧只挪 3.7%，推杆几乎转不动，
  读作"一出招就僵住"。改为明确角速度上限 **150°/s**——既能顺着摇杆把连招引向新方向，
  又不会一帧甩离当前目标
- **技能每段硬吸附覆盖玩家意图**：连招每段 `FaceTarget()` 瞬时锁向自动目标。
  改为**玩家意图优先**——推杆幅度足够且方向与目标偏差 >45° 时顺着摇杆出招，
  连招跟着你走；否则才咬住自动目标
- 摇杆输入统一在帧首读取一次，供出招转向与移动共用（消除重复计算）

### 转身盲区修复（本次迭代 · 视野变窄/看不见前方）

实测反馈「改变方向/转身时视野变狭窄、处于盲区」。定位到**两个独立成因**并分别修正：

**成因一：镜头开始回正前的等待过长（主因）**
上一轮防抖调优引入的低通滤波（固定 0.2s 收敛）+ 稳定计时门槛（0.22s）叠加，
转身后镜头要等约 **0.42 秒**才开始跟——这段时间你朝新方向走却看不见前方。修正：

- **自适应低通**：朝向还在乱动时收敛慢（滤掉摇杆抖动），一旦稳定就快速收敛（5→18/s）
- **门槛收短**：探索 0.22→0.15s、战斗 0.35→0.24s（快速搓杆仍永远攒不满，抗抖不变）
- **慢速移动也回正**：阈值 1.4→0.6 m/s——蹲行、走路、出招定步此前**完全不回正**，
  在这些状态下转身等于永久盲区
- 综合：开始回正前的等待 **0.42s → 0.21s**

**成因二：渐进式回正（小角度稳、大角度快）**
固定 85°/s 的封顶让大角度掉头追得慢。改为按偏差插值——
探索 0.5s/70°/s → 0.16s/300°/s，战斗 0.28s/180°/s → 0.15s/340°/s：
180° 掉头完全归位 **1.23s → 0.90s**，而 20° 小幅修正仍保持平缓（0.83s），抗抖不受影响

**成因三：转身时吊杆扫过墙体被拉近 → 画面贴脸变窄**

- 碰撞探测球 0.25→**0.18**：擦过墙角/柱子不再误触发大幅回缩
- 回缩下限 1.35→**1.6m**：即使贴墙也保留可用景别
- **伸出恢复 0.3→0.14s**：扫过障碍后视野立刻回到正常景别，不再长时间贴脸发窄
  （回缩仍保持 0.03s 的快速响应，避免穿墙）

### 战斗提速调优（本次迭代 · 对照大作帧数据标准）

实测反馈「动作/技能/绝招攻击速度很慢」——对照主流动作游戏帧数据做了一轮系统调优：

| 项目 | 调优前 | 调优后 | 大作标准 |
| --- | --- | --- | --- |
| 轻击起手 / 取消窗 | 0.07 / 0.20s | 0.05 / 0.155s | 0.05–0.13 / 0.13–0.18s |
| 轻击全长（拳系） | 0.40s | 0.30s | 0.25–0.35s |
| 剑系末段旋风斩 | 0.60s | 0.46s | 0.4–0.5s |
| 成招终结窗口 | 0.68s | 0.50s | 0.45–0.55s |
| **技能动作锁** | **1.7–2.2s** | **1.15–1.45s** | **0.8–1.4s** |
| 超必杀「觉醒·乱舞」 | 2.2s | 1.75s | 1.5–2.0s |
| 蓄力满 / 二连间隔 | 1.2 / 0.5s | 0.85 / 0.34s | 0.7–1.0 / 0.3–0.4s |
| 指令技冷却 | 3.5s | 2.2s | 1.5–2.5s |

- **收招闪避取消（AAA 核心手感·新增）**：技能/绝招/蓄力二连演完主要打击段、进入**恢复相位**后
  可用**闪避立刻打断收招**——不必干等动作播完，攻防转换一气呵成。这是"卡手感"的最大来源，
  也是大作（DMC/战神/只狼）通用的 dodge-cancel 规则；任何状态切换自动复位，不会误放行
- **攻击动画同步提速 ~15%**（1.4–1.7× → 1.55–1.95×）：逻辑窗口收紧后动画必须跟上，
  否则挥击相位演不完就被下一段切掉——动作依然完整，只是更"脆快"
- 闪避输入改为**每帧统一读取一次**，供收招取消与常规翻滚共用，杜绝消费式输入被读两次而丢键

### 动作与判定同拍 + 打击分量分级（本次迭代 · 「出招慢、看着没力」）

上一轮提速改的是**战斗逻辑**的帧数据（招式 0.30s 一拍），但**画面上的动作**从来没跟着改：
动捕层按固定倍速播 Mixamo 原片段，一记 2 秒的挥砍即使提速到 1.75× 仍有 ≈0.9s 有效时长，
是招式窗口的两三倍。于是每一击都只播到一半就被下一击重置——看到的永远是「慢吞吞地举手、
还没打到就换了个动作」。这一轮把两件事对上拍，并把"力度"做成连续量：

| 项目 | 改前 | 改后 |
| --- | --- | --- |
| 攻击动画时长 | 固定倍速，≈0.6–1.0s（与招式窗口无关） | **由招式帧数据反推倍速**，恰好在 0.30–0.35s 内打完发力窗 |
| 片段裁剪 | 只掐头（前 12–22%） | 掐头 + **去尾**（尾部 20–30% 的"走回站姿"由下一招/移动层接管） |
| 动作层淡入/淡出 | 固定 0.07 / 0.12s | 随招式长短收缩（短招 0.03 / 0.08s），快拳姿态立得住 |
| 顿帧（卡肉） | 轻 0.035s / 重 0.07s，且顿帧中的新请求**被丢弃** | 按力度 0.04→0.115s 连续分级，且可**续期**（重击不再被前一记轻击的顿帧吃掉） |
| 命中特效 | 轻/重两档写死 | 火花密度 9→22、闪核 0.30→0.62 随力度连续变化 |
| 受击冲量 | 轻/重两档 | 按力度 ×0.85–1.6，重击把身体打得明显更夸张 |
| 敌人受击霸体冷却 | 1.1s（Boss 2.4s） | **0.7s（Boss 1.9s）**——一套连段里不再只踉跄一次 |
| 轻连段击退 | ×0.5（几乎看不见） | ×0.85 |
| 刀光拖尾 | **动捕模式下从未开启过**（开合逻辑只写在程序化骨骼分支里） | 按发力窗开合；模型自带兵器/换装兵器都自动挂刃尖拖尾（加色发光，非实心色带） |
| 基础伤害 / 重击 | 16 / 34 | **20 / 42**（+25%），「重击」分级线同步 28→34，普通连段不会因此变成每下都击倒 |
| 五大技能连招 | 每段 0.16–0.22s 节拍、动作锁 1.15–1.45s | 每段 0.14–0.19s、动作锁 **1.0–1.25s**，单段伤害 +35–45% |
| 超必杀「觉醒·乱舞」 | 四击 0.27–0.32s 一拍、锁 1.75s | 四击 **0.22–0.26s** 一拍、锁 **1.5s**（判定窗同步收到 0.21s，短于一拍） |

要点说明：

- **动画倍速由帧数据反推**（`PlayableAnimator.PlayIndex`）：`速度 = 发力窗时长 / 招式时长`，
  下限是原来的默认倍速（绝不比现在慢）、上限 3.4×（再快就只剩残影、看不清出的是什么招）。
  招式表写 0.3 秒出一拳，画面就真的 0.3 秒打完一拳；连点时下一击在**命中相位之后**接入，
  连段读作一串鼓点而不是一串被掐断的起手式
- **「重击」阈值集中到 `DamageResolver`**（`HeavyPosture`/`HeavyPhysical`/`IsHeavy`/`Power01`）：
  此前这两个数在 `EnemyController` 里复制了四遍，调攻击力会悄悄让普通连段升格成「每下都击倒」。
  集中一处后，攻击力调值不再牵一发动全身
- **顿帧可续期**：群战/连段里命中密集发生，旧实现"正在顿帧中就丢弃新请求"会让重击的强顿帧
  被前一记轻击吃掉。改为取更晚的截止时刻续期——每一下都吃得到与自己力度相称的顿挫
- **乱舞的判定窗曾长于节拍**（0.16+0.20=0.36s > 0.27s 一拍）：下一段的 `OpenHitboxTimed`
  会掐掉上一段还没开完的判定协程，于是「演了四刀只结算两刀」——看着华丽却打不痛人。
  现已收到 0.21s，短于最短一拍
- **不加震屏**：按既有约定，任何击中都不晃屏、不闪屏、不掉转镜头。分量全部由
  动作速度 + 卡肉 + 受击反应 + 命中点特效 + 刀光承担

### 快到看不见也是一种慢：可读性下限（本次迭代 · 上一版提速的回调）

实测反馈「有些动作/技能/绝招快到看似角色静止不动，尤其快速按重键的巨剑跳劈」。
两个独立成因，一个是**输入门缺失**的老 bug，一个是上一版**提速过头**：

**① 重键没有动作锁这道门（真 bug，与提速无关）**

轻击按下时若正处于动作锁会进缓冲排队；重键从来没有这道门，每按一次都无条件
`StartCharge()`。于是连点重键时：

```
t=0.00  按重 → 蓄力姿态第 0 帧
t=0.02  松开 → 跳劈第 0 帧（HeavyCombo 开始）
t=0.10  按重 → 又切回蓄力姿态第 0 帧（把跳劈打断在第 0.08 秒）
t=0.12  松开 → 跳劈第 0 帧 …
```

两个姿态都还没动起来就被换掉，画面上就是**完全定住**；顺带每按一次白扣 6 点体力、
把 `HeavyCombo` 协程打断在半路（第二段旋风斩永远打不出来）。
现已补上与轻击同一条规则：动作锁期间不重入蓄力，恢复相位仍可取消接下一发。

**② 反推倍速的上限 3.4 超过了眼睛的极限**

30fps 下 3.4 倍意味着相邻两帧之间跳过 0.11 秒的动作数据，肢体位置一跳一跳地闪，
大脑读不出"运动"，只读出"位置在变的静止姿势"。回调到 **2.2**（每帧推进 0.073 秒），
并补一条 **`MinShownMotion = 0.18s`**：节拍再短，动画也至少按 0.18 秒给
（≈30fps 下 5-6 帧），低于这个数就不是"打了一下"而是"抖了一下"。

| 项目 | 上一版 | 本次 |
| --- | --- | --- |
| 反推倍速上限 | 3.4× | **2.2×** |
| 单次出招最短动画时长 | 无下限（0.14s 节拍就播 0.14s） | **≥0.18s** |
| 动作层淡入下限 | 0.025s（硬切，同样读作卡） | **0.04s** |
| 技能连招节拍 | 0.14–0.19s | **0.18–0.20s** |
| 超必杀四击节拍 | 0.22–0.26s | **0.25–0.28s**（仍快于原始 0.27–0.32） |
| 顿帧时长 / 冻结深度 | 0.04–0.115s / ×0.05 | **0.035–0.10s / ×0.07** |

顿帧那一项要单独说：0.115 秒的近乎全停塞进一记 0.30 秒的轻击，占了三分之一时长，
本身就会被读成"打着打着人不动了"。顿帧要短到只当作"撞击的那一下"，不能长到像掉帧。

**③ 三重气环期间角色真的没有动作**

「定心·四象归一」的三重内收气环只放特效，不换姿态——角色自始至终保持`蓄力`这个
**hold 姿态**（动画停在最后一帧），0.42 秒里人物完全静止，只有光环在扩散。
现在每一环打一记旋身，三环＝三次旋转把搅扰荡开，动作与演出对得上。

### 轨迹、伤害、力度出自同一行规格（本次迭代）

关于「伤害轨迹和力度是否成正比」：**判定形状、伤害、削韧三项一直取自 `MoveTable`
同一行，但击退是个例外**——轻连段的击退用的是一条与招式无关的算式：

```csharp
float knock = (nextDepth >= 2 ? 2f : 1f) + (recipeHit ? 5f : 0f);
if (btn == AttackBtn.Kick) knock += 3.5f;   // 只看「第几段 + 是不是剑」
```

于是同为剑系的「突刺」和「旋风绝斩」击退一样大，而它们的判定框（0.9×0.9×2.7 的
细长直线 vs 3.6×1.4×3.6 的环身）和伤害（1.45× vs 2.00×）差着一个量级。
现改为直接读规格表，四项全部同源：

| 招式 | 轨迹 | 伤害 | 削韧 | 击退 |
| --- | --- | --- | --- | --- |
| 前手直拳 | 点·直击 | 0.70× | 14 | 1.0 |
| 侧踹 | 线·穿刺 | 1.05× | 24 | 2.0 |
| 弓步突刺 | 线·穿刺 | 1.45× | 14 | 3.0 |
| 巨剑横斩 | 横弧·扫击 | 1.10× | 10 | 4.5 |
| 旋风绝斩 | 环·周身 | 2.00× | 28 | 6.5 |
| 旋身空翻踢 | 横弧·扫击 | 1.20× | 34 | 9.0 |
| 蓄力跳劈 | 面·范围 | 2.40× | 26 | 3.5 |

成招终结再 ×1.8。**招式越大 → 判定框越大 → 伤害越高 → 推得越远**，改一处即四项同步。

一处诚实的偏差要标出来：判定窗（`windup`/`open`）是**绝对秒数**，不随播放倍速变化。
长片段的倍速被可读性上限压住时，判定会比画面上的挥击**略早**开启（大约早 1-2 帧）。
方向是对的——动作游戏惯例是判定宁可早不可晚，早一点读作"打得爽快"，
晚一点才读作"明明砍到了却没伤害"。巨剑类片段的收招裁剪已同步收紧
（`end` 0.74→0.68 等）来缩小这个差值。

### 界面重排 + 主菜单（本次迭代 · 「界面很乱、文字重叠、HP 看不清、一进游戏就在打」）

实机截图暴露的四处问题，每一处都能指到具体坐标：

| 问题 | 成因（旧坐标） | 处理 |
| --- | --- | --- |
| 顶部任务文字被按钮压住 | 13 个功能按钮平铺右上角三行，横向排到 x≈975，任务文字居中占 x 460~1460 | 全部收进**一枚「≡ 菜单」**，右上角只留菜单 + 暂停（V2.0 再加一枚「目标」，挪到菜单正下方，仍不进任务文字那一行） |
| 「起步」等姿态按钮上糊着一行字 | 姿态按钮 y=-366 占 x 25~499，心法文字同在 y=-366 占 x 180~940，**重叠 300+ px** | 心法**另起一行**（y=-338），按钮上移到 y=-290 |
| 意势圆点与融合链文字叠在一起 | 两者都在 y=-344，圆点 x 25~147、文字 x 0~400 | 文字起点右移到 x=172，且改左对齐锚点 |
| HP 看不清 | 9 条属性条同宽同高同款竖排、**条上无数字**、**无背板**，白字直接压在黄昏天空上 | HP 独立成主条（38px 高，带「168 / 200」读数），其余 8 项缩成两列小条；整块加半透明深色背板；所有 HUD 文字加深色描边 |

左上角状态区从 9 条 306px 高的等宽条，压缩成「主条 + 两列小条」，占地小了近三成，主次却分明了。
版面坐标集中在 `GameBootstrap.HudLayout` 一处，不再各处硬写 y 值互相压。

V2.0 在这套版面之上再收一层（方案 12.2）：体力作为第二条常驻条插在血条下方，
八项心理值里只有**意志**固定占住小条网格第一格，其余七项改成"被打到哪一条哪一条才亮"，
亮起时按同一套网格从意志后面补位、最多同时四条，几秒后收起。
战斗时屏幕上通常只有三条，暂停与复盘页仍然完整展示——底层数值一个都没删。

**主菜单 / 暂停菜单（新增 `MainMenuPanel`）**

此前进程一起来就直接在世界里挨打，既没有标题页也没有暂停出口。补上标题页的理由不是仪式感：

- 玩家需要一个**还没开始**的状态来看操作说明、改设置、决定从哪一章进；
- 进程刚起来时世界还在生成（建场 → 烘焙寻路 → 加载动作库），直接扔进战斗＝一睁眼就在挨打，且前几秒必然卡顿；
- 暂停要有出口——没有暂停菜单时手机上只能杀进程。

同一块面板两种模式：**标题页**（进入世界 / 操作说明 / 设置 / 退出）与**暂停页**
（继续 / 操作说明 / 设置 / 返回标题 / 退出，ESC 或「暂停」键呼出）。
两者都用整屏不透明背板盖住 HUD 与触屏按钮并归零 `timeScale`，
因此不需要逐个隐藏 HUD 元素（那样极易漏掉某一个，反而更乱）。

两处时序坑一并处理：
- 章节开场白改挂在「进入世界」的回调上，不再在 Bootstrap 里直接弹——否则剧情面板会盖在标题页上，两个模态同时开着还互相抢 `timeScale`；
- `MobileControls` 的摇杆与战斗按钮是在它**自己的 Start() 里**建的，比 `GameBootstrap.Start` 晚一帧，会成为更靠后的兄弟节点盖在标题页上。标题页每帧确认一次自己仍是最后一个兄弟（开销可忽略），不去猜各家的建立时机。

### 再快一档：靠裁窗口，不靠提倍速（本次迭代）

上一版把倍速上限从 3.4 收到 2.2 解决了"快到看不清"，但整体又偏慢了一点。这次继续提速，
用的是**另一个杠杆**：

- 倍速上限 2.2 → **2.6**（每帧推进 0.087s，仍在连得起来的范围内）；
- 真正的提速来自**发力窗再裁短**——拳 0.70→0.64、腿 0.76→0.70、突刺 0.72→0.66、
  旋斩 0.76→0.72、旋踢 0.86→0.78。只演核心一击，摆架势与回位都交给上下一招。
  观感快得多，**每帧推进量却不增加**：提倍速会闪，裁窗口不会。
- 技能连招节拍 0.18 → **0.16**，超必杀四击 0.25–0.28 → **0.22–0.24**（锁 1.45s）。

**连贯性另有一个成因：同一段动作反复从头播。**
定心三重气环全是旋斩、收心三连腿全是旋踢、燃火三连突进全是突刺——同一个片段连着重启三次，
读作"抖了三下"而不是"打了三下"。现在三段各换轨迹：

| 技能 | 改前 | 改后 |
| --- | --- | --- |
| 定心·四象归一 | 旋斩 ×3 | 旋斩 → **撩斩** → 旋斩 |
| 收心·万流归元 | 旋踢 ×3 | 旋踢 → **侧踹** → 旋踢 |
| 燃火·五段燎原 | 突刺 ×3 | 突刺 → **横斩** → 突刺 |
| 镜界·退身反击 | 横斩 ×2（掷气刃） | 横斩 → **撩斩** |

判定形状本就按姿态取自 `MoveTable`，换姿态＝换轨迹，顺带让三段的打击范围也各不相同。

### 七个实机问题的定位与处理（本次迭代）

| # | 反馈 | 定位结论 |
| --- | --- | --- |
| 1 | 「挡」真的起作用吗 | **起作用**，但会**静默失效**：体力不足时 `SpendStamina` 返回 false，格挡直接不成立、照吃满伤，玩家举着盾却看不出区别。现改为力竭时仍减伤 45% 并明确提示；被绕背也给提示（格挡只护正面） |
| 2 | 已解锁关卡应能任意传送 | **本来就可以**（`LevelSelectPanel` + `StoryManager.ZoneUnlocked`：序章、已到过章节的区域、通关后全部）。无需改动 |
| 3 | 角色身上有影子 | 不是投影，是**背光面没有照度**：夜间主光只有 0.14、镜头补光塌到 0.12，角色正面无任何光源 |
| 4 | 场景很暗、看不清 | 同源。且 24 个区域里**只有 6 个手工摆了路灯**，截图里的小题大做审判庭一盏都没有 |
| 5 | 敌人被打飞很远 | **上一版我引入的回归**。`knockback` 是力度记号(1~16)，两个消费端却当**米**用 |
| 6 | 面具没对准眼睛 | 眼孔线本该由**量测顶点**算出，但 `.glb` 走 glTFast 的 ScriptedImporter，网格 `isReadable=false`，量测必然失败 → 一路退到「中心上移 0.18 头宽」这个对谁都不准的常数 |
| 7 | 收剑没对准鞘口、剑鞘拉得很长 | 收刀时鞘口**迎向剑柄**（`aimPt = _blade.TransformPoint(_gripL)`）——先入鞘的是剑尖不是剑柄 |

**照明（3+4）**

| 项 | 改前 | 改后 |
| --- | --- | --- |
| 夜间主光 | 0.14 | **0.30** |
| 夜间环境光 | (0.30,0.31,0.42) | **(0.42,0.44,0.55)** |
| 夜间镜头补光 | 0.12 | **0.34** |
| 路灯点亮时机 | dayFactor<0.25（天全黑才亮） | **<0.40**（黄昏就亮，和现实一样） |
| 无灯区域 | 18 / 24 | **0**（`EnsureZoneLighting` 统一补：出生点 60m 内没灯就补一圈四盏，已手工布灯的区域自动跳过） |

**击飞（5）** —— 这是我上一版把击退改成读 `MoveTable` 时带出来的：

```csharp
// 改前：knockback 当米用
Vector3 kb = dir * dmg.knockback * 0.85f;          // 横斩 4.5 → 一记轻击推开 3.8 米
bool bigLaunch = dmg.knockback >= 4f;               // 4 这条线连横斩都算数
KnockFly(dir, 5.5f + dmg.knockback * 0.6f, 0.55f);  // 成招终结 16 → 飞 15 米
```

改后：普通连段推开 `min(knockback*0.09, 0.8)` 米（0.2~0.8m，打得动但接得上）；
击飞门槛抬到 `knockback≥8`，只剩旋身空翻踢(9)与成招终结(×1.8)够格；
击飞距离 `clamp(1.6+knockback*0.22, 1.6, 4.2)` 米。

**大作是不是都这样？** 不是。普通连段把人推远反而接不上下一段，
所以主流动作游戏里普攻只做小幅位移，「击飞」是**专门的吹飞技/终结技**的特权。

**面具（6）**：结构性修复（`Editor/ReadableMeshPostprocessor` 强制装配类模型可读）
只能救 FBX，`.glb` 那条路短期没有可靠通用解。所以另给一条玩家自己能拧的：
背包页新增**面具微调**（上/下/前/后/放大/缩小/复位），按面具名各自持久化，点一下即时生效。

**收刀（7）**：鞘口改为迎向**剑尖**；对口段改为**先转正再平移**（旋转在 60% 处即对齐，
位置到 100% 才到位）——真实收刀是先把刃立正对准鞘口再顺轴推进，
位置与旋转同步插值时每一帧都是「既没对准轴、又已贴到鞘口」，画面上就是剑横在鞘口上。

### 又一批实机问题（本次迭代 · 四修三诊）

| # | 反馈 | 定位 | 处理 |
| --- | --- | --- | --- |
| 1 | 有时突然跳到另一个场景 | 传送门是**碰到就走**，只有一个 1.5s **全局**冷却挡着。落点常在门框附近，落地时仍压在某扇门（往往是回程门）的触发体里；冷却一过，走位/闪避的微小抖动就再次触发 | 冷却是防连击不是防误触。改为**每扇门各自记「你有没有离开过我」**：落点压在门里 → 该门失效，走出去才重新武装 |
| 2 | 背包不贴背 | 座位偏移 `0.85 躯干半厚 + 0.34 包厚`，包体一厚就把整包推离后背半个身位 | 系数降到 `0.55 + 0.20` 并按身高封顶（`≤0.075 bodyH`），背板微嵌后背 |
| 3 | 倒地死亡卡住过不下去 | 死亡面板只挂在 `OnPlayerDied` 这**一个事件**上，而它只在 `PlayerStats.TakePhysicalDamage` 里抛。任何别的途径把血打到 0，事件就不来；而 `CombatState.Death` 是**不可退出的终态**（`RequestState` 对 Death 直接 `return false`），于是永远躺着，只能杀进程 | 加状态看门狗：**状态（死了）比事件（死亡那一刻）可靠**。每 0.5s 查一次 `Stats.IsDead`，只要没血就一定把复盘面板弹出来 |
| 5 | 角色贰鞋尖上翘 | 脚掌放平 `LevelAnkle` **只在 Idle/Guard 姿态执行**。但异源骨骼的脚踝 rest 朝向差异是**每一帧**都在发生的——走一步、出一拳、被打一下，脚尖就翘回去，停下来才平 | 放平扩到**一切贴地姿态**（只排除翻滚/击倒/死亡/腾空）；脚底高度校准仍只在静立姿态更新（出招时脚离地是正常的，拿那些帧量测会把模型上下拽） |
| 6 | 跳上车顶车开走人不动 | `CharacterController` **不会**自动跟随脚下移动的物体——角色的世界坐标是自己算的。Unity 从来没有内建这个，所有第三人称游戏都得自己补 | 新增 `CarryByPlatform()`：每帧记住脚下碰撞体的位移，原样加到角色身上（带瞬移保护，平台重生时不会把人甩飞） |

**诊断但未改的两条**（成因已定位，但改动风险高于收益，需要实机反馈才敢动）：

- **#4 外装武器剑柄没握在手里**：`FitAndGripWeapon` / `WrapWeaponPivot` 按包围盒长轴猜「握端」再对齐掌心。
  对自带武器有效，是因为它的轴向与参考骨架一致；外装武器（尤其 gltf 导入、根节点带轴向修正旋转的）
  长轴端点判定一旦选反，握位就整体偏移一个剑长。这套判定改一处会影响所有武器与剑鞘装配，
  需要先确认「是所有外装武器都握不住，还是只有 scene 剑」。
- **#7 第一关有时跑不起来 / 掉出可见区域**：`MoveSpeedMultiplier` 是**一个被五个系统共写的标量**
  （拖延沼泽 / 冻结 debuff / 法庭束缚 / 责任球 / 技能解冻），各自用 `Min(cur, x)` 压低、
  各自无条件写回 `1f`。两个效果重叠时先退出的那个会替另一个解除；而若某个源的
  `OnTriggerExit`/`OnDestroy` 没跑到（被传送出触发体是典型情形），倍率就**永久卡低**。
  另有一条独立成因：`apMult = Lerp(0.65, 1, 行动力/35)` ——行动力低时最高速只有 65%，
  这是设计（拖延的体感），但玩家读作 bug。正解是把「共写标量」换成「按来源登记、每帧取最小」，
  那要同时改五处调用点。掉出可见区域大概率是出生点在寻路烘焙完成前落到了几何体外，
  需要日志确认落点坐标才好下手。

### 握不住剑 / 对话框挡按钮 / 行人是胶囊（本次迭代）

**① 武器不是"握住"而是"插进手里"**

上一轮我以为是握端判反（长轴端点选错），实机大图否定了这个猜测：所有武器都一样，
而且不是偏了一个剑长，是**手与柄互相嵌套**。真正的成因在 `HandGripFrame` 少了一步：

```csharp
palm = Vector3.Lerp(wrist, midP, 0.55f);   // 掌【内部】的一个点
...
w.position += palm - gripHoldW;            // 把柄心对到掌内 → 柄轴穿过手掌
```

`palm` 是掌心**内部**的点，把柄心直接对上去，柄就从手掌中间穿过去——
无论 `FingerGrip` 把手指卷得多好，读出来都是"手插在剑柄上"。
真实握持是**柄贴在掌面上、四指绕过去合拢**，所以柄心必须沿掌面法向外移半个柄粗：

```
法向 n = (小指根→食指根) × (腕→中指根)，符号用拇指定（拇指恒在握持侧）
palm += n * (拳宽 * 0.32)
```

**② 言语攻防面板压住战斗按钮**

版面约束（1920×1080 参考系）：右下按钮簇最左缘 x≈1189（「挡」），左下摇杆占 x 100~420。
旧面板 1500×300 摆在正中 y=250，横跨 x 210~1710、纵跨 y 100~400——
把「挡」「剑」和半个「拳」全压在下面，**弹出言语攻防时玩家等于没法防守**。

收到 **920×236** 并左移上抬到 x 260~1180、y 502~738 的空白带；字号 24/30 → 20/24，
选项按钮 470×96 → 286×78。按钮全部露出，摇杆也不挡。

**③ 行人是胶囊**

字面意义上的：`GameObject.CreatePrimitive(PrimitiveType.Capsule)` 涂个随机色就完事。
改用项目里已有的程序化人形骨骼 `HumanoidRig`（玩家无模型时的同一套兜底）——
十几个方块拼出头/躯干/四肢，肤色/衣裤/鞋/发色各自随机，路人之间看得出差别；
再挂 `HumanoidAnimator` 并由 `PedestrianWanderer` 按 NavMesh 实际移速喂步态，
走路时摆臂迈腿，不再是"滑行的胶囊"。全世界共 10 个行人（两个区域），开销可忽略。

> 注：这里必须用 `HumanoidAnimator` 而不是 `SimpleAnimator`——后者只整体倾斜 visual，
> 没有 `rig` 字段，对方块骨骼等于没动。

V2.0 把这套躯体抽成 `ZoneBuilder.MakeHumanoidNpc`，开放城区的市民、
AI 现场生成场景里的 NPC 与经典关卡的路人三处共用（`CityNpc` 同样按 NavMesh 移速喂步态）。
各建各的会立刻露馅：城区是 V2.0 的主舞台，一条街上一半是人、一半是纯色胶囊，
"模拟真实世界"先垮在观感上。

### 收刀返工 + 可学习的战斗规则（本次迭代）

**① 收刀：错的不是"迎向剑柄还是剑尖"，是【鞘根本不该转】**

上一轮我把鞘口从"迎向剑柄"改成"迎向剑尖"——那只是在错误里换了个方向。实机两张分解图说明了真相：

```csharp
Quaternion target = Quaternion.FromToRotation(鞘轴, 从左掌指向剑尖) * _set.rotation;
```

收刀时剑在右手、举在身前，这行把**整套剑鞘转到指向剑尖的方向**上——鞘被甩成水平横在腰前，
鞘与剑首尾相接连成一条三倍身宽的长线（截图二）；第一阶段的"对准"则表现为手臂整个甩出去（截图一）。

真实的拔/收刀是「**鞘稳在胯侧不动，剑去找鞘**」。现在过渡期间沿用自然携持姿态
（竖提于左胯、鞘口朝上偏前），对准完全交给剑那一侧：先转到与鞘轴共线 → 剑尖送到鞘口 → 沿轴推进。

**② 融招很慢的真正原因：成招把自己的帧数据改写成了全游戏最慢的一招**

```csharp
_cur.length = 0.5f;  _cur.cancelAt = 0.36f;   // 普通轻击是 0.30 / 0.155~0.195
```

取消窗几乎翻倍——**"打出融招"的奖励，是全游戏最长的一段僵直**。改为 `0.40 / 0.22`。

另外移除「重→剑」这条融招（`HK`）：重键按下去的常规结果是**蓄力**（动作锁 0.8~2.05s），
把它排进主攻击连招里，中间必然横着一段蓄力，怎么调都不可能连贯。
重击要入连招走的是指令技（方向+重）与切手技那条即时路径。

**③ 单次伤害上限（可玩性规则）**

Boss 重招（基础 22 × 精英招 1.5 × 背刺 1.4 ≈ 46）能一口气打掉近半条命——
玩家还没读懂这一招就已经死了一半。这不是难度，是**不可学习**。
加 `MaxSingleHitRatio = 0.25`：单次物理伤害最多掉四分之一血。
死亡至少需要四次失误，玩家才有机会在同一场战斗里认出招式并改打法。上限只压单次，连续挨打照样会死。

**④ 可学习的固定规则：读招成功 → 对方必定破绽**

大型动作游戏的"我变强了"不来自随机，而来自一条**每次都成立、且能被自己发现**的因果链：

> 看见前摇 → 正确时机闪避/格挡 → 对方硬直 → free 输出 → 打断它的连段

只要有一次不成立，玩家就会归因于运气，从此不再尝试。所以 `PunishAttacker` **不掷随机数、不看敌人等级、不设概率**：

| 触发 | 结果 |
| --- | --- |
| 完美闪避（无敌帧内被物理命中） | 攻击者破绽 1.6s + 必暴击 + 意势+1 |
| **精准格挡**（按下「挡」后 0.2s 内接住物理攻击） | **零伤害** + 攻击者破绽 1.6s + 意势+1 |
| 定心格挡（同窗口接住心理攻击） | 攻击者破绽 1.6s + 专注恢复 |

破绽期本就吃 1.6~2.8 倍伤害，奖励自然给足。玩家能学到的规则因此是确定的三句话：
**红光只能闪 / 普通攻击可以精准格挡并打出破绽 / 破绽期是你的输出窗口**。

**⑤ 台词来源标注**

同一块面板此前既播"脑内回声"也播"心魔喊话"，玩家分不清该用哪套应对
（对内要"不认同"，对外要"不接招"，正确答案不同）。现在标两个维度：
`内部 · 脑内回声 / 外部 · 心魔喊话`，以及 `AI 生成 / 本地台词`
（`DialogueLibrary.LastFromAI` 在取到云端/Provider 台词时置位）。
台词正文也带 `【内】/【外】` 前缀。

**⑥ 部位命中反馈**：本就有（`HitReactionOverlay` 的部位标签 + 头部 1.35 倍伤害 + 腿部 1.4 倍削韧），
这次加强可读性——标签放大一档、按部位分色（头金/腿蓝/躯干橙）、头部标「会心」，并补一簇同色火花。

**⑦ 言语面板再缩小**：920×236 → **820×200**，字号与选项按钮同步再收一档。

### 大作级近战规则（本次迭代 · 对齐主流动作游戏手感）

参考主流大型动作游戏（阿卡姆/战神/只狼/黑神话）的战斗规则，补齐四项核心手感系统：

- **攻击令牌 · 围攻礼让**（`CombatDirector`）：同一时刻只允许 **最多 2 名普通敌人**真正发起
  攻击，其余围绕走位、伺机而动——杜绝被一群敌人同时糊脸的廉价难度，群战更有节奏、可读；
  Boss 不受限（舞台主角）；令牌在一套攻击结束、被打断（硬直/破绽）、死亡、脱战时归还，
  并有超时保险回收
- **待战环走位**（`MaintainStandoff`）：没抢到攻击令牌的敌人只逼近到玩家周围一圈「待战环」
  （攻击距离 +1.8m），太近后撤、太远贴回、在环上绕圈候场——**绝不堆挤进玩家身体**，
  真正做到「一两个进攻、其余围而不攻」，围攻不再是一拥而上的混乱
- **远程只做言语弹幕，不做物理削血**：普通敌人不配置远程物理攻击（近身格斗）；所有 Boss 的
  远程弹幕（法槌标签/凝视光束/引文/追问/硬币「才这点钱」/账单「就这一次」/旧话复读）一律
  **改为纯言语·心理伤害**（去掉远程物理削血），并受凝念能量预算限制——保留「言语攻击弹幕」的
  压迫，去掉不符合大作规则的远程物理弹雨

### 言语攻击只压心神，不扣 HP（本次迭代 · 严格对齐方案「十四言语攻防系统」）

方案明确：敌人言语攻击 →「**玩家心理数值受到影响**」、错误回应「增加反刍/内疚/羞耻」——
**不涉及 HP**。据此全面核对并修正所有言语/感知类攻击，使其只作用于心理数值：

- **纯言语攻击**（台词驱动）：`DoMentalAttack`（凝视/低语 + 三选一言语攻防，本就 `isMentalOnly`）
  与 8 处话语弹幕（标签/凝视光束/引文/追问/硬币/账单/旧话）——全部只压专注/自尊/边界/意志、
  推高反刍/关系消耗，不扣 HP
- **感知/索取型攻击**（此前误扣 HP，本次修正为纯心理）：**凝视扫射**「千目审视」→ 羞耻/自尊；
  **车灯眩光**（外界刺激）→ 专注；**索取冲击**（默认索取型）→ 边界 + 关系消耗
  （举盾明确拒绝仍触发 Boss 大破绽，联动不受影响）
- **保留扣 HP 的只有「动作攻击」**：近战连招、对攻，以及**物理力学 AoE 机制**
  （审判冲击波/自我否定重锤/概念迷环/意义崩桥/责任法槌/内疚落石——预警圈可闪避的物理招式）——
  这些是「动作打动作」，与言语攻击是两条独立路径
- **危险攻击 · 红光警示**（不可格挡）：精英/Boss 有概率使出**危险一击**——头顶亮「危」、
  地面红圈更大更亮、专属警示音、前摇略长给足反应窗口；命中**不可格挡（硬挡会被破防吃满伤）
  ，只能闪避**，教玩家看红光读招而非无脑格挡；伤害/击退加成兑现威胁
- **破韧处决**：敌人被削韧击破进入破绽期时，用**重击/大招**命中触发「**处决**」——
  巨额增伤（×2.8）+ 强顿帧 + 短慢镜 + 能量爆发横幅，把「削韧破防→抓破绽猛攻」
  做成有仪式感的收益循环
- **连段伤害衰减**：连续命中第 7 击起温和递减到最低 0.75 倍（停手 1.5s 复位）——
  正常 4—5 段连招几乎不受影响，只压制长时间锁血的无限连 cheese，鼓励收放有度的连段

### 五种战斗姿态（本次迭代）

把方案「五种姿态」落成战斗层的策略选择——**属性条下方一排五枚姿态**，点选或按 `Tab`/`F` 循环切换：

| 姿态 | 心法 | 克制弱点轴（心理减伤 60% + 言语契合加成） | 物理输出 |
| --- | --- | --- | --- |
| 起步 | 不要想太多，先动起来 | 拖延 / 求职焦虑 / 低信心 | ×1.06 |
| 边界 | 我守住我自己 | 边界冲突 / 公平责任 | ×1.00 |
| 定心 | 外界可以存在，但不能接管我 | 噪声干扰 | ×1.00 |
| 事实 | 发生了什么，先说清楚 | 公平责任 / 求职沉默 | ×1.08 |
| 意志 | 我害怕，但我继续向前 | 意志崩塌 / 羞耻 / 自我怀疑 / 失败恐惧 | ×1.10 |

- **把姿态切到与来袭弱点轴匹配的一档**，这次心理伤害 ×0.4（减 60%）；进攻型姿态（起步/事实/意志）另有物理输出加成
- 与言语攻防联动：正确回击时若**当前姿态正好克制这条轴**，额外回补属性、更快清空反刍（「姿态契合·加成」）
- HUD 高亮当前姿态并显示心法，切换时底部字幕提示核心句

### 责任转嫁法院（终章 · 边界与责任线）

按方案「第四章 边界与责任线 / Demo 关卡四」把整座法院落成一个**自南向北纵深推进**的关卡，
物理空间、地图结构、可交互物体、战斗机制均对齐方案：

- **地图结构（三段式推进）**：法院大门（南墙缺口）→ **文件走廊**（两侧证词走廊/陪审团阴影，
  文件山与锁链闸挡路）→ **责任天平大厅**（责任天平/责任归属台/边界圈）→ **审判席**
  （最高法官台 + 法槌，Boss 在此出现）
- **可交互 / 可破坏物体**：
  - **文件山**：用兵器（**事实之刃**）连击可击碎，露出**事实证据**拾取物（回补心理属性、
    象征"用事实回应而非争对错"）
  - **锁链闸**：横跨走廊，链未断时站入**大幅减速**；劈断任一根锁链即**「破念」**解除束缚
  - **责任天平**：走近即**判断责任归属**——天平从失衡回正，**解锁/确认「责任归还」**并回补边界
  - **边界圈**：安全区，站入缓慢恢复边界、消退「过度负责」
- **责任球机制**：Boss【全责法官】周期性把「责任球」抛向玩家，玩家要**分辨真假**——
  - **红球=虚假责任**：举**边界盾**（格挡键 / 触屏「挡」）当场挡回抛球者削其韧性；
    若不挡任其命中，会附着**「过度负责」**（减速 + 边界流失 + 反刍上升）
  - **绿球=真实责任**：**别推开**，接下它回稳意志；用盾把真实责任推开=推卸本分，掉自尊
- **三件战斗工具**（对齐方案「边界盾 / 责任归还 / 事实之刃」）：
  - **边界盾**＝格挡，当场挡回飞来的责任球
  - **责任归还**＝新技能（技能键 `3` / 触屏 术→重）：清除「过度负责」减速、把所有飞来的
    虚假责任球一次性打回法官、回补边界——"把不属于我的部分准确还回去"
  - **事实之刃**＝劈开文件山取证
- **法槌落判**：Boss 近身时在玩家脚下预警红圈后**落槌 AoE**（可翻滚/走出圈外躲开），
  即方案「法槌·Boss 阶段攻击源」
- 与言语攻防联动：法官心理攻击走**边界轴**，最优回击「帮助有边界，这次你自己承担」，
  **边界姿态**正好克制（心理减伤 60% + 契合加成）；战后复盘新增法院四栏
- 入口：主线打通城市广场后，从广场**东侧大门缺口**的传送门进入；「敌人+」面板亦可随处添加
  【全责法官】挑战，个性化画像中「边界/公平责任」高分者遭遇战优先匹配全责法官

### 安全屋系统（本次迭代 · 成长闭环产品化）

右上角「安全屋」枢纽汇总五大子系统（各面板均可独立打开，数据本地持久化）：

- **复盘**：四栏（事实/感受/边界/行动）改为**可编辑输入框**——默认按当前章节生成模板
  （十章各有专属文案），也可改写成自己的话；「归档此战」= 清空反刍 + 回补属性 +
  **写入旧事档案** + **复盘点 +1**（反刍 ≥10 才发点，防刷）
- **成长（技能树）**：五条路线 × 两级共十节点——边界加固/明确拒绝、专注扩容/声音过滤、
  起步惯性/火种常燃、事实之刃锋利/成本判断、自尊锚点/旧事止响；用复盘点解锁、即时生效
  （上限扩容/承伤缩减/冷却缩减/输出增益）
- **装备（五大套装）**：边界守卫套/专注夺回套/公平复盘套/行动起步套/旧我整合套——
  一次穿一套，被动倍率对应各困境线（含方案核心台词）
- **图鉴**：18 种心魔的心理轴、**克制要点**与累计击败数
- **档案**：历史复盘四栏翻页浏览——旧事从脑内回声，变成档案里安静的一页
- **目标板 · 今日唯一目标**（方案核心循环）：从七条成长挑战模板选一条或自己写
  （游戏内挑战/现实行动小任务均可），「钉下目标」后 **HUD 常驻显示**；
  「完成打卡」每日一次 +1 复盘点并恢复行动力——每次进游戏都有明确的小目标；
  独居小屋的目标板物件靠近时提示今日目标状态
- **行动 · 现实行动追踪闭环**（本作核心命题落地）：复盘「行动」栏写下的
  「现实里：去做 X」在归档时**成为一条现实承诺**；下次回安全屋「行动」面板,
  游戏会问「上次你说要做 X,做到了吗?」——**做到 → 复盘点 +2 + 连续行动天数 +1**,
  暂时没做则温和归档不惩罚。把游戏内成长与现实里的一小步真正绑定;隔天未确认
  会有回访提示,安全屋入口挂待确认角标
- **关卡选择 · 传送**（右上角「传送」按钮直达）：全部区域列表 + 解锁状态，
  **当前章节目标区域用 ▶ 高亮**，点击即传送到区域出生点——跨区域章节
  （如第八章回访噪声街区）不再靠走路找入口；章节通关后字幕直接指出下一战场

### 新技能（本次迭代 · 方案 Demo 必备技能补齐）

| 技能 | 键位 | 效果 |
| --- | --- | --- |
| 五分钟火种 | `4` / 触屏 术→跳 | 恢复行动力、清除减速与身份冻结、意势+1——不等动力，先开始 |
| 不读心盾 | `5` / 触屏 术→闪 | 10 秒内抵消下一次心理攻击、幻影假目标显形消散——猜测不当事实 |
| 注意力回收 | `6` / 触屏 术→挡 | 清除全部幻影、恢复专注、降低反刍——把注意力拿回来 |

（旧事归档为回声馆展柜交互与复盘面板动作；旧我整合式为终局整合圆环演出）

### 个性化画像（第 5 阶段）

- HUD「画像」面板：输入自己的经历/困扰 → 自动脱敏（手机号/邮箱/证件/链接）→
  弱点轴关键词分析 → 展示弱点画像（十轴打分）与推荐试炼场景 → 一键应用并本地保存
- 应用后敌人的心理攻击伤害加成与台词方向会针对你的高分弱点；原文分析后即弃不落盘

### 人形角色与类人动作

- **完整人形骨骼**（程序化生成）：头部（眼/鼻/嘴/发型/斗笠）、躯干、骨盆、
  双臂（肩/肘/手）、双腿（髋/膝/脚），玩家与所有敌人均为人形
- **类人关节动画**：走/跑步态循环（腿摆+臂摆反相+躯干起伏+待机呼吸）、
  慢走（摇杆半推）、蹲伏潜行（C 键/「蹲」）、跳跃收腿、翻跟头（团身空翻）、
  挥臂劈砍（肩肘联动+转腰）、双手举械重劈、受击后仰、硬直摇晃、仰面倒地、死亡瘫倒
- **快速灵活转身**：转向角越大转速越快，掉头不拖泥带水；镜头自动跟随提速联动

### 角色与兵器

- **两套角色预设**（「角色」按钮切换，本地持久化）：青岚剑客（清瘦/青蓝劲装/佩剑）、
  烈风刀客（魁梧/赤褐短打/斗笠/佩刀）——身材、肤色、着装、兵器全不同
- **兵器系统**：剑/刀/棍/爪程序化建模，挥击带**刀光拖尾**；不同敌方持不同兵器
  （明日幻影持棍、咳声刺客利爪、羞耻镜像佩剑、拖延影魔大刀）
- **远近双距离攻击**：近战连招 + 远程「斩念气刃」（技3/气 键，自动瞄准发射剑气）；
  远程系敌人（低语/镜像/影魔）中距离发射心念弹

### 敌人配置

- 每个场景默认只有**一个章节心魔**
- 「敌人+」面板自由添加挑战：5 种类型（明日幻影/咳声刺客/自我怀疑低语/羞耻镜像/拖延影魔）× 4 档难度（见习/标准/精英/首领，数值与体型缩放）

## 操作

### PC（编辑器 / 桌面构建）

| 按键 | 功能 |
| --- | --- |
| WASD / 鼠标 | 移动 / 镜头 |
| V | 镜头回正（一次性拉到角色行进方向背后；交战中松杆时也会自动执行） |
| 鼠标左 / 右键 | 轻攻击 / 重攻击 |
| Space / Shift | 跳跃 / 闪避（流云步） |
| Ctrl（按下瞬间为精准格挡） | 格挡（边界盾）/ 定心格挡 |
| Q / R | 锁定敌人 / 内功（意志燃烧） |
| Tab / F | 循环切换战斗姿态（起步/边界/定心/事实/意志） |
| 1 / 2 / 3 | 技能：定心护体 / 能量斩·斩念气刃 / 责任归还（法院清除过度负责、打回责任球） |
| 4 / 5 / 6 | 技能：五分钟火种（解冻/恢复行动力）/ 不读心盾 / 注意力回收（清幻影） |
| E（靠近目标板按住） | 目标板蓄力，恢复心理属性 |
| E（靠近家中物件 / 心理裂隙） | 使用房间物件（目标板/书桌/衣柜/镜子/电脑/手机/冰箱）；走进裂隙 |

### 安卓真机

- 左下虚拟摇杆移动（半推=走路，全推=奔跑），右半屏拖动转镜头；**双击右半屏＝镜头回正**
- **方向与镜头是分开的两套操作**：探索时朝同一方向持续跑，镜头才缓缓绕到身后；
  **近身交战（敌人 8m 内）镜头完全不跟方向**（不敏感、不抖）——改由取景窗把正在打的
  敌人框在画面里：敌人在画面中间一大片区域内时镜头纹丝不动，只有快要出画才推回来；
  松杆时再自动回正一次把构图摆正，敌人彻底出画超过 0.5 秒则无条件摆回去。
  **其他一切情况（无敌人／敌人 8m 外／正在撤离）镜头实时同步跟方向。**
  想全程盯住某个敌人就按「锁」
- **右下六核心键 + 一修饰键（按拇指人体工学解出的布局）**：拳 / 剑 / 重 / 闪 / 挡 / 跳 + **术**
  - 三层排布：拳（最高频）在拇指自然静止点 9.1mm 处、最大；剑/重/闪 内弧 27.3mm；
    挡/术/跳 外弧 44.3mm，且**与内弧同辐射线**——拇指沿一个方向扫出去先碰内环再碰外环，
    肌肉记忆是一维的。任意两键间隙 ≥6.6mm，高于触屏可靠间隙下限
  - **术＝技能修饰键**（借鉴手柄 L1+面键把 4 键变 16 招）：点一下亮起 3 秒，
    亮起期间六个核心键临时变成六个技能——**术→拳=定 · 术→剑=气 · 术→重=还 ·
    术→跳=火 · 术→闪=盾 · 术→挡=收**；放完自动落回普通模式，再点「术」可取消。
    亮起时按钮字面自动换成实际装备的技能名并变蓝紫，「术」自身随剩余时间收亮
  - 触屏用**点选式**而非手柄的按住式：一根拇指按不住修饰键再去点另一个键；
    平板/外接手柄真按住时同样生效，且可连放多个技能
  - 重路由发生在输入中枢（`MobileInput.SkillMap`），所以一次点击**要么出拳要么放技能，
    不会两者都发**；六个技能不再各占按钮位，战斗区从 14 键降到 7 键
  - 低频操作移出战斗区：锁定 / 拔刀收刀移到屏幕右侧边缘竖排，摇杆旁「蹲」切换蹲伏
  - 技能因此也进入融合链——术→剑 打出的是「术后追斩」，一键技能成为组合的一环
- 责任转嫁法院：**劈开文件山**取事实证据、**劈断锁链**破念解束缚、走近**责任天平**判断归属解锁「还」；
  面对 Boss 抛来的**责任球**——红球举「挡」打回、绿球别挡接下，被减速时按「还」一次清掉
- 右上角只有三枚：「≡ 菜单」（功能全在里面：养成 / 探索 / 角色 / 系统 / 调试）、「暂停」、「目标」
- **V2.0 新增四个面板**，「目标」在 HUD 上直接点，其余三个在菜单里（「目标」组排第一位）：
  「目标」（写下目标 / 有效性检查 / 重铸 / 现实行动打卡）、
  「旅程图」（旅程图 · 障碍图 · 章节来源三个页签，可加入自己预设的章节）、
  「逆境史」（宿敌逐条回放与逆袭判定 + Hall of Goals）、
  「逆境图谱」（弱点/优势图谱：样本数、场景数、置信度、证据等级，可整条删除、可一键关闭个性化）
- 在家里还可以不开任何菜单：走到墙上目标板/电脑/书桌/衣柜/镜子/手机/冰箱前按 E 直接打开对应面板
- **HUD 情境化**：常驻只有 HP / 体力 / 意志 + 当前目标 + 姿态条；
  专注、自尊、边界、行动、反刍、消耗、刺痛**被打到哪一条哪一条才亮**，几秒后收起
  （底层数值一个都没删，复盘页仍然完整展示）
- 敌人发动心理攻击时，屏幕下方弹出「言语攻防」三选一回应面板——选对即可回击破防、化解伤害
- 左侧属性条下方「姿态条」五枚按钮：点选切换战斗姿态（把姿态对准来袭弱点轴即可大幅减伤）

### 客厅电视 · 悬浮小窗 · 出门就能跑（本次迭代）

三件事一起做，因为它们都在"住所 + 手机平台"这一层。

**一层休息厅那面空墙上现在有一台 4.8 米宽的液晶电视**（16:9，沙发正对着它）。
走近按「用」（或菜单 → 系统 → 电视）打开遥控面板：六个频道（三个内置 + 三个自己粘
YouTube 链接的槽位）、开关机、播放/暂停、静音、后台播放开关、切悬浮小窗、在 YouTube 里打开。

画面分两套，这是权衡而不是偷懒：

- **站在电视正面** → 真正的 YouTube 播放。手机上能内嵌 YouTube 的唯一正路是 WebView
  （把视频流解出来喂给 VideoPlayer 既违反条款也隔几周就失效），而 WebView 渲染进的是
  安卓的 View 树、出不到 GL 贴图里，所以画面是一块**盖在"电视屏幕投影出来的矩形"上**
  的原生网页层：C# 每帧把屏幕四角投影到屏幕空间算出包围矩形交给它。
  正因为是轴对齐矩形，**只在玩家大致处于正面时**才摆上来——斜着看时贴不上一个透视四边形。
- **走开、从侧面看、隔着墙、或在编辑器里** → 世界里的屏幕显示程序生成的动态画面
  （每个频道一套配色 + 扫描线 + 四角压暗），声音继续。从侧面看过去电视是亮着的、在动的。
  「隔着墙」这条是玩家反馈「播放器会跟着移动」的根因：上一版只判断了正面与距离，
  没判断**看不看得见**，于是站在隔壁房间朝电视方向看，视频就被画在了那堵墙上、
  还跟着镜头走。现在加了一条视线检测（跳过玩家自己的碰撞体，每 0.15 秒一次）。
- 网页层的矩形按 Unity 画面像素量出来，**必须换算成安卓视图的真实像素**再摆
  （分辨率缩放、刘海、分屏、悬浮小窗都会让两者对不上），否则画面会摆到电视旁边去。
- **为什么画面会"跟着移动就左右跳"**：网页层是安卓的原生视图，布局在 UI 线程上排队，
  比算出矩形的那一帧晚一到两帧才生效；而 3D 里的电视是当帧渲染的。两者之间因此永远
  差一个固定延迟——这没法靠对齐算法消除，原生视图和 Unity 的渲染压根不同步。
  对策是按这个固定延迟**做外推**：用上一帧到这一帧的位移把矩形往前推 1.5 帧，正好补上
  滞后；一帧就跨过四分之一屏的甩镜头外推也救不回来，那时收起网页层，让贴在电视上的
  程序画面顶上（它永远不会跳）。另外取的是四角投影的**内接矩形**并再往里收 3.5%
  （包围盒一定比透视梯形大，会溢到边框和墙上），变化不足 2 像素不重新下发布局，
  网页层盖上来时世界里的屏幕改画纯黑当衬底。
- **「此视频不能观看」/「错误 153」不是播放器坏了**：那张卡片是 YouTube 自己画的——这条视频的作者
  禁止了站外嵌入。所以内置频道只是默认值，主力是自己粘链接的三个槽位；
  播放起来没有的话，游戏会在几秒后给一句提示，让你换台或用「在 YouTube 打开」。
  加载方式有两套，第一套没播起来会**自动换第二套再试**，并把 YouTube 给的错误码
  翻成人话报给玩家（150/101 = 作者禁止站外播放，152/153 = 来源被拒，100 = 视频不存在…）：
  ① 自建播放页，从一个**真实来源**供给：页面不走网络，而是由 `shouldInterceptRequest`
     应答 `https://tv.adversityroad.app/player.html` 这个请求，于是 WebView 认定文档
     真的来自这个第三方域名，`origin` 也与之相符。此前用
     `loadDataWithBaseURL("https://www.youtube.com", …)` **伪造**来源——一个 Referer 是
     youtube.com 自己的嵌入不是真嵌入，YouTube 回的正是"此视频不能观看（150/152）"。
  ② 直接加载 `youtube.com/embed/<id>` 并补 `Referer` 请求头
     （WebView 自己不发 Referer，无 Referer 的嵌入请求回的正是"错误 153"）。
  两套都会把 UA 里的 `; wv` 去掉——那个标记是页面识别"我在 WebView 里"的依据，
  YouTube 见到它会拒掉一部分内容。播放控制也分两套：①走 IFrame API 函数，
  ②直接操作页面自己的 `<video>` 元素。

网页层**一律不吃触摸**（Java 侧容器先截走触摸再拒绝它，事件继续派发给 Unity 视图）：
不然站在 5 米宽的电视前时摇杆会被网页盖住，玩家会卡死在原地。所有播放控制都走面板。
面板打开或游戏暂停时网页层自动收起——它画在 UI 之上，不收起就会盖住面板。

**后台播放**：收起画面时不停播；应用切到后台时保活网页层（`Application.runInBackground`
+ WebView `resumeTimers`）。最靠得住的形态是**悬浮小窗**——窗口还在前台可见，系统不会掐声音。
（没有做前台服务，所以长时间被系统限制的后台仍可能被掐掉。）

**悬浮小窗（安卓画中画）**：菜单 → 系统 → 悬浮小窗，或电视面板上的「切悬浮小窗」。
小窗里游戏继续跑。三块拼起来才成立：

1. `Assets/Plugins/Android/adversityroad.androidlib` —— 只为把
   `android:supportsPictureInPicture` 合并进 Unity 生成的活动。没有这个清单属性，
   系统直接拒绝请求；而**替换** Unity 自己的 AndroidManifest.xml 要手工维护启动活动、
   intent-filter 与 meta-data，写错一处应用装得上却起不来——库工程只是"贡献属性"，
   写错了是构建失败，不会出一个起不来的包。
2. `com.adversityroad.pip.PipBridge` —— `enterPictureInPictureMode` +
   一个无界面 Fragment 监听进出小窗（Fragment 不必写进清单）。
3. `UI/PipAdaptor` —— **画面自适应**：进小窗即把整块 UI 关掉，只留游戏画面。
   小窗只有几百像素宽，摇杆会缩成指甲盖，而且**小窗根本收不到触摸**
   （点一下只会把应用拉回全屏），那些控件除了挡画面没有别的作用。回全屏时原样恢复。

**出了住所就能跑**：室内"只走不跑"以前靠触发盒的进出事件累加一个静态计数，
而"离开"有好几种走法只有一种会发退出事件——走出大门会发；从传送面板跳到别的关卡时
玩家被瞬移走、往往等不到退出回调；世界重建把触发盒直接销毁更不会有人来减那个计数。
后两种一旦发生计数永远停在 1，**这辈子再也跑不起来**。现在改成**按包含关系判定**：
每帧看玩家坐标在不在任何一块室内区里，无状态，怎么离开的都不影响，下一帧状态就是对的。
出门时补一句「出门了——可以放开跑」。跑步机的豁免口照旧。

## 获取安卓 APK

1. 推送到 `main` 或 `claude/**` 分支（或在 Actions 页手动 Run workflow）。
2. 打开仓库 **Actions → Build Unity Android → 对应运行**。
3. 页面底部 **Artifacts** 下载 `Android-Build`，解压得到 APK 安装到手机。

## 项目结构

```
Assets/
├── Scenes/SampleScene.unity          # 构建入口场景（挂 GameBootstrap）
├── _Project/
│   ├── Materials/M_Base.mat          # URP Lit 基础材质（保证 Shader 进包）
│   ├── Resources/Quiz/quiz_bank.json # 休养生息题库（420 题·7 章·来源标签可追溯）
│   └── Scripts/
│       ├── Goals/         # V2.0 Goal OS：GoalModel/GoalParser(有效性检查+安全拒绝)、
│       │                  # GoalJourneyGraph(有向图+四维进度)、GoalPriorityEngine、
│       │                  # GoalChapterGenerator(三层Prompt+JSON契约)、
│       │                  # ChapterBlueprintValidator(CQS+四重校验)、
│       │                  # FallbackBlueprintLibrary、ChapterModuleLibrary(已批准模块库)、
│       │                  # LegacyChapterCatalog(V1 七大线→通用逆境模块)、GoalProgressHook
│       ├── OpenWorld/     # V2.0 开放世界：OpenWorldBuilder(六区连续城区+Player Home)、
│       │                  # DistrictCatalog、WorldState(持久世界状态)、
│       │                  # WorldLayerController(现实/逆境/内心三层+心理裂隙)、
│       │                  # WorldEventManager(六类事件)、CityNpc(日程)、
│       │                  # GoalBeaconController、ProceduralQuestAssembler(确定性组装)、
│       │                  # GoalWorldBinder、HomeFixture(家中物理 UI)、
│       │                  # WallTv(客厅超大屏电视 + 频道/后台播放)、
│       │                  # IndoorZone(室内镜头与只走不跑·按包含关系判定)
│       ├── Adversity/     # V2.0 逆境层：AdversityDirector + AdversityBudget(五维预算)、
│       │                  # PlayerBehaviorAnalyzer、AdversityProfile(弱点/优势/经历图谱)、
│       │                  # AdaptiveEnemyController(T1-T7+心理攻击链)、NemesisSystem、
│       │                  # AdversityHistoryHook(逆袭判定)、StressStateMachine、
│       │                  # ResolveSystem + CourageSystem
│       ├── Core/          # GameBootstrap、StoryManager(十章剧情)、BattleFlow、GrowthSystem
│       │                  # (复盘点/技能树/套装/图鉴/档案)、QuizSystem(休养生息答题规则层)、
│       │                  # QuizAiBank/QuizAiService(AI自动命题·校验与生成)、
│       │                  # AIPromptConfig、UiUtil、GameManager、GameEvents、SafetySettings
│       ├── World/         # ZoneBuilder(24 区域程序化建造)、DayNightCycle、
│       │                  # CarMover、PedestrianWanderer、Portal
│       ├── Player/        # 移动、镜头、锁定、七维属性
│       ├── Combat/        # 连招、状态机、Hitbox/Hurtbox、CombatFeedback(特效)、
│       │                  # SimpleAnimator(程序化动作)、心理伤害、技能、机制物件
│       ├── AI/            # 敌人 FSM、EnemyCatalog(18类型×4难度)、四大机制Boss(法槌/
│       │                  # 放大器/明天之王/旧我)、EnemyDialogue、DialogueLibrary(台词库+LLM挂点)
│       ├── Personalization/ # 弱点轴、玩家画像、场景匹配、安全过滤
│       ├── Quest/         # 任务与复盘（改进卡）
│       ├── Mobile/        # 虚拟摇杆、触屏按钮、转镜头区（消费式输入，防丢帧）
│       ├── Platform/      # 平台能力封装：WebScreen(安卓网页层=电视画面)、
│       │                  # PipMode(画中画悬浮小窗 + 进出通知)
│       ├── UI/            # HUD(常驻三条+情境七条)、敌人状态条、敌人配置面板、AI提示词面板、
│       │                  # QuizPanel(休养生息答题)、QuizReviewPanel(AI命题审核)、
│       │                  # GoalOSPanel(目标板)、JourneyMapPanel(旅程/障碍/章节来源)、
│       │                  # AdversityHistoryPanel(宿敌逆袭史 + Hall of Goals)、
│       │                  # AdversityProfilePanel(弱点/优势图谱·置信度·可删可关)
│       └── Save/          # 本地 JSON 存档（数据最小化、可一键删除）
```

## 开发路线（对应产品方案）

### V2.0 阶段（本次迭代，对应方案第 22 节）

- [x] 阶段 0 架构冻结：GoalData / Chapter Blueprint / Adversity Budget /
      VulnerabilityEdge / NemesisData 五份数据契约冻结；Legacy 章节资产清单建立（V1 零删减）；
      AI Prompt 与 JSON Schema 输出契约成文
- [x] 阶段 1 开放世界基础：Player Home、六大街区、NPC 日程、交通背景、世界状态持久化，
      V1 玩家控制 / 镜头 / 战斗系统直接在开放世界里跑
- [x] 阶段 2 Goal OS：目标输入、Goal Parser、Journey Graph、Goal Beacon、Goal Priority，
      Legacy 章节与目标绑定
- [x] 阶段 3 AIRequired 章节：Chapter Generator、Validator、Fallback Library、
      ProceduralQuestAssembler（相同 seed 必复现）
- [x] 阶段 4 逆境导演与自适应敌人：行为分析、弱点/优势图谱、五维逆境预算、
      T1-T7 敌人与宿敌、Stress / Resolve / Courage
- [x] 阶段 5 V1 七章开放世界化：24 个子章升级为可被 Goal OS 动态插入的通用逆境模块，
      通过心理裂隙从现实地点进入（原关卡资产原样复用）
- [x] 阶段 5.5 战斗层补齐：敌方武术十类 Archetype（10.5）+ 完整动作语法
      （10.6：Startup/Active/Recovery/Tracking/Poise/GuardDamage/
      CancelWindow/ParryWindow/DodgeWindow/Telegraph，含"重招必须可读"自检）
- [ ] 阶段 6 商业化打磨：武术**动画资产**、开放世界美术密度、
      移动端性能（Addressables / 流式加载）、存档迁移

### V1.0 阶段（已完成，全部保留）

- [x] 第 1-2 阶段：核心动作原型 + 战斗闭环
- [x] 第 3 阶段：心理战系统（五维心理属性、心理攻击、定心格挡、心理硬直）
- [x] 第 4 阶段（本版）：四区域世界（含街道/行人/车辆/昼夜/路灯/房屋/室内）、
      章节故事线、敌人难度分级、战斗特效反馈、敌人实时台词、AI 提示词配置
- [ ] 第 5 阶段：个性化系统（问卷/文本导入 → 画像 → 场景与敌人匹配）、云端 LLM 台词生成
- [ ] 第 6 阶段：可发布 Demo（人形角色模型与动作资产、音效、技能树、设置菜单）
  - [x] 言语攻防（快速选择式回应）+ 反刍值 + 战后复盘四栏
  - [x] 五种战斗姿态（起步/边界/定心/事实/意志：姿态×弱点轴减伤 + 言语契合加成）
  - [x] 责任转嫁法院（终章·按方案落地）：三段式法院（门厅→文件走廊→天平大厅→审判席）、
        可击碎文件山取证、锁链闸破念、责任天平判断归属、责任归还技能、法槌落判 AoE、
        责任球真假判断 + 边界盾 + 过度负责状态 + 边界圈安全区
  - [x] 暂停休养生息答题系统（方案 V3.0）：420 固定题库（来源标签可追溯）、
        能量失衡触发暂停答题、加权抽题（未答/错题/失衡能量优先）、答对回能量、
        答错讲依据不惩罚、2 秒心理护体、练习模式与本章掌握进度
  - [x] AI 自动命题（方案 V3.0 第四十六节）：来源约束提示词 + 动态题源上下文、
        四重自动校验（结构/来源/去重/风险分级）、低风险题即时临时使用、
        人工审核定长期入库、高风险题 100% 人工复核、命题审核面板

## 设计原则

- 游戏基于真实经历抽象为敌人原型与场景气氛，**不复刻真实人物/地点/事件**。
- 心理攻击只做成游戏机制（低语/凝视/泥潭+抽象台词），**绝不输出可复制到现实的操控话术**。
- 失败不惩罚：死亡生成复盘任务，引导策略调整后再战，章节进度保留。
- 隐私数据最小化：个人材料与提示词本地保存，支持一键删除。
